# Fiscal V2 前端升级规划书

> - 状态：APPROVED · 已进入 plan + build 工作流
> - 日期：2026-09-04（Asia/Shanghai）
> - 基线：Fiscal v1.9.0（Build 37）
> - 范围：macOS 与 iPhone 前端的产品结构、信息架构、视觉语言、交互体验与验收
> - 性质：产品与体验规划，不是代码、接口、数据库、工程施工或发布计划

## 0. 文档定位与权威关系

本规划书用于定义 Fiscal V2 的前端升级方向。它回答的是：V2 应该成为怎样的产品、两端如何分工、现有能力如何重新组织、什么体验才算升级成功。

本规划已经确认并进入 `plan + build` 工作流，权威关系如下：

- [`PROJECT_PLAN.md`](PROJECT_PLAN.md) 仍是当前版本、发布状态和既有边界的权威入口。
- 本文件是 V2 的产品与体验依据；它授权本轮已确认的双端前端收缩，但不自动授权提交、发布、后端变更或生产操作。
- 本文件不安排代码结构、源文件归属、接口调整、测试命令、构建顺序或人员分工。
- 正式执行入口和当前阶段只回写到 `PROJECT_PLAN.md`，避免出现两个并行的权威施工计划。

本规划建立在以下既有材料之上：

- [`PROJECT_PLAN.md`](PROJECT_PLAN.md)：当前 v1.9.0 状态、双端体验合同与业务边界。
- [`MACOS_FRONTEND_UPGRADE_CONTRACT.md`](MACOS_FRONTEND_UPGRADE_CONTRACT.md)：现有 macOS 全页面升级合同。
- [`archive/plans/v1.7.0-macos-workspace-reset.md`](archive/plans/v1.7.0-macos-workspace-reset.md)：macOS 工作台重整与已删除的泛化待办思路。
- [`archive/audits/frontend-audit-v1.5.1-2026-08-22.md`](archive/audits/frontend-audit-v1.5.1-2026-08-22.md)：历史前端结构、状态与对象专属构图问题。
- [`archive/releases/v1.8.0/RELEASE_STATE.md`](archive/releases/v1.8.0/RELEASE_STATE.md) 与 [`archive/releases/v1.9.0/RELEASE_STATE.md`](archive/releases/v1.9.0/RELEASE_STATE.md)：当前双端升级与验证基线。

---

## 1. 执行摘要

### 1.1 V2 的一个核心方向

Fiscal V2 应从“由多个财务功能模块组成的 App”，升级为“围绕钱的过去、现在与未来展开的连续财务界面”。

建议用一句话固定 V2 的产品母题：

> **一条财务时间主轴，两种平台工作方式。**

- iPhone 是“现在界面”：适合快速确认当前财务事实、看见最近变化与下一项已知事件、完成单个明确动作。
- macOS 是“连续工作台”：适合在较大范围内浏览、搜索、比较、核对、批量处理、追踪关系与深入分析。
- 两端共享同一种财务语言、状态语义和对象关系，但不共享同一套页面布局。

V2 不是简单换皮，也不是把当前全部页面再画一遍。它首先重组用户理解产品的方式：

- 历史流水、今日状态和已知未来形成连续时间关系。
- 交易、信用应还、分期期次、报销回款和现金流计划以“财务事件”语言呈现。
- 账户、时间、状态、分类、商户和来源成为观察财务事实的范围或镜头，而不是互相割裂的产品世界。
- 信用、分期、报销、现金流等专业能力继续保留，但降到事件关系和专项任务层，不再争夺第一层认知。
- 报表成为观察一组正式财务事实的分析镜头，不再与账目时间上下文断开。
- 设置、主数据、识别/导入、数据与安全、待同步等内容必须可达，但只作为治理或上下文入口存在，不再构成独立的根空间。

### 1.2 V2 的北极星问题

用户打开 Fiscal 后，应能迅速回答：

1. 我当前持有多少钱、欠多少钱，数据更新到什么时候？
2. 最近哪几件事改变了我的财务状态？
3. 下一笔已经知道的流入或流出是什么，何时发生，确定程度如何？
4. 当前是否有一件必须确认、核验或处理的真实财务事件？
5. 某个金额从哪里来、影响了什么、是否已经正式生效？

如果一项新设计不能更快、更清楚地回答这些问题，它即使更现代，也不构成 V2 升级。

### 1.3 V2 最重要的取舍

V2 要舍弃的不是功能，而是以下组织方式：

- 用大量平级模块要求用户先理解产品内部分类。
- 在同一页面堆放入口、摘要、账户卡、搜索、列表和操作，使页面同时承担多个主问题。
- 用卡片套卡片表达所有层级，导致关键金额、普通信息、异常和说明拥有相似重量。
- 把同一笔信用消费、应还、还款或同一笔垫付、待回款、到账拆成互不相干的页面事实。
- 用通用提示组件替代对象内的失败、冲突、结果未知与成功回执。
- 让 iPhone 像缩小的桌面工作台，或让 macOS 像被放大的手机卡片流。
- 把 exactDue 这类“确定性标签”误写成通用待办状态；它只是在未来义务里说明“已准确应付”，不是新的任务中心。

V2 要保留的是：

- 统一账本与既有账务口径。
- CNY 两位小数、金额方向和上海业务日。
- 已有的预览、确认、冲突、幂等、结果未知核验、离线只读和待同步保护。
- 米白、深绿、金色的品牌基础，但重新控制其使用面积与视觉权重。
- iPhone“今日、记一笔、账目”三个高频入口的骨架。
- macOS 在宽屏下持续保留范围、主内容和详情的工作优势。
- 截图记账继续只通过既有快捷指令与轻敲背面两下触发。

---

## 2. 当前体验诊断

### 2.1 已经做对、V2 不应推倒的部分

当前前端并不是一个失败的产品。它已经具备 V2 可以复用的成熟基础：

- 两端主要业务能力完整，真实账务写入和读取路径已经存在。
- 金额、流入、流出、余额、欠款、未来量值等语义已有明确区分。
- 加载、空白、失败、离线、冲突、结果未知、部分完成和成功回执已有较完整覆盖。
- iPhone 已经形成少量高频入口加全屏复杂任务的基本骨架。
- macOS 已经形成适合检查账目与对象详情的桌面工作台雏形。
- 深色模式、动态字体、长内容、窗口宽度和可访问性已有验证习惯。
- 后端事实与客户端推测之间已有严格边界，不能为了视觉效果倒退。

V2 应在这些可靠能力上重建前端叙事，而不是重新发明账务系统。

### 2.2 根本问题一：产品仍然以内部模块为中心

当前 iPhone 虽然只有三个高频入口，但未来能力、信用账期、分期、报销、现金流、AI 待确认、账单导入、报表、设置与治理、待同步等内容仍可能被讲成多个彼此平行的产品块。

当前 macOS 必须明确收束为三个根空间：财务时间线、财务分析、设置与治理。其他能力只能作为时间线的上下文对象、分析下钻，或者设置里的治理入口出现，不能再变成独立的根工作区。

这种结构可以保证“功能都能找到”，却要求用户先判断：

- 这件事情属于过去、今天、未来还是某个对象关系？
- 我应该从时间线、分析还是治理入口进入？
- 这个金额是同一件事情的哪个阶段？

用户真正关心的往往不是模块，而是“这笔钱何时发生、是否已经确定、影响哪个账户、接下来要做什么”。

### 2.3 根本问题二：同一件财务事情被拆散

典型关系包括：

- 信用消费先进入流水，随后形成账期应还，最后通过还款形成现金流。
- 一笔垫付先是支出，随后进入报销单，再形成待回款和实际到账。
- 一笔分期购买既是已经发生的消费，也对应未来期次与信用负债。
- 一项现金流计划先存在于未来，实际结算后又成为正式流水。
- AI 内容或账单识别行在确认前只是候选，确认后才成为正式账目。

当前每个页面可以分别展示这些对象，却没有始终用同一种关系语言把它们串起来。用户容易看到多个金额，但仍需自行判断是否重复计算、哪个是当前真值、下一步应在哪里完成。

### 2.4 根本问题三：“今日”仍偏摘要，“账目”仍偏功能容器

“今日”已经展示当前金额、信用欠款、本月支出、未收报销和已知未来，但仍容易变成多个摘要卡的集合。

“账目”除了搜索和流水，还承担业务入口、账户余额板、筛选和对象详情。页面职责过多后，主问题不再清楚：用户到底是在找一笔账、看账户，还是进入其他功能。

V2 需要把两者的责任重新钉死：

- “今日”只负责当前事实、最近变化、最近未来和真正需要处理的事件。
- “账目”负责以时间主轴浏览历史、今天和已知未来，并提供搜索、筛选与下钻。
- iPhone 的未来不是独立模块，而是“账目”里的一个时间范围。
- 业务工具不能继续用入口卡片挤占账目首屏。

### 2.5 根本问题四：视觉重量过于平均

当前共享视觉语言已经带来一致性，但也容易造成“所有内容都是卡片”的副作用：

- 独立摘要、普通列表、说明、操作、异常和对象详情经常拥有相近的外框与圆角。
- 一页中多个相似容器互相竞争，视觉顺序需要靠阅读文案才能建立。
- 复杂业务页看起来像同一套管理表单换了标题。
- macOS 宽屏出现内容锁在窄列或无意义空白，iPhone 则出现纵向卡片过长。

V2 必须从“组件统一”进一步走向“信息层级统一”。

### 2.6 根本问题五：两端像同一品牌，但还不像同一个产品心智

当前双端一致性更多体现在颜色、金额格式、状态组件和业务规则上；进入方式、详情层级和任务位置仍明显不同。

V2 不追求像素一致，而要让用户在两端都能用同样的顺序理解一件事：

> 何时发生 → 是什么 → 多少钱 → 影响什么 → 现在是什么状态 → 下一步是什么 → 结果是否确认。

---

## 3. V2 产品目标

### 3.1 核心目标

1. **从模块导航转向财务事实导航**
   用户先看到钱和时间，再按需要进入信用、分期、报销或现金流专项能力。

2. **建立历史、今天、未来的连续阅读**
   流水、当前状态和已知未来拥有统一时间锚点，不再是三个互不相干的页面世界。

3. **让同一事件形成可追溯关系链**
   消费与还款、垫付与回款、购买与分期期次、计划与实际结果之间可以顺着对象关系阅读。

4. **减少容器，提升财务层级**
   主要依靠金额、时间、对齐、留白、分隔和状态建立层级，而不是依靠不断增加的卡片。

5. **让动作和反馈留在原对象内**
   编辑、预览、确认、失败、冲突、结果未知和成功回执属于当前事件或任务，不漂移到全局提示。

6. **统一双端产品语言，保留平台优势**
   iPhone 快速、聚焦、单任务；macOS 持续、密集、可比较、可批量。

7. **用任务结果证明升级**
   V2 必须在看懂现在、找到账目、看见未来、完成记账和核验结果等核心任务上明确优于 V1。

### 3.2 产品优先级

V2 的体验优先级固定为：

1. 财务含义正确。
2. 当前状态可信。
3. 重要金额容易看见。
4. 下一步明确。
5. 操作连续且可恢复。
6. 信息密度合适。
7. 视觉精致与动效愉悦。

低优先级不能牺牲高优先级。尤其不能为了减少点击而弱化确认，也不能为了视觉纯净而隐藏状态与口径。

### 3.3 预期产品结果

V2 完成后：

- 用户无需理解内部模块，也能沿时间找到一件财务事实。
- 同一件事情在首页、时间线、详情和报表中的金额与状态一致。
- 已发生、准确应付、已确认未来、预计、候选和结果未知拥有稳定区别。
- 没有异常时界面安静；有问题时问题紧贴相关事件出现。
- iPhone 可以迅速看懂并完成单一动作。
- macOS 可以不丢上下文地浏览和处理更大范围。
- 复杂能力完整可达，但不再平铺成一级导航森林。
- 用户看到的是自己的钱和时间，而不是产品内部的模块结构。

---

## 4. V2 设计原则

| 原则 | V2 要求 | 必须避免 |
| --- | --- | --- |
| 时间先于模块 | 先按过去、今天、未来组织事实，再说明来源领域 | 先选信用、分期、报销模块才能找到一笔钱 |
| 事件是基本阅读单位 | 一件事拥有时间、金额、状态、账户影响、来源与下一步 | 标题和金额在列表，关键关系散落到其他页面 |
| 今天是默认锚点 | 打开后先看到当前状态、最近变化和最近未来 | 用一组月度统计卡替代当前财务状态 |
| 事实与预计严格分开 | 已入账、应还、已确认、已排期、预计、候选均有稳定语义 | 用相同样式把未来计划伪装成已发生事实 |
| 动作依附于事件 | 还款、到账、确认、核验从对应对象发起 | 建立脱离财务对象的通用待办中心 |
| 时间线不是万能页 | 时间线负责理解时间关系，复杂任务进入上下文工作区 | 把所有表单、按钮和工具都塞进时间线 |
| 关系链优于孤立对象 | 展示消费与还款、垫付与回款等上下游关系 | 让用户自行记忆多个页面中的关联 |
| 一个对象只有一个当前真值 | 不同镜头可重复展示，但金额与状态含义必须一致 | 各页面各自解释同一对象并产生冲突 |
| 摘要服务于理解 | 摘要必须能解释构成、来源和下一步 | 堆叠不能下钻、不能解释的数字 |
| 安静状态保持安静 | 正常浏览以内容为主，异常和到期才提升权重 | 所有内容都使用徽标、强调色和大卡片 |
| 卡片只表达真实边界 | 卡片用于独立摘要、决定、预览、异常和回执 | 每个分区、每一行、每个按钮都套卡片 |
| 逐层展开复杂度 | 默认只展示判断所需信息，证据和高级操作按需展开 | 首屏铺开全部字段、说明和危险动作 |
| 上下文必须恢复 | 返回后保留时间、范围、筛选、选择和滚动位置 | 从详情返回后跳回默认首页 |
| 双端同义、各自原生 | 信息顺序和状态一致，布局适合各自设备 | 双端互相缩放或机械复制 |
| 安全状态留在原处 | 失败、冲突、未知和回执留在发起流程中 | 反馈放在被遮住的背景页或短暂消失 |
| 文案说用户语言 | 说明发生了什么、是否完成、下一步是什么 | 暴露接口、版本、请求、令牌等工程语言 |

---

## 5. 核心产品模型：财务事件

### 5.1 定义

“财务事件”是 V2 用于组织界面的产品概念，指已经改变、正在影响或已知将改变用户财务状态的一件事情。

它不取代交易、账户、信用账期、分期、报销单或现金流计划，也不表示新增一种账务记录。现有领域对象继续提供正式事实；V2 只用统一事件语言把它们放进同一条时间脉络。

### 5.2 哪些内容属于事件

| 内容 | V2 中的身份 |
| --- | --- |
| 已入账的支出、收入、转账、信用消费、还款与退款 | 正式历史事件 |
| 信用账单到期、本期准确应还（exactDue） | 已知未来事件；到期或逾期后进入今天附近 |
| 分期计划中的未来期次 | 与分期计划关联的未来事件 |
| 报销垫付、待回款与实际到账 | 相互关联的财务事件 |
| 手工现金流计划 | 已排期或预计的未来事件 |
| AI 待确认内容、账单识别行 | 候选事件；确认前不进入正式账务统计 |
| 离线待同步、冲突、结果未知 | 事件上的系统状态，不是独立财务事件，也不是首页独立模块 |
| 账户 | 事件的作用范围与资金承载对象，不是事件 |
| 分类、商户、来源 | 观察事件的维度，不是事件 |
| 报表 | 观察一组正式事件的镜头，不是事件 |
| 设置、归档、安全与运行状态 | 工具与治理空间，不是事件 |

### 5.3 一个事件必须能够回答的信息

| 维度 | 用户需要知道什么 |
| --- | --- |
| 时间 | 何时发生、何时到期或预计何时发生；业务日使用 Asia/Shanghai |
| 身份 | 这是什么事情，属于哪个真实对象 |
| 金额 | 金额是多少；是实际、准确应付、已确认未来、预计还是候选金额 |
| 方向 | 流入、流出、账户间转移、负债变化或中性量值 |
| 影响 | 影响哪个账户、账期、报销关系、分期计划或现金流计划 |
| 状态 | 已入账、待处理、已排期、预计、取消、作废、归档等 |
| 确定性 | 已发生、确定应还、已确认、已排期、预计或暂时无法确认 |
| 来源 | 手工、AI、账单导入、信用账期、报销、分期或现金流计划 |
| 关系 | 与哪些账目、账期、计划、期次或回款相连 |
| 下一步 | 当前是否需要动作、能做什么、为什么暂时不能做 |
| 结果 | 最近一次操作是成功、确定失败、冲突还是结果未知 |

列表不必一次展示全部字段，但详情必须能够逐层回答它们。

### 5.4 三个时间区

#### 过去：已经形成账务事实

- 展示正式流水、已到账报销、已完成还款、已结清账期、已执行计划和保留历史的作废记录。
- 金额必须使用实际事实，不使用“预计”口吻。
- 先显示日期、标题、实际金额与账户影响，完整关系和来源进入详情。
- 作废和归档仍保留历史位置，但明确说明是否计入当前结果以及可否恢复。
- 报表下钻最终回到同一套历史事件表达，而不是另一套明细语言。

#### 今天：当前理解与行动锚点

“今天”不是泛化待办页，而是围绕当前业务日形成的窄时间带，包含：

- 当前账户、信用与报销的即时事实。
- 最近发生、足以解释当前变化的事件。
- 今天发生或今天到期的事项。
- 已逾期且尚未解决的真实事件。
- 等待确认或核验的候选事件。
- 受到冲突、结果未知或待同步状态影响的事件。
- 距离当前最近、会影响近期安排的已知未来事项。

每一项都必须绑定真实对象。处理完成后，它自然变为已完成、已确认、已核验或正式历史事件，不能在另一套待办列表里留下副本。exactDue 只是未来义务的一种确定性标签，不会单独升级为待办中心。

#### 未来：已经知道但尚未发生

未来区只展示有事实来源的事项，不根据历史行为猜测用户未来消费。

未来事件必须区分：

- **准确应付**：已经形成、金额和责任明确的义务。
- **已确认**：金额与日期已由真实对象或用户确认。
- **已排期**：计划或规则已存在，等待时间到达。
- **预计**：有业务依据，但金额或日期仍存在不确定性。
- **日期待定**：事项成立，但当前没有可靠日期。

未来金额不能混入历史实际金额。任何未来净额都必须标明范围、构成与确定性，并避免重复计算同一义务。

### 5.5 事件生命周期

财务事件在界面中的典型生命周期是：

`候选 → 预计或已排期 → 到期或需要决定 → 已入账或已完成 → 历史凭证`

生命周期允许以下分支：

- 候选被忽略，不进入正式账本。
- 预计事项被修改或取消，保留必要关系和状态说明。
- 到期事项逾期，停留在今天附近并提升紧迫程度。
- 操作发生冲突，进入“需要读取最新事实后重新决定”。
- 操作结果未知，进入“需要核验”，禁止盲目重复提交。
- 离线写入沿用现有边界，显示待同步状态和服务器确认时间。
- 正式记录被作废，仍保留原时间位置、原始关系和历史说明。

业务状态和系统状态必须分开。例如“报销待回款”是业务状态，“离线待同步”是系统状态；两者可以同时存在，但不能互相替代。

### 5.6 关系链与避免重复计算

- 信用消费是消费发生时的正式事件；信用卡还款是之后发生的现金流事件。两者相关，但还款不得再次计为消费。
- 报销垫付是原始支出；待回款是未来关系；实际到账是现金流事实。待回款不能伪装成已收到收入。
- 分期购买是消费事实；分期计划和未来期次描述未来负债安排；实际账期与还款仍按既有规则入账。
- 转账作为一个业务事件展示来源与目标账户的双向影响，不拆成两条看似无关的收入和支出。
- 现金流计划在实际发生后应能说明对应的正式流水，并退出未发生状态，不能继续占用未来合计。
- 同一未来义务如果同时被多个领域对象描述，应展示一个权威事件，其他对象作为关系说明，不能重复计入汇总。

### 5.7 时间主轴的职责边界

时间主轴负责：

- 让用户看懂事件何时发生。
- 把历史、今天和已知未来放进连续关系。
- 提供搜索、时间范围、账户范围和可靠筛选。
- 从事件进入其详情、关系和专项任务。

时间主轴不负责：

- 承载所有表单和复杂写入。
- 替代信用、分期、报销和导入的对象专属结构。
- 建立新的通用待办状态机。
- 展示设置、主数据和系统管理内容。
- 生成现有事实不支持的预测或“安全可花”建议。

---

## 6. 双端共享产品合同

### 6.1 必须共享的产品语言

iOS 与 macOS 必须共享：

- 相同的过去、今天与未来定义。
- 相同的事件分类、信息顺序和关系链。
- 相同的余额、欠款、流入、流出、转账、还款和报销用语。
- 相同的确定性等级与状态名称。
- 相同的异常语义：失败、离线、冲突、结果未知、只读、归档和作废。
- 相同的预览、确认、危险动作和成功回执结构。
- 相同的时间、账户、分类、商户、来源和状态筛选含义。
- 相同的对象入口：从事件能够回到所属账目、账期、分期、报销单或计划。
- 相同的上下文恢复原则：返回后不丢时间、范围、筛选和阅读位置。

### 6.2 必须保留的平台差异

| 体验维度 | iPhone | macOS |
| --- | --- | --- |
| 核心角色 | 快速看懂现在、完成单一动作 | 持续浏览、比较、批量处理、深入分析 |
| 根结构 | 今日、中央记一笔、账目 | 财务时间线、财务分析、设置与治理 |
| 主内容 | 单列、一次聚焦一件事 | 范围、主轴、检查器同时存在 |
| 详情 | 底板或完整页面 | 右侧检查器或上下文工作区 |
| 复杂任务 | 完整页面、清楚返回 | 独立任务工作区，关闭后恢复选择 |
| 批量处理 | 仅在必要场景出现 | 可作为明确模式高效处理 |
| 导航方式 | 触控、系统返回、底栏 | 鼠标、键盘、工具栏、侧栏与检查器 |
| 信息密度 | 克制、渐进展开 | 高密度但保持可扫描 |

### 6.3 双端统一的阅读顺序

一件财务事件在两端都遵循：

1. 时间与对象。
2. 金额与方向。
3. 当前状态和确定性。
4. 账户或负债影响。
5. 来源与关系。
6. 下一步或不可用原因。
7. 最近操作结果。

### 6.4 双端统一的操作节奏

所有写入与高风险动作遵循：

`明确对象 → 填写或选择 → 查看真实影响 → 确认 → 等待结果 → 得到凭证或进入核验`

- 输入发生任何会影响结果的变化后，旧预览必须立即失效。
- 确定失败保留有效输入并给出可执行下一步。
- 冲突要求先获取最新事实，再重新决定。
- 结果未知只能核验，不能把重复提交作为默认动作。
- 成功回执说明实际改变了什么，并留在当前任务中供用户确认。

---

## 7. V2 信息架构总览

### 7.1 建议的产品层级

| 层级 | 产品空间 | 主要内容 |
| --- | --- | --- |
| 第一层：日常 | 今日、账目、记一笔 | 现在事实、连续时间、快速写入 |
| 第二层：对象 | 账户、信用账期、分期、报销、现金流计划 | 事件来源、关系链与专项操作 |
| 第三层：分析 | 财务分析 | 对正式事实的期间观察 |
| 第四层：治理与复核 | 设置与治理 | 主数据、AI 待确认、账单导入、待同步、归档与安全 |

第一层回答“我的钱发生了什么”；第二层回答“这件事属于什么关系”；第三层回答“一组事实说明什么”；第四层回答“哪些内容尚未成为稳定事实”；治理层回答“这套系统如何被管理”。

### 7.2 主关系示意

```text
过去的正式事件  ←  今天锚点  →  已知未来事件
       │               │               │
       └────── 事件详情与关系链 ────────┘
                       │
                 上下文任务流程
                       │
          成功凭证 / 确定失败 / 冲突 / 核验

报表：观察一段正式事件的镜头
工具：把候选内容变成正式事件，或管理系统本身
```

### 7.3 现有能力去留与归并矩阵

| 现有页面或能力 | V2 决策 | V2 中的新角色 | iPhone 安放 | macOS 安放 |
| --- | --- | --- | --- | --- |
| 今日 | 重构 | 当前时间锚点，不再是摘要卡集合 | 默认根入口 | 时间主轴的今天位置与当前摘要 |
| 流水 | 归并并保留 | 过去区的正式财务事实 | “账目”中的历史范围 | 中央时间主轴的历史主体 |
| 已知未来 | 并入时间主轴 | 有来源的未来事件 | “账目”中的未来范围 | 中央时间主轴的未来部分 |
| 账户余额板 | 保留但降级为范围与摘要 | 当前资产/负债构成、账户筛选入口 | 今日摘要与账户详情 | 紧凑账户范围栏或摘要带 |
| 账户详情 | 保留 | 账户范围、余额与相关事件 | 从金额或筛选进入 | 检查器；管理操作进入设置 |
| 记一笔 | 独立保留 | 全局最高频创建动作 | 底部中央固定动作 | 工具栏固定动作与键盘入口 |
| 信用账期 | 保留并降到对象层 | 信用消费、应还、还款的关系对象 | 从欠款、应还或事件进入 | 从事件检查器或专项工作区进入 |
| 分期 | 保留并降到对象层 | 购买、计划、期次与未来负债关系 | 从购买或未来期次进入 | 从事件检查器或专项工作区进入 |
| 报销 | 保留并降到对象层 | 垫付、待回款、到账的关系链 | 从待回款或垫付事件进入 | 从事件检查器或专项工作区进入 |
| 现金流计划 | 保留并降到对象层 | 未来事件来源与计划管理 | 从未来事件或新建入口进入 | 从事件详情或来源筛选进入 |
| 报表 | 保留并重新定位 | 对所选期间正式事实的分析镜头 | 清楚的次级分析入口 | 稳定的分析工作区 |
| 报表下钻 | 归并 | 返回已筛选的正式事件 | 完整明细页，回到原报表 | 中央主轴切到对应事件集合 |
| AI 待确认 | 保留并重新定位 | 候选事件复核工具 | 当前有候选时提示，完整处理进专项页 | 候选镜头或专项审阅工作区 |
| 账单导入 | 独立保留 | 生成候选事件的多阶段工具 | 独立完整任务 | 独立证据、识别行、处置工作区 |
| 待同步 | 取消一级模块身份 | 正式事件或写入上的系统状态 | 相关事件就地显示，保留核验页 | 时间主轴、检查器及恢复工具 |
| 设置与治理 | 归并并保留 | 主数据、AI/导入复核、待同步核验、归档、安全与识别配置 | 今日顶部唯一的低频治理入口 | 三个根空间之一，不再拆出“工具”或“系统与数据”根入口 |
| 作废记录 | 保留 | 历史事件的一种状态 | 历史筛选与详情 | 时间筛选与检查器 |
| 归档对象 | 保留 | 对象生命周期中的只读状态 | 筛选查看，管理入口在设置 | 筛选查看，管理入口在设置 |
| 启动、解锁、离线只读 | 保留并简化 | 进入工作区前的安全门和数据新鲜度 | 独立启动门 | 独立启动门 |

“归并”不等于删除业务能力。V2 删除的是重复入口、割裂阅读和不必要的页面身份，不删除信用、分期、报销、计划、导入、归档或恢复等既有能力。

### 7.4 推荐的显性名称

为避免规划阶段擅自改变用户已经熟悉的入口，首轮原型建议继续使用：

- iPhone：`今日`、`记一笔`、`账目`。
- macOS：`财务时间线`、`财务分析`、`设置与治理`。

其中“现在界面”和“时间主轴”是设计职责，不必直接成为最终按钮文案。名称是否调整，应在真实原型中与信息架构一起评审。

---

## 8. iPhone / iOS 端详细规划

### 8.1 端侧定位

iPhone V2 不是 macOS 工作台的缩小版，而是用户随手打开时最短路径的“现在界面”：先确认当前事实，再处理眼前事件，最后沿时间向过去或未来展开。

iPhone 承担四项核心任务：

1. 快速看懂当前余额、信用负债、待回报销和数据新鲜度。
2. 从一个稳定入口完成高频记账，并在原流程内看见可核验结果。
3. 用同一套时间语言查看已经发生、今天位置和已知未来。
4. 在真实事件上下文里进入信用账期、分期、报销、现金流、AI 待确认和账单导入等复杂任务。

iPhone 不承担：

- 桌面式永久多栏和高密度批量工作。
- 把所有业务模块平铺为底部 Tab。
- 用首页展示所有统计口径和管理工具。
- 根据消费历史生成未经授权的预算、评分、“安全可花”或未来建议。

### 8.2 根导航合同

底部继续只保留三个高频入口：

| 入口 | V2 职责 | 不再承担 |
| --- | --- | --- |
| 今日 | 当前事实、最近变化、最近未来、真实待处理事件 | 功能入口大厅、完整月报、所有账户卡片集合 |
| 记一笔 | 全局最高频创建动作 | 先选功能再进入表单、截图/OCR 入口 |
| 账目 | 连续财务时间主轴、搜索、筛选、详情 | 报表/导入/报销入口卡片矩阵 |

导航必须满足：

- 从任一根入口一次点按可进入记一笔。
- 从任一对象或任务返回后，恢复原根入口、时间范围、账户范围、筛选和滚动位置。
- 核心能力不依赖省略号菜单才能发现；报表、工具和设置使用具名入口或普遍可理解的图标与标题。
- 轻量只读详情使用可关闭底板；编辑、多步预览、导入和高风险操作使用完整页面。
- 避免 sheet 套 sheet。任务从底板升级为完整页面时，必须保留所选对象和返回路径。
- 低频能力可有一个清楚的工具索引兜底，但不能再次做成占据“账目”首屏的功能卡片墙。

### 8.3 “今日”首页

#### 8.3.1 首页职责

首页只回答四个问题：

1. 数据更新到什么时候？
2. 我当前持有和承担的主要金额是什么？
3. 最近发生了什么，下一件已知事情是什么？
4. 当前有没有一件必须确认或核验的真实事件？

首页不承担完整账户管理、报表分析、对象历史、批量复核或所有功能导航。

#### 8.3.2 推荐内容顺序

1. **业务日与数据新鲜度**
   显示上海业务日与最近更新时间。离线时明确改为“离线快照 · 截至……”，不能继续呈现成实时数据。

2. **当前财务位置**
   用一块平面化主区域展示最重要的当前金额。建议以现金/储蓄可用余额为主金额，信用欠款和未收报销作为带标签的支持指标。不得用一个含义模糊的净数代替资产、负债和待回款。

3. **最近变化与最近未来**
   并列或连续展示最近一次正式资金变化，以及下一项有可靠来源的未来事件。点击后将“账目”定位到对应时间与事件。

4. **当前需要处理**
   只在存在真实对象时出现。每项显示来源对象、金额、日期、客观状态和唯一主动作。非零信用欠款本身不自动等于“需要处理”；只有到期、逾期、待确认、冲突、结果未知等状态才提升。

5. **安静结束态**
   没有待处理对象时，用一行平静文案结束，不占一张大面积“没有事项”卡。

#### 8.3.3 首页视觉限制

- 首屏最多保留一个主金额区域和一个需要处理区域。
- 普通支持指标不各自占一张完整卡片。
- 本月支出等期间统计降到次级位置或报表，不与当前余额争夺第一层级。
- 首页不放报表、导入、报销等入口网格。
- 说明文字只解释口径与数据时间，不写工程边界或大段教育文案。
- 待同步写入若未成为服务器确认事实，首页数字必须明确是否包含，并同时给出确认数据时间。

### 8.4 “账目”连续时间主轴

#### 8.4.1 时间结构

- 默认定位在今天锚点。
- 锚点上方是过去的正式事件，锚点下方是有来源的未来事件。
- 提供“更早 / 回到今天 / 更晚”或等价显式导航，不能只依赖用户猜测滚动方向。
- 按上海业务日分组；跨月与跨年有清楚边界。
- 没有可靠日期的未来事项进入“日期待定”，不强塞到任意一天。
- 搜索结果保留日期信息，但明确进入“搜索结果”模式，避免伪装成自然时间轴。

#### 8.4.2 事件行语法

每个事件行优先呈现：

1. 标题。
2. 金额与方向。
3. 日期或相对时间。
4. 账户或关系对象。
5. 确定性与必要状态。

事件行必须满足：

- 金额完整、等宽、稳定对齐；长标题不能挤压金额。
- 已发生、准确应付、已确认、已排期、预计和候选均有文字标识。
- 未来金额明确流入或流出，但不使用已完成状态误导用户。
- 转账表现为一个事件，并在详情中说明来源与目标账户的双向影响。
- 作废与归档拥有不同名称和状态，不能只用降低透明度处理。
- 普通行使用平面列表、留白和分隔；只有需要决定、异常或回执才使用完整容器。

#### 8.4.3 搜索、范围与筛选

筛选至少应覆盖现有事实可靠支持的维度：

- 时间范围。
- 全部账户或单账户。
- 事件类型。
- 已发生或未来确定性。
- 分类、来源与状态。
- 是否包含作废或归档对象。

交互要求：

- 当前筛选始终可见，可单独移除或一次清空。
- 切换账户不静默清掉仍适用的时间条件。
- 筛选为空、搜索无匹配、本身无数据、读取失败和离线快照无匹配使用不同文案。
- 读取更早历史或更远未来时保留已加载内容；下一页失败只在列表末端就地重试。
- 从结果进入详情再返回，原搜索词、筛选、位置与选择保持。

### 8.5 记一笔

#### 8.5.1 高频录入原则

- 一点即到金额，不先进入功能选择大厅。
- 输入顺序以真实任务为准：金额、类型、账户关系、业务日期、名称、分类、备注。
- 支出、收入、信用消费、转账和还款共享稳定骨架，仅在类型需要时展开额外字段。
- 备注等低频字段可以弱化，但必填事实不能藏在“更多”后面。
- 每个操作区只有一个主操作。

#### 8.5.2 特殊类型

- 转账与还款明确使用“从 / 到”关系，不用两个语义含糊的账户选择。
- 来源和目标必须不同，账户类型必须与现有账务规则匹配。
- 切换交易类型后，不兼容的账户、分类、账期或其他关联立即清除。
- 普通账目保持直接保存；需要预览的动作继续先展示真实影响再允许确认。
- 任何影响结果的输入变化都立即使旧预览失效。

#### 8.5.3 错误、键盘与结果

- 空白新表单保持安静；字段失焦、被修改或尝试提交后才显示对应错误。
- 页面级失败、冲突和结果未知留在当前完整页面内。
- 键盘出现时，当前字段、错误和底部主操作始终可见，并提供清楚的收起路径。
- 保存成功不能只靠短暂提示。完成面应说明金额、类型、账户、业务日期和实际保存结果。
- 成功后提供“再记一笔”和“查看详情”；待同步使用不同凭证，不能冒充服务器已确认。
- 有内容的草稿关闭时避免误丢失，但本规划不新增跨设备草稿或模板能力。

### 8.6 事件与账目详情

所有详情采用统一事实顺序：

1. 标题、金额、日期和状态。
2. 账户影响或资金去向。
3. 分类、来源与领域关系。
4. 当前可用动作及不可用原因。
5. 分录、证据和变更历史。

详情行为：

- 轻量只读浏览可由中/大两档底板承接。
- 一旦进入编辑、预览、冲突处理或危险确认，切换为完整任务页。
- 分类、加入报销、转为分期、作废或恢复围绕当前对象展开，始终显示所操作金额和对象身份。
- 手势只能作为有可见替代入口的快捷方式；危险动作不能依赖一次侧滑完成。
- 关闭详情后回到原事件，不重置时间线。

### 8.7 账户体验

账户在 V2 中是范围和资金承载对象，不再占据一个与时间线竞争的入口。

- 首页当前金额可下钻到账户组成。
- “账目”可按全部账户或单账户查看。
- 账户详情展示余额语义、类型、数据时间、相关事件与现有可用动作。
- 信用账户必须使用欠款、额度、应还等信用语义，不能使用普通可用余额模板。
- 账户管理、归档与恢复进入设置；日常详情与管理详情保持清楚边界。
- 已归档账户可查看历史，但明确只读。

### 8.8 报表

报表是对一段正式财务事实的观察镜头，而不是另一个账本。

- 从首页进入时默认当前期间；从时间线进入时尽量继承可支持的期间与账户范围。
- 顶部持续显示月/年、当前期间、账户范围、当前镜头和数据更新时间。
- 保留总览、支出、现金流、债务四个镜头；每个镜头先给一个结论，再给构成和明细。
- 不把桌面表格压缩到手机；一屏只承担一个主要比较。
- 所有图形提供数字列表或表格替代。
- 零值、没有记录、统计方式不可用、数据过期、离线和读取失败明确区分。
- 下钻进入完整明细页，顶部保留镜头、期间和范围；返回后恢复原位置。
- 导出确认再次显示期间、统计方式、币种和上海业务日。
- 报表只陈述现有事实，不生成消费评价、节流建议或未经支持的预测。

### 8.9 信用、分期、报销与现金流计划

这些能力保留对象专属结构，但主要从相关金额、事件或关系进入；低频索引只负责兜底发现。

| 任务 | V2 iPhone 结构 |
| --- | --- |
| 信用账期 | 先显示信用账户、当前负债、最近到期日与准确应付，再按时间列账期。还款前明确付款账户、信用账户和目标账期。调整账期按设置、查看影响、确认分步。 |
| 分期 | 计划列表以状态和下一期识别对象；详情以期次时间为主，区分已锁定、未来、取消、已完成和提前结清。购买事实与未来负债不能混为同一金额。 |
| 报销 | 始终同时标明报销总额、已到账和未到账。详情围绕当事人、垫付账目和到账记录展开。预计回款不能伪装成已收到资金。 |
| 现金流计划 | 明确分开未来事项与历史；计划金额与真实结算金额同时出现时各自带标签。计划不能自动伪装成正式流水。 |

共同要求：

- 每个任务都有对象摘要、关系、当前状态、下一步和结果。
- 高风险动作先说明对象、金额、影响和是否可恢复。
- 复杂流程使用完整页面，不在时间线行内展开长表单。
- 完成后回到原事件，并让状态就地收敛。

### 8.10 AI 待确认、账单导入与待同步

#### AI 待确认

- 候选内容在确认前不进入正式余额、流水和报表。
- 显示来源、识别结果、低置信字段、用户最终值与实际入账影响。
- 原始内容默认收起，不用技术诊断压过财务事实。
- 保留现有自动执行边界，不扩大权限；自动完成的内容必须有清楚凭证与来源。
- 确认、修改、忽略、撤销和结果核验留在当前候选流程中。

#### PDF / 账单导入

- 保持“选择文件与隐私边界 → 本机提取 → 校验 → 逐行处置 → 确认前检查 → 结果凭证”的完整步骤。
- 逐行页显示页码证据、识别内容、候选匹配、缺失和可能重复。
- 批量确认清楚列出新建、关联、跳过与失败项。
- 只有最终确认才产生账务写入。
- 结果未知只允许核验，不允许再次盲目提交。

#### 待同步

- 它是相关事件上的系统状态和一个安全核验页，不升级为日常模块，也不作为首页独立产品模块存在。
- 队列、同步中、冲突需重新决定、结果未知与确定失败分别展示。
- 不新增待同步原地编辑，也不自动重放结果未知的写入。
- 首页与汇总金额明确说明待同步内容是否计入。

### 8.11 设置与治理

设置首页分成三组：

- **财务基础**：账户、分类、商户、归档区。
- **识别与导入**：AI 与识别设置、质量状态、账单导入入口及隐私说明。
- **数据与安全**：运行状态、数据兼容状态、加密归档、恢复边界和个人口令。

要求：

- 正常系统状态保持紧凑，异常才提升视觉权重。
- 账户、分类、商户表单按任务分组，不做无尽的设置长页。
- 信用字段只在信用账户出现。
- 归档、恢复、合并、拆分和修改口令先说明对象、影响和不可逆边界。
- 不新增附件中心、设备管理或当前产品不具备的归档恢复能力。

### 8.12 截图快捷指令固定边界

V2 必须完整保留现有方式：

- 只允许 iOS 快捷指令读取最近 10 分钟截图。
- 由用户轻敲背面两下触发。
- App 内不新增“选择照片”“导入截图”“扫描截图”或常驻 OCR 入口。
- 不扩大照片读取范围。
- 快捷指令产生的内容沿用当前候选与确认规则，不能因 V2 视觉升级扩大自动执行权限。
- 设置可以解释现有触发方式和隐私范围，但不能伪装成新的 App 内能力。

### 8.13 iPhone 视觉与交互语言

- 延续米白、深绿和金色品牌基础，但减少整页大圆角容器。
- 普通信息依靠留白、时间位置、金额对齐和发丝分隔建立层级。
- 卡片只保留给决定、预览、冲突、结果未知、离线说明和成功凭证等明确边界。
- 深绿用于品牌、选择、主操作和既有正向金额；金色用于支出、欠款和客观注意；红色用于确定错误与危险；黄色用于预计、离线、待核验和结果未知。
- 任何状态同时使用文案、图形或结构，不能只靠颜色。
- 核心金额使用等宽数字、CNY 两位小数和稳定对齐；标签必须说明余额、负债、流入、流出、预计或计划。
- 同一页面最多一个主操作；禁用原因只显示一次。
- 动效只解释状态变化，不承担导航结构。减少动态效果时保持完整理解。
- 返回、关闭、底板和键盘遵守 iPhone 习惯；财务写入不能只有隐藏手势入口。

### 8.14 iPhone 可访问性与适配

- 继续面向现代 iPhone、iOS 26+ 和竖屏；不顺带扩大到 iPad、横屏或旧系统。
- 所有可操作目标至少 44pt，底部操作避开安全区。
- 默认字号、XXXL 与辅助功能大字号均完成真实页面验收。
- 大字号按“元信息换行 → 左右结构转上下结构 → 按钮纵排 → 页面滚动”重排，不缩小核心金额。
- VoiceOver 顺序遵循“日期与对象 → 金额和方向 → 状态 → 动作”。
- 报表图形提供等价数字内容。
- 支持浅色、深色、减少动态效果和减少透明度。
- 状态变化后正确移动焦点：错误到首个字段、冲突到冲突标题、成功到结果凭证。

### 8.15 iPhone 产品验收

1. 进入首页后，无需滚动即可识别主金额和数据更新时间或离线状态。
2. 首页能把最近一次实际变化和下一项已知未来连接到同一时间主轴。
3. 从任一根页面一次点按可进入金额录入。
4. 普通账目、转账、信用消费和还款的方向、账户与金额语义正确。
5. 未来事件正式发生后，首页、时间线、详情和报表收敛到同一事实，不保留重复“幽灵事件”。
6. 搜索、筛选、详情和复杂任务返回后，根入口、范围、选择与滚动位置保持。
7. 信用、分期、报销、现金流、AI 和导入拥有对象专属结构，不只是同一长表单换标题。
8. 每项写入均能在当前任务看到加载、字段错误、确定失败、冲突、结果未知和成功凭证。
9. 结果未知不会提供盲目重复提交。
10. 浅色、深色、默认字号、XXXL、辅助功能大字号、长名称和长金额均无关键内容截断。
11. App 内不存在新增照片、截图或 OCR 入口。
12. 正式金额都能追溯到既有账务事实或用户输入，界面不产生第二套金额真相。

---

## 9. macOS 端详细规划

### 9.1 端侧定位

macOS V2 定位为“可持续停留的个人财务桌面”。它不是放大版 iPhone，也不是传统财务后台，而是适合长时间浏览、核对、比较、批量处理、追溯关系和分析事实的工作台。

macOS 主要承担：

- 在较长时间与较大数据范围内浏览财务事件。
- 同时看见当前范围、主要内容和对象详情。
- 快速搜索、筛选、移动选择和连续处理多笔内容。
- 检查信用、分期、报销、现金流和账单导入的复杂关系。
- 进行期间分析、报表下钻与导出。
- 管理主数据、归档、安全和系统状态，但这些都必须收束到“设置与治理”。

它不应：

- 把所有业务能力永久平铺在主导航中。
- 为每个业务对象重复建立一套互不一致的三栏。
- 使用大量放大的手机卡片填满宽屏。
- 用最大化窗口制造更大的空白、卡片或标题。

### 9.2 macOS 信息架构

| 层级 | 建议入口 | 职责 |
| --- | --- | --- |
| 核心工作区 | 财务时间线 | 默认入口，连续查看过去、今天和已知未来 |
| 分析工作区 | 财务分析 | 月/年、总览、支出、现金流、债务、下钻与导出 |
| 治理工作区 | 设置与治理 | 主数据、AI 待确认、账单导入、待同步、归档、安全与识别设置 |
| 上下文对象 | 信用账期、分期、报销、现金流计划 | 从事件、筛选或全局新建进入的专项工作区 |
| 全局动作 | 记一笔 | 固定在工具栏和键盘入口，不作为主导航模块 |

“需要核验”只能筛出现有事件上真实存在的冲突、结果未知或候选状态，不能重新建立一个脱离对象的泛化待办中心。

### 9.3 主窗口空间结构

默认工作区由四个稳定区域组成。

#### 9.3.1 窗口工具栏

工具栏只承载全局且高频的内容：

- 当前工作区名称和范围。
- 回到今天。
- 时间范围。
- 搜索。
- 筛选入口与生效数量。
- 刷新及数据时间。
- 全局“记一笔”主操作。
- 导入、导出等次级动作菜单。

约束：

- 同一行最多一个实心主按钮。
- 页面内部动作不堆进全局工具栏。
- 当前时间、账户与筛选不能只藏在弹窗里。
- 工具栏在普通、紧凑和全屏窗口中保持稳定顺序。

#### 9.3.2 左侧范围栏

左侧只回答“我正在看什么”，不同时承担品牌海报、模块森林和复杂表单。

主要内容：

- 财务时间线与财务分析。
- 今天、本月、未来等常用时间视角。
- 全部账户与单账户范围。
- 当前范围内可用的事件来源或状态筛选。
- 底部工具、系统与设置入口。

账户是时间线范围，不是与流水并列的另一个事实世界。选择账户后，中央内容和右侧检查器同步进入该账户语境。

#### 9.3.3 中央时间主轴

- 获得窗口大部分宽度。
- 长期承载事件列表、日期分组、搜索结果和报表下钻明细。
- 不被大面积账户卡、说明横幅或页面标题持续挤压。
- 保持稳定列宽与金额对齐，减少每次切换业务模块时完全重建页面。

#### 9.3.4 右侧检查器

- 始终围绕当前事件、账户或关系对象工作。
- 可收起、可调整宽度，但信息顺序稳定。
- 没有选择时展示紧凑范围摘要、更新时间和可发现的快捷提示。
- 不用大图标和大段空态占据整栏。
- 不充当另一个业务模块首页。

### 9.4 财务时间主轴

#### 9.4.1 时间方向与锚点

推荐方向：较早日期在上，较晚日期在下，打开时定位今天。这样过去在上、未来在下，符合常规时间阅读与列表追加习惯。

首轮原型必须同时验证这一方向与反向方案；最终选择以真实使用时的定位速度、滚动理解和未来浏览效率为准，不仅凭审美决定。

时间轴要求：

- 今天使用克制标记，不占一张巨型卡片。
- 历史、今天与未来共享日期节奏，但确定性明确不同。
- 跨月显示月份边界，跨年显示年份。
- 日期标题可随滚动保持可见。
- 任意位置都可一键或快捷键回到今天。
- 筛选后仍保留日期语境，不退化成无时间信息的后台表格。

#### 9.4.2 日期分组与摘要

- 每个日期组可显示简短实际流入、实际流出或事件数量。
- 已发生与未来金额不得合并为一个看似精确的日合计。
- 今天到期、逾期和结果未知在日期组内提升，但仍绑定原事件。
- 无事件日期默认不占空间；只有分析或连续日历场景才显示零值日期。

#### 9.4.3 事件行

桌面事件行的固定阅读顺序：

1. 日期或时刻。
2. 标题。
3. 类型、账户或来源。
4. 状态与确定性。
5. 金额。
6. 必要动作标记。

要求：

- 金额右对齐并使用等宽数字。
- 长名称可以进入第二行，不能挤压金额。
- 收入、支出、欠款、回款和中性量值使用既有语义，同时配合文字或形状。
- 流水、还款、分期期次、报销回款与计划事件只有必要的细微区别，不各自变成一张大卡。
- 只有决定、异常、风险、预览和回执使用完整表面。
- 选择、键盘焦点、多选、作废、归档和未来状态彼此可区分。

#### 9.4.4 事件到对象工作区

选择事件后，检查器先展示事件本身，再提供打开信用账期、报销单、分期计划或现金流计划的上下文入口。

进入专项工作区时保留：

- 原时间范围。
- 原账户范围和筛选。
- 原滚动位置。
- 原选中事件。
- 清楚的返回路径。

完成处理回到时间线后，原事件就地更新状态。用户不需要重新搜索或重新选择范围。

### 9.5 时间、账户与筛选

范围和筛选分开表达：

- 时间范围回答“看哪段时间”。
- 账户范围回答“看哪些资金”。
- 筛选回答“这段时间里的哪些事件”。

#### 9.5.1 时间范围

建议提供今天、本月、上月、未来 30 天、未来 90 天和自定义范围。

- 当前范围始终显示在工具栏。
- 前后期间切换保留兼容的账户和筛选。
- 不适用条件被清除时，要明确告知，不能静默重置。
- 业务日期统一使用上海时区。

#### 9.5.2 账户范围

- 支持全部账户与单账户。
- 信用账户使用欠款语义，不伪装成普通余额。
- 账户切换后，检查器先显示余额、类型、当前范围摘要和相关事件。
- 已归档账户仍可看历史，但明确只读。
- 长账户名可换行或弱化机构信息，金额保持完整。

#### 9.5.3 筛选与搜索

- 只使用现有事实能可靠表达的条件。
- 生效条件以紧凑标签展示，可单独移除或一次清空。
- 搜索默认作用于当前范围，同时明确提供“搜索全部账本”。
- 无结果必须区分范围无数据、筛选过窄、搜索无匹配、读取失败和离线快照无匹配。
- 搜索、筛选、选择和检查器在刷新后保持合理稳定，不被旧结果覆盖。

### 9.6 右侧检查器

#### 9.6.1 单项选择

统一信息顺序：

1. 标题、金额和状态。
2. 下一步动作或当前限制。
3. 业务日期、发生时刻和账户影响。
4. 分类、来源及关联对象。
5. 分录或余额影响。
6. 修改历史和来源链。
7. 写入结果、冲突或未知结果。

首屏必须先回答“这是什么、多少钱、现在是什么状态、我能做什么”。技术来源和完整历史进入下方渐进内容。

#### 9.6.2 多选

多选检查器展示：

- 已选数量。
- 按流入、流出及不可相加项分开的摘要。
- 共同可用的批量操作。
- 因类型或状态不兼容而不能处理的项目及原因。
- 提交前影响与提交后逐项结果。

不能用一个总金额掩盖方向、确定性或业务含义不同的对象。

#### 9.6.3 操作区

- 操作区固定在检查器容易到达的位置。
- 同一阶段最多一个主操作。
- 作废、恢复、结清、取消等与普通编辑分区。
- 危险操作明确对象、金额、影响和能否恢复。
- 预览失效、冲突、结果未知和离线只读留在当前检查器或任务工作区。
- 双击事件只打开详情或任务，绝不直接触发财务写入。

### 9.7 记一笔与编辑任务

- “记一笔”始终位于工具栏，并具有稳定键盘入口。
- 金额进入任务后首先获得焦点。
- 类型切换只展示当前类型需要的字段。
- 账户、目标账户、分类和日期使用一致语义。
- 来源/目标、资金方向和对象身份在确认区清楚可见。
- 简单编辑可在检查器或紧凑任务面完成；复杂或高风险操作进入独立任务工作区。
- 保存区不随空白漂移；长表单滚动时关键结果和主操作仍可找到。
- 完成后返回原时间线范围，并定位到新建或更新事件。

### 9.8 信用、分期、报销与现金流计划

这些能力不再永久占据左侧一级导航，但保留专业工作区和全局新建入口。

统一专项结构：

- 顶部：来源对象、总金额、当前状态、最近与下一关键日期。
- 中央：该对象包含的事件、账期、期次、当事人或计划项。
- 右侧：当前选择详情、可用动作和结果。
- 完成后：时间主轴立即反映结果，关闭后恢复原事件。

专项要求：

| 领域 | macOS 重点 |
| --- | --- |
| 信用账期 | 信用账户、账期状态、应还、已还、未还、本期消费与还款关系同时可追踪。调整与还款不争夺同一视觉主位。 |
| 分期 | 购买、计划、期次和未来负债按时间对应；已锁定、未来、取消与结清清楚区分。 |
| 报销 | 报销总额、已到账、未到账、当事人、垫付与多次回款形成可检查关系。 |
| 现金流计划 | 系统来源与手工计划、计划与结算、未来与历史分别可辨；只读对象不显示假可用主按钮。 |

### 9.9 财务分析与报表

- 分析继承可支持的时间范围；不支持的账户或事件筛选明确说明已回到正式报表口径。
- 顶部固定显示月/年、期间、分析镜头、更新时间与导出。
- 首屏使用一条对齐的关键指标带，避免多个平级大卡片。
- 总览、支出、现金流和债务各自先给结论，再给构成与明细。
- 图表具备刻度、单位、状态说明和等价数据表。
- 点击分类、账户或指标进入可返回的明细，返回后保留期间、镜头和滚动位置。
- 空期间、零值、数据不完整、离线和读取失败分别呈现。
- 导出为次级动作；完成、取消和失败各有清楚回执。

### 9.10 AI 待确认与账单导入

#### AI 待确认

- 作为候选事件复核工具，不混入正式金额。
- 列表显示候选金额、来源、低置信字段与当前状态。
- 检查器对照脱敏证据、建议值、用户最终值、入账影响和结果。
- 原始识别内容默认收起。
- 适合连续审阅，但不能因效率降低单项确认风险。

#### 账单导入

账单导入保留完整的多阶段治理工作区，但它的入口应属于“设置与治理”，而不是独立根模块。它确实需要同时看见：

- 左侧页面与脱敏证据。
- 中央识别行、状态与批量选择。
- 右侧当前行的候选匹配、修正和处置。

顶部明确显示选择、隐私确认、解析、复核、校验、确认和回执阶段。页面、行和检查器始终同步；单行与批量动作分层；结果未知只能核验；完成后可直接查看产生或匹配到的时间线事件。

### 9.11 设置与治理

- 低频治理工具位于侧栏底部，保持可达但安静。
- 设置采用分类与内容并置，减少重复标题和容器。
- 账户、分类、商户的新建、编辑、排序、合并、拆分、归档和恢复始终显示当前对象。
- 数据与安全只作为治理内容的一部分存在，不独立成一个常驻根空间。
- 正常状态紧凑，风险和不可逆操作提升。
- 创建归档、修改口令等动作说明范围、不包含内容、完成结果和安全下一步。

### 9.12 键盘与鼠标

建议形成稳定且可发现的快捷体系：

| 操作 | 建议快捷方式 |
| --- | --- |
| 记一笔 | `⌘N` |
| 搜索当前范围 | `⌘F` |
| 刷新当前工作区 | `⌘R` |
| 财务时间线 | `⌘1` |
| 财务分析 | `⌘2` |
| 返回上一个上下文 | `⌘[` |
| 移动事件选择 | 方向键或 `J / K` |
| 快速预览 | `Space` |
| 打开当前对象 | `Return` |
| 关闭或退出编辑 | `Esc` |
| 确认有效预览 | `⌘Return` |

规则：

- 所有快捷键同时出现在菜单或帮助提示中。
- 文本输入时，单字母导航不能抢占输入。
- 单击选择，双击打开，`Shift` 连续多选，`Command` 离散多选。
- 右键菜单只承载次级操作，并与检查器可用动作一致。
- 悬停不是发现操作的唯一方式。
- 拖拽不能直接改变账务结果、归档状态或分类归属。

### 9.13 窗口尺寸与空间适配

以当前支持的约 `1000 × 680` 最小窗口为基线，同时覆盖标准窗口和全屏。

#### 宽窗口

- 完整显示文字侧栏、时间主轴和检查器。
- 时间主轴保持舒适阅读宽度，不随超宽屏无限拉长。
- 多余空间优先增加有效比较信息和检查器可读性，不放大卡片与标题。
- 报表可使用双列，但文字行宽受控。

#### 标准与紧凑窗口

- 侧栏可收为图标栏。
- 检查器缩窄或临时收起，但主轴金额和状态保持可读。
- 次要字段转为纵向排列，不通过缩小字体硬塞。
- 导入工作区优先保证识别行可读，次要证据区允许收起。
- 主工作区不以横向滚动补救布局。

#### 窗口行为

- 调整过的栏位宽度和显隐在同一工作区保持。
- 全屏与普通窗口切换不丢范围、选择和滚动位置。
- 长标题、极大金额、大量筛选和多行错误单独验收。
- 缩小时先收起次级区域，不改变金额字号和核心语义。

### 9.14 macOS 产品验收

1. 打开后无需切换业务模块，即可看到今天附近的历史、当前事项和已知未来。
2. 任一事件都能直接说明金额、状态、账户影响、来源和下一步。
3. 时间、账户、筛选、报表和专项工作区之间往返不丢原选择与滚动位置。
4. 信用、分期、报销与现金流计划从事件和全局新建均可到达，但不占永久一级导航。
5. 最小、标准和全屏三种窗口无关键操作遮挡、金额碰撞或主布局横向滚动。
6. 长名称、极大金额和大字号下，金额与状态优先完整。
7. 仅用键盘可以搜索、移动选择、打开详情、预览、记一笔、确认和返回。
8. 鼠标右键、双击和多选符合桌面习惯，不直接触发危险写入。
9. 报表期间、口径、数据时间和下钻路径始终可见，返回后不丢上下文。
10. 导入全过程可同时核对证据、识别行、处置状态与最终范围。
11. 设置与治理易于找到，但不与日常时间主轴争夺注意力。
12. 加载、空白、离线、确定失败、冲突、结果未知、部分完成和成功回执各自稳定可恢复。
13. 浅色、深色、增强对比度、减少动态效果、减少透明度、VoiceOver 和完整键盘控制均可完成核心任务。
14. 界面不再出现大面积卡片堆叠和无意义空白；信息密度提高，但阅读顺序仍能一眼建立。

---

## 10. V2 共享视觉语言

### 10.1 视觉母题

V2 的视觉关键词是：

> **安静、准确、连续、有时间感。**

界面应像一份持续更新、可以行动的个人财务记录，而不是营销仪表盘，也不是企业后台。

推荐的视觉关系：

- 画布安静，内容清楚。
- 金额比容器重要。
- 时间比模块标题重要。
- 状态比装饰重要。
- 关系通过对齐和脉络表达，不通过更多卡片表达。
- 异常需要被看见，但正常状态不制造紧迫感。

### 10.2 色彩

延续现有米白、深绿、金色方向，但重新规定使用面积。

| 色彩角色 | 使用范围 | 禁止用途 |
| --- | --- | --- |
| 深绿 | 品牌、当前选择、主要动作、既有正向金额 | 大面积背景、普通说明、所有可点击内容 |
| 金色 | 支出、欠款、到期与客观注意 | 确定错误、危险操作、普通未来事项背景 |
| 红色 | 确定错误、危险和不可逆风险 | 一般警告、预计、离线、结果未知 |
| 黄色 | 预计、离线、待核验、结果未知 | 已完成事实、普通支出、成功 |
| 中性色 | 正文、次级信息、普通列表和容器 | 用过低对比度隐藏必要信息 |

色彩规则：

- 状态永远不能只靠颜色表达。
- 已发生事实与未来事项即使方向相同，也应通过确定性标签区分。
- 深色模式不是简单反色；应保持画布、主内容、检查器和状态之间的层级。
- 正常页面不形成全绿、全金或全米白的一色调外观。
- 增强对比度和减少透明度时，信息优先级不得变化。

### 10.3 字体与金额

- 页面标题、区段标题、事件标题、正文、辅助说明和字段标签形成固定层级。
- 核心金额使用等宽或制表数字、稳定小数位和清楚对齐。
- 金额必须配合含义：余额、欠款、流入、流出、应还、预计、计划或中性统计值。
- 长金额不能以省略号隐藏；可在金额自身区域滚动或展开查看。
- 负数符号、币种、千位和两位小数在两端保持一致。
- 辅助信息不得小到或淡到无法承担日期、账户和状态等必要含义。
- 大字号优先换行和纵向重排，不通过缩小核心金额保住原布局。

### 10.4 卡片、容器与分隔

卡片只用于以下边界：

- 独立的当前摘要。
- 真正需要用户决定的事件。
- 预览与确认。
- 离线、冲突、结果未知和确定失败。
- 成功凭证与部分完成结果。
- 需要与周围内容隔离的高风险区域。

普通内容优先使用：

- 日期分组。
- 网格与列对齐。
- 留白。
- 区段标题。
- 发丝分隔。
- 选中背景。
- 轻量状态标签。

限制：

- 不允许卡片套卡片成为常规布局。
- 单屏不应出现大量外观相同、权重相同的盒子。
- 普通事件行不各自使用阴影。
- macOS 不使用放大的 iPhone 卡片填充宽屏。
- iPhone 不以长篇 Section 卡片堆叠替代任务分步。

### 10.5 图标与图形

- 图标辅助识别对象或动作，不替代业务名称。
- 同一对象在两端使用相同语义图形。
- 图标按钮必须有可见或可访问名称；低频危险动作不能只放无文字图标。
- 不使用 emoji 作为账务状态图标。
- 报表图形必须有刻度、单位、数值与等价列表。
- 装饰性图表、无数据支撑的趋势和只为填空的插图不进入正式界面。

### 10.6 动效与触感

动效只用于说明：

- 当前选择发生变化。
- 事件从未来转为已发生。
- 预览因输入变化而失效。
- 操作进入处理中、成功、失败或核验状态。
- 详情、检查器和完整任务之间的层级转换。

约束：

- 动效不承担唯一状态表达。
- 不用装饰动画延迟高频操作。
- 结果未知不能使用与普通失败相同的消失动画。
- 减少动态效果时，所有状态与层级仍完整。
- iPhone 触感只辅助确认，不替代可见凭证。

---

## 11. 共享交互与状态合同

### 11.1 标准任务节奏

普通写入与高风险操作统一遵循：

1. 明确当前对象。
2. 填写或选择必要信息。
3. 显示当前可验证事实或操作影响。
4. 允许用户确认。
5. 展示处理中状态。
6. 进入确定成功、确定失败、冲突或结果未知之一。
7. 成功后显示凭证；失败后保留可恢复上下文；未知后只允许核验。

高风险操作不能仅为了少一步而跳过对象和后果确认。

### 11.2 主操作

- 每个操作区最多一个主操作。
- 页面级主操作与对象级主操作不在同一区域竞争。
- 次要动作、安静动作和危险动作拥有稳定视觉层级。
- 禁用动作必须解释一个最直接原因，重复原因只显示一次。
- 不能把导航按钮画成财务确认按钮。
- 不能把刷新、返回、查看详情等普通动作做成整栏强强调。

### 11.3 表单

- 字段按用户真实填写顺序，而不是按后端字段顺序排列。
- 新表单初始安静，不在未交互时显示整页错误。
- 字段错误贴近字段；跨字段或页面级问题在当前任务顶部。
- 保存只在内容有效且确有变化时可用。
- 切换对象、类型或任务时，旧错误、旧预览和不兼容选择必须收敛。
- 关闭后是否保留草稿必须明确，不能偶然保留一半状态。
- 成功后表单不继续展示过期错误或旧确认面。

### 11.4 状态矩阵

| 状态 | V2 必须做到 | 禁止出现 |
| --- | --- | --- |
| 初始 | 保持安静，说明当前可做什么 | 空表单立即满屏报错 |
| 加载 | 保留页面骨架与范围，说明正在读取什么 | 无限转圈、整个页面反复闪空 |
| 真空 | 说明确实没有对象，并在有真实下一步时提供动作 | 用“暂无数据”掩盖失败或无权限 |
| 筛选为空 | 保留筛选，提供清除或调整入口 | 困在空页面、静默清除条件 |
| 过期数据 | 显示数据时间和刷新方式 | 把旧快照伪装成实时事实 |
| 离线只读 | 标明截至时间、可看范围和不可写原因 | 暗示所有写入都会自动排队 |
| 待同步 | 标明本地状态、服务器确认时间和下一步 | 混入正式汇总而不说明 |
| 字段错误 | 贴近所属字段，修正后及时消失 | 只在全局横幅展示字段问题 |
| 页面级失败 | 保留输入与已读事实，在当前任务内重试 | 错误只出现在被遮挡的背景页 |
| 确定失败 | 说明没有完成、原因和安全下一步 | 清空有效输入或使用成功样式 |
| 冲突 | 展示对象已变化，要求读取最新事实后重新决定 | 静默覆盖、沿用过期预览 |
| 结果未知 | 说明无法判断结果，阻止盲目重复，提供核验 | 把超时直接当失败并允许重提 |
| 部分完成 | 分列已完成和未完成，只重试剩余部分 | 笼统显示全部成功或整批重做 |
| 分页失败 | 保留已加载内容，在末端就地重试 | 清空整个列表或替换成错误页 |
| 预览失效 | 标明输入已变化，重新检查后才可确认 | 修改金额/账户/日期后提交旧预览 |
| 危险确认 | 明确对象、金额、影响和可恢复性 | 只用红色按钮表达风险 |
| 成功 | 说明实际完成内容，并更新相关事实 | 只显示短暂图标、无法回查结果 |
| 作废 | 保留历史位置、说明不再计入或可否恢复 | 无痕消失或与归档混淆 |
| 归档 | 明确只读和恢复边界 | 与作废共用同一名称和动作 |

### 11.5 上下文恢复

以下状态在适用时必须恢复：

- iPhone 根入口或 macOS 工作区。
- 时间范围与今天锚点。
- 账户范围。
- 搜索词与筛选。
- 报表期间与镜头。
- 当前选择。
- 滚动位置。
- 尚未完成但明确允许保留的输入。

返回、关闭、刷新和完成操作均不能把用户无理由送回默认首页。

### 11.6 文案合同

界面文案优先回答：

- 发生了什么。
- 哪笔钱或哪个对象受到影响。
- 操作是否完成。
- 当前数据更新到什么时候。
- 用户接下来能做什么。

正式界面避免：

- 请求、接口、令牌、版本、修订、幂等键等工程术语。
- “未知错误”“操作失败”等没有下一步的模糊文案。
- 把未来、预计、待确认写成已经发生。
- 用“总额”描述不可相加的不同方向或不同确定性金额。
- 用“余额”同时指资产余额、信用欠款和净资产。
- 用“成功”描述排队、未知或尚未读回确认的结果。

---

## 12. 安全、隐私与账务边界

### 12.1 账务事实

- 不改变支出、收入、信用消费、还款、转账、退款、报销回款的既有方向与口径。
- 不改变信用消费不因还款而重复计为消费的规则。
- 不改变报销垫付、待回款和到账的区别。
- 不改变分期购买、未来期次、账期和还款的关系。
- 不把未来事项、候选或待同步内容静默混入正式历史与报表。
- 不为统一时间线建立第二套余额或事件真相。

### 12.2 数据来源

- 界面只展示现有事实能够支持的金额、日期、关系与状态。
- 数据源不支持的趋势、预测、逐行明细或关系，应明确说明暂不可用。
- 不在前端拼接不同时间版本的数据，制造看似完整但不可核验的结果。
- 不用本地推测冒充正式预览、成功凭证或服务器最新事实。

### 12.3 隐私

- OCR、PDF 与账单原文默认克制展示，仅在当前复核上下文展开。
- 通知、列表摘要和错误信息不暴露敏感原文。
- 账单导入明确说明文件、本机提取、脱敏内容和保存边界。
- 截图快捷指令继续限定最近 10 分钟截图与用户主动触发。
- 不因 V2 增加 App 内相册、照片、截图或常驻扫描入口。

### 12.4 前端范围边界

- 本规划默认不改变 Backend/API/数据库/migration。
- 若某项设计缺少现有事实支持，应登记为产品阻断，不能由前端猜测完成。
- 本规划不授权新增待同步原地编辑、提醒、附件、设备管理或新的归档恢复能力。
- 不扩大到 iPad、Apple Watch、Web、多用户、多账本或家庭协作。

---

## 13. V2 分阶段路线图

### 13.1 推进原则

- V2 不以“完成多少页面”为进度，而以“一个完整用户问题是否被解决”为进度。
- 每一阶段同时考虑 iPhone 与 macOS；不能先冻结一端，再让另一端被动适配。
- 每一阶段结束都有决策门。方向问题未解决时，不进入下一阶段堆积页面。
- 先证明时间主轴、事件语法和双端分工成立，再迁移全部复杂能力。
- 每项现有能力都必须有明确去向，但不强迫所有功能进入时间线。
- 产品安全优先于减少点击；高风险动作允许必要减速，但必须更清楚。
- 截图只能证明视觉结果；返回、筛选、确认、失败恢复和上下文保持必须用完整交互验证。

### 13.2 阶段总览

| 阶段 | 核心问题 | 建议重心 | 关键决定 |
| --- | --- | ---: | --- |
| D0 探索与基线 | 当前为什么仍不满意 | 10% | 是否认可 V2 北极星与双端分工 |
| D1 信息架构 | 钱、时间、对象和动作如何重新组织 | 15% | 财务事件与时间主轴是否成立 |
| D2 视觉与交互原型 | 新方向是否第一眼更清楚、更像 Fiscal | 20% | 是否冻结视觉母题和交互节奏 |
| D3 双端核心体验 | 高频主链是否真实优于 V1 | 25% | 是否值得继续覆盖全部能力 |
| D4 全能力收敛 | 复杂能力是否融入同一产品语言 | 20% | 是否达到完整能力覆盖 |
| D5 全局收口 | V2 是否可以完整替代 V1 | 10% | Go / No-Go |

比例表达工作重心，不是固定日历。阶段未通过决策门时，不因时间到达自动进入下一阶段。

### 13.3 D0 · 探索与体验基线

#### 目标

把“前端不满意”拆成可以观察、比较和验证的问题，避免再次只凭新旧截图判断升级。

#### 产物

1. **双端现状体验地图**

   - 一级入口、二级工作区、详情、底板、确认与返回关系。
   - 每个页面实际回答的用户问题。
   - 重复入口、绕路、层级过深、无意义空白和卡片堆叠位置。
   - 同一业务在两端的结构、名称与状态差异。

2. **任务清单与优先级**

   - 高频低风险：看余额、看最近流水、快速记账、搜索。
   - 高频中风险：修改分类、编辑账目、浏览未来。
   - 低频高风险：转账、还款、分期、报销到账、作废、导入确认。
   - 低频管理：账户、分类、商户、设置与治理。

3. **体验问题证据册**

   每个问题记录：用户原本想做什么、首先看见什么、经过哪些入口、在哪里停下来理解、可能误读什么、属于哪一端或共享语言。

4. **V1 基线记录**

   在相同数据、设备和起点下记录完成时间、导航次数、返回或改选次数、是否需要解释、是否发生金额/日期/状态误判，以及完成后能否确认结果。

5. **北极星与非目标确认稿**

   固定 V2 是连续财务界面，不是新的待办中心、模块大厅、三栏缩放、卡片换皮或账务规则重写。

#### 决策门 G0

- 主要不满来自信息架构、视觉构图、操作节奏，还是三者共同作用？
- 时间与事件能否覆盖最高频主链？
- 哪些内容明确不进入时间主轴？
- iPhone 与 macOS 的角色是否足够不同且互补？
- 成功能否用任务结果判断，而不只用“看起来更现代”判断？

#### 退出条件

- V1 所有用户可达能力均已登记。
- 至少十条关键任务完成双端基线记录。
- 北极星、平台分工与非目标得到用户确认。
- 争议问题被列为待验证假设，而不是伪装成既定结论。

### 13.4 D1 · 信息架构与产品语义

#### 目标

先确定用户如何理解 Fiscal，再确定页面如何呈现。

#### 产物

- 财务事件语法及过去、今天、未来定义。
- 已发生、准确应付、已确认、已排期、预计、候选、待同步、冲突与结果未知的状态词典。
- 双端导航合同。
- V1 能力到 V2 的完整映射。
- 账户、时间、状态、分类、商户与来源的范围/筛选合同。
- 从事件到对象详情、专项任务、报表下钻和返回的关系图。
- 双端统一术语与禁止用语清单。

#### 决策门 G1

- 用户能否明确区分历史事实、未来事项和候选内容？
- 时间线是否仍是财务时间线，而没有退化为任务列表？
- 每项 V1 能力是否都有自然去向？
- 每个一级入口是否只承担一个清楚主问题？
- 两端是否共享语义又保留各自工作方式？
- 是否存在必须靠口头解释才能理解的核心标签？

#### 退出条件

- V1 能力映射覆盖率 100%。
- 高频任务没有孤立入口、死路或无法解释的多入口。
- 所有写入流程定义了发起点、确认点、结果位置和返回位置。
- 双端导航、术语、状态和关系合同冻结。
- 不适合进入时间主轴的功能均有明确理由与去向。

### 13.5 D2 · 视觉与交互原型

#### 目标

证明 V2 不仅结构正确，而且能在第一眼形成更强的金额层级、时间感和行动感。

#### 核心低保真原型

iPhone 至少覆盖：

- 今日正常与有待处理事件。
- 账目的过去、今天、未来。
- 记一笔。
- 事件详情与对象关系。
- 搜索、筛选和返回。

macOS 至少覆盖：

- 时间/账户范围。
- 中央时间主轴。
- 单项和多项检查器。
- 记一笔。
- 财务分析与明细返回。

#### 核心高保真原型

必须覆盖：

- 正常数据与数据密集。
- 长账户名、长商户名与极大金额。
- 空白、加载和筛选无结果。
- 离线或过期数据。
- 确定失败、冲突与结果未知。
- 危险确认、部分完成和成功凭证。
- 浅色、深色、大字号和减少动态效果。
- iPhone 键盘与安全区。
- macOS 最小窗口、标准窗口和全屏。

#### 新旧并排评审

对同一任务比较：

- 第一眼最先看到什么。
- 主金额是否清楚。
- 历史/今天/未来是否可识别。
- 下一步是否明确。
- 操作路径是否更短或更连续。
- 风险与结果是否更可信。
- 是否仍有“后台模块”或“卡片模板”感。

#### 决策门 G2

- 5 秒内能否说出最重要的金额、状态和下一步？
- 不看页面标题时，能否判断处于过去、今天还是未来？
- 去掉颜色后，状态是否仍可理解？
- iPhone 是否像原生移动体验，macOS 是否像真正的桌面工作台？
- 是否仍存在卡片套卡片、信息同权、主操作竞争或说明压过事实？
- V2 是否明确优于 V1，而不只是不同？

#### 退出条件

- 核心原型完成至少两轮真实交互走查。
- 金额、时间、状态与动作误判问题被关闭。
- 用户确认视觉母题、信息密度和平台差异。
- 核心状态在异常数据下仍成立。
- 后续页面不再依赖口头补充布局规则。

### 13.6 D3 · 双端核心体验成型

#### 目标

先让四个最重要的问题形成完整闭环：

1. 我现在持有和承担的主要金额是什么？
2. 最近发生了什么？
3. 接下来已知会发生什么？
4. 我怎样记下一笔钱，并确认它确实保存？

#### iPhone 核心范围

- 启动、解锁、数据时间与离线状态。
- 今日。
- 账户与关键余额。
- 账目的历史、今天和未来。
- 搜索、筛选和详情。
- 记一笔与结果凭证。
- 直接关联事件的轻量动作。
- 返回后恢复原上下文。

#### macOS 核心范围

- 启动、解锁和数据状态。
- 时间与账户范围。
- 财务事件浏览。
- 搜索、筛选、选择与检查器。
- 账户与关键余额。
- 快速记账与必要确认。
- 连续处理多笔时的上下文稳定。
- 最小、标准和全屏窗口。

#### 决策门 G3

- 四个北极星问题能否直接回答？
- iPhone 是否明显更适合快速查看和单项处理？
- macOS 是否明显更适合连续浏览和深度处理？
- 两端是否展示同一财务事实？
- 高频任务是否在速度、清晰度或上下文保持上优于 V1？
- 即使只看到核心范围，用户是否已经感到这是 V2 而非换皮？

#### 退出条件

- 高频核心场景连续通过。
- 金额、方向、账户、日期和结果误判为 0。
- 核心流程覆盖正常、空、加载、失败、离线、冲突和结果未知。
- 两端分别通过验收，不能互相替代。
- 至少四项核心任务优于 V1，其余不退化。
- 用户明确认可继续覆盖完整能力。

### 13.7 D4 · 全能力收敛

#### 目标

让复杂能力进入同一套 V2 语言，同时保留专业性与安全边界。

#### A. 债务与确定未来

- 信用账期。
- 还款。
- 分期创建、预览、修改、结清与取消。
- 到期、逾期、结清与未来期次。

验收重点：购买、负债、账期、期次和还款关系清楚，不把专业对象压扁成普通流水。

#### B. 报销与资金关系

- 报销单。
- 关联垫付。
- 已到账与未到账。
- 多次回款。
- 预览、确认与结果未知。

验收重点：报销总额、已到账、未到账始终带标签，关系与现金流不重复计算。

#### C. 分析与复核

- 月报、年报。
- 总览、支出、现金流和债务。
- 分类、账户等构成、下钻与导出。
- AI 待确认。
- PDF / 账单导入。

验收重点：报表是观察正式事实的镜头；AI 与导入是产生或核验候选的工具，不成为新的信息孤岛。

#### D. 工具、治理与主数据

- 账户、分类、商户。
- 待同步。
- 设置、安全、归档和恢复边界。
- AI 待确认与账单导入。

验收重点：不强行事件化，但完整继承 V2 的表单、状态、错误、危险确认和回执语言；这些内容统一从“设置与治理”或对象上下文进入，不再形成新的根模块。

#### 决策门 G4

- 所有 V1 用户能力是否都有清楚、自然且可发现的 V2 去向？
- 复杂能力是否仍保留完整的财务关系与必要信息，而没有为了视觉简化被压扁？
- 信用、分期、报销、分析、AI、导入和系统工具是否都融入同一套金额、时间、状态与回执语言？
- 是否出现了新的一级模块森林、重复入口或脱离原对象的处理中心？
- 所有高风险动作是否更容易确认对象、金额、后果以及最终结果？
- iPhone 与 macOS 是否在能力完整性上对等，同时保持各自适合的平台工作方式？

#### 每组退出条件

- 旧入口与新入口映射完整。
- 正常与异常状态矩阵完整。
- 能力没有因视觉简化丢失。
- 没有重新形成新的一级模块森林。
- 高风险操作更容易确认对象与后果。
- 编辑、预览、确认和结果属于同一连续流程。
- 返回后保留范围、期间、筛选、选择与阅读位置。
- 双端没有仅因迁移遗漏形成的不对称。

### 13.8 D5 · 全局收口与上线判断

#### 目标

证明 V2 可以完整替代 V1，而不是只证明核心截图漂亮。

#### 产物

- 双端全页面体验清单。
- 全局一致性审查。
- 金额、日期、状态、按钮和文案审查。
- 可访问性与大字号审查。
- 长内容、极端金额和数据密度审查。
- 全量场景矩阵及可复核证据。
- V1 / V2 核心任务对比。
- 已知限制与接受理由。
- Go / No-Go 结论。
- 上线后一周和一月的观察问题。

#### 决策门 G5

- V2 是否真正形成连续财务界面？
- 用户是否会主动选择 V2，而不是因 V1 被移除才使用？
- 两端是否均达到完整水平？
- 是否存在未解释的失败、未知结果或上下文丢失？
- 是否有任何可能导致误记、重复写入或误判资产负债的界面问题？
- 遗留项是否真的不影响安全、理解和主要任务？

#### 退出条件

- 所有硬性上线条件通过。
- 无最高和高严重度体验问题。
- 中等问题均有明确接受理由或后续处理决定。
- 完成真实数据规模的只读走查与受控写入、核验、恢复走查。
- 用户完成最终并排验收并明确给出 Go 结论。

---

## 14. V2 验收体系

### 14.1 四层验收

| 层级 | 核心问题 | 验收对象 |
| --- | --- | --- |
| 页面理解 | 第一眼能否看懂 | 金额、时间、状态、对象与主动作 |
| 任务完成 | 是否能正确完成事情 | 入口、步骤、确认、结果与返回 |
| 异常恢复 | 出错后是否仍可信 | 失败、冲突、离线、未知与部分完成 |
| 产品替代 | 是否真实优于 V1 | 双端完整性、任务指标与用户主动选择 |

任一层失败，都不能用视觉精致度抵消。

### 14.2 基线与测量方法

- V1 与 V2 使用相同设备、相同数据、相同网络条件和相同任务起点。
- 每个高频场景至少重复三次：第一次记录可发现性，后两次记录熟悉后的效率。
- 同时记录耗时、操作次数、退回次数、误操作、是否需要解释、是否确认结果。
- 单用户产品不伪造大样本统计；如只有产品所有者参与，应如实记录样本数，以连续多次通过替代虚假百分比。
- 高风险任务不单纯比较速度；正确性、对象确认和结果可核验拥有否决权。
- 视觉比较使用同一组脱敏数据和相同尺寸，避免用更有利的样本粉饰 V2。

### 14.3 定性体验问题

每个核心页面必须让用户无需解释回答：

1. 我在哪里？
2. 这里最重要的钱是什么？
3. 这笔钱已经发生、将要发生，还是需要处理或核验？
4. 它影响哪个账户、负债或报销关系？
5. 我现在能做什么？
6. 操作之后，系统认为发生了什么？

核心流程还应满足：

- 用户感受到在处理与钱相关的事情，而不是在模块之间找功能。
- 视觉重点与财务重要性一致。
- 不需要记住上一页内容才能理解当前页。
- 普通查询快速；需要谨慎时界面主动减速。
- iPhone 与 macOS 像同一个产品，但不是同一布局的缩放版本。
- 对“清楚、可信、从容、连续”的主观评价不低于 4/5；“可信”若未达到 5/5，必须追查具体原因。

### 14.4 定量体验目标

| 指标 | V2 目标 |
| --- | --- |
| 当前状态理解 | 打开首页后 5 秒内，正确说出主金额、数据时间、下一件已知事件和是否有待处理项 |
| 高频任务成功 | 核心场景连续三轮全部成功 |
| 财务语义错误 | 金额、方向、账户、日期、账期和状态误判为 0 |
| iPhone 普通记账 | 不慢于 V1；熟悉后目标 15 秒内完成普通支出，无多余页面跳转 |
| iPhone 高频入口 | 今日、账目、记一笔从根界面一步可达；常用详情不超过两次层级转换 |
| macOS 核心工作区 | 一次主导航可达；搜索、选择和详情不丢范围或筛选 |
| 查找最近交易 | 不慢于 V1；熟悉后目标 10 秒内找到并确认详情 |
| 查看下一重要事件 | 熟悉后目标 8 秒内找到金额、日期、确定性和影响账户 |
| 上下文保持 | 适用场景中根入口、期间、筛选、选择和滚动位置 100% 保持 |
| 写入结果可确认 | 每一笔写入都有确定成功、确定失败、冲突或结果未知，覆盖率 100% |
| 异常恢复 | 高风险场景均能在不重复写入、不误清输入的前提下恢复或核验 |
| 处理中反馈 | 用户动作后立即有状态变化；持续操作有明确处理中对象 |
| iPhone 触控 | 所有目标至少 44pt，关键操作不被键盘和安全区遮挡 |
| 长内容适配 | 核心金额、状态、对象和动作不被截断，允许换行、堆叠与滚动 |
| 颜色独立性 | 移除颜色后仍能通过文字、符号或结构理解全部状态 |
| 跨端一致 | 同一事件的金额、方向、日期、关系与状态一致率 100% |

若 V1 已优于某个目标，则以“不低于 V1”为更高门槛。

### 14.5 核心场景矩阵

| 场景 | 风险 | iPhone 重点 | macOS 重点 | 必测状态 |
| --- | --- | --- | --- | --- |
| 启动、解锁、连接 | 高 | 清楚能否进入及是否只读 | 启动后工作区状态明确 | 加载、口令错误、服务不可用、离线、过期数据 |
| 看懂今天 | 高 | 首屏看见主金额、最近变化与下一事件 | 今天锚点与当前范围清楚 | 正常、全零、大金额、长账户名、部分数据缺失 |
| 普通记账 | 高 | 单手、键盘、错误与保存可见 | 快速输入并回到原时间线 | 非法金额、缺账户、保存中、失败、未知、成功 |
| 转账与还款 | 极高 | 来源、目标、方向与后果明确 | 对象与确认信息完整 | 同账户、类型不匹配、冲突、结果未知 |
| 查找流水 | 高 | 搜索、筛选、详情和返回连续 | 表格、选择、检查器与多选 | 无结果、分页失败、作废、编辑失败、清除筛选 |
| 浏览未来 | 高 | 确认、预计、逾期与取消可辨 | 时间范围切换保持上下文 | 空、远期、到期、日期变化、账户筛选 |
| 信用与分期 | 极高 | 应还、日期和下一步优先 | 账期、期次与关联交易可追踪 | 无账单、结清、逾期、预览失效、冲突、取消 |
| 报销与回款 | 极高 | 总额、已到、未到都有标签 | 垫付、报销单与回款关系清楚 | 多次回款、超额、预览失效、部分完成、未知 |
| 现金流计划 | 高 | 计划与实际、未来与历史可辨 | 连续管理多项计划 | 空、只读、结算、取消、冲突、未知 |
| 报表与下钻 | 中高 | 期间、镜头与范围持续可见 | 多维分析与明细不丢上下文 | 零值、空、过期、首次失败、下一页失败、导出失败 |
| AI 待确认 | 高 | 证据、低置信字段和最终值易对照 | 连续审阅但不弱化风险 | 零候选、低置信、撤销、失败、未知、历史回读 |
| PDF / 账单导入 | 极高 | 分步处理，原文不过载 | 证据、识别行和处置并列 | 空白、缺失、冲突、部分成功、分页失败、离线 |
| 主数据与设置 | 高 | 长表单分组、错误就地 | 管理高效且对象明确 | 初始空表单、重复项、冲突、危险确认、成功读回 |
| 跨端对照 | 极高 | 移动端简洁但不缺含义 | 桌面端密集但不改含义 | 同一数据、同步延迟、缓存过期 |

每个场景必须记录：起点、任务、期望财务结果、期望界面结果、返回位置、异常恢复方式和验收证据。

### 14.6 可访问性矩阵

#### iPhone

- 浅色与深色。
- 默认字号、XXXL 与辅助功能大字号。
- VoiceOver。
- 减少动态效果与减少透明度。
- 键盘展开、切换字段和关闭键盘。
- 长账户、长分类、长商户、极大金额和多行错误。
- 底栏、底板与安全区。

#### macOS

- 最小、标准与全屏窗口。
- 浅色、深色和增强对比度。
- VoiceOver 与完整键盘控制。
- 减少动态效果与减少透明度。
- 收起/展开侧栏与检查器。
- 长对象名、大金额、多筛选和多行错误。
- 鼠标、触控板、右键、双击与多选。

### 14.7 Go / No-Go

#### 硬性 No-Go

出现任意一项即不得进入发布：

- 金额、方向、账户、日期、账期或报销口径存在歧义。
- 任一写入可能因重试造成重复结果。
- 修改输入后仍可确认过期预览。
- 确定失败、冲突和结果未知没有清楚区分。
- 高风险流程缺少异常状态验收。
- iPhone 或 macOS 任一端的核心主链未完成。
- 同一事件在双端呈现不同财务含义。
- 返回、刷新或切换范围产生危险上下文错位。
- 大字号、VoiceOver、键盘或窗口尺寸使关键操作不可达。
- 用户仍认为核心体验是在模块中找功能，而不是围绕财务事实行动。
- V2 只能在精心挑选的正常截图中成立。

#### Go 条件

- 北极星问题在两端均能快速回答。
- 核心任务连续多轮通过。
- 所有极高风险场景与状态变体通过。
- 金额与账务语义错误为 0。
- 至少四项高频任务客观优于 V1，其余不退化。
- 用户在 V1/V2 并排时主动选择 V2。
- 两端都形成完整体验，不存在明显主次版本。
- 遗留项均不影响理解、安全、可达性与结果核验。
- 完成真实数据规模走查，并记录已知限制。

仅局部间距、非关键动画和不影响理解的次要润色可以留到上线后。缺失状态、金额歧义、导航死路与跨端语义差异不能归入“后续优化”。

---

## 15. 风险、反模式与护栏

### 15.1 时间线成为新的万能首页

**风险**：把入口、表单、分析、系统状态和所有动作继续塞进时间线，只是把旧“账目页”换了名字。

**护栏**：

- 时间线只负责看懂时间关系、范围、事件和入口。
- 复杂操作进入对象专属任务。
- 报表、导入、设置保持独立职责，但入口必须收束在财务时间线、财务分析或设置与治理中。
- 核心时间线首屏不放功能入口卡片矩阵。

### 15.2 重建泛化待办中心

**风险**：把信用、报销、导入、分类和同步抽成一个新的“需要处理”总表，重走已经删除的产品路径。

**护栏**：

- 每项需要处理的内容必须绑定真实事件或候选对象。
- 处理状态来自原对象，不复制一套新的可编辑状态。
- 完成后回到原事件生命周期，不保留第二份待办。
- 不新增静音、稍后、优先级或提醒系统，也不把 exactDue、待同步或候选状态泛化成待办。

### 15.3 时间线信息过载

**风险**：把所有领域细节都压成事件行，导致手机拥挤、桌面扫描困难，并丢失专业业务结构。

**护栏**：

- 行只显示判断所需字段。
- 关系、证据、分录和完整历史逐层展开。
- 信用、分期、报销、导入保留对象专属工作区。
- 时间线不展示系统设置和主数据维护，治理入口单独收束到“设置与治理”。

### 15.4 未来与历史混淆

**风险**：统一时间后，预计、已排期、准确应付和正式流水看起来相同，用户误以为未来已入账。

**护栏**：

- 确定性使用文字、结构和图形，不只靠颜色。
- 未来金额不混入历史实际合计。
- 未来事项正式发生后由正式事件接替，避免重复。
- 日期待定单独成组，不伪造日期。

### 15.5 视觉“去卡片”变成缺乏边界

**风险**：为了减少卡片而失去分区、选中状态与高风险边界。

**护栏**：

- 使用网格、留白、分隔、日期组和检查器建立结构。
- 决定、预览、异常、危险和回执继续使用明确容器。
- 平面化不等于降低对比度或删除状态。

### 15.6 为前端理想伪造数据

**风险**：为了完整时间线或漂亮图表，在前端计算现有事实无法保证的预测、趋势或关系。

**护栏**：

- 只展示可追溯事实。
- 缺失能力明确登记，不在客户端拼成权威结论。
- 不引入“安全可花”、财务评分或未知未来预测。
- 任何新数据需求须单独获得范围确认。

### 15.7 双端机械统一

**风险**：用同一布局追求一致，导致 iPhone 复杂、macOS 空洞。

**护栏**：

- 统一内容顺序、状态、术语与关系，不统一像素布局。
- iPhone 一次聚焦一件事。
- macOS 同时保留范围、主内容与检查器。
- 两端分别验收，不能用一端通过替代另一端。

### 15.8 视觉探索无限扩张

**风险**：未冻结母题就持续尝试颜色、圆角和动画，拖延核心信息架构验证。

**护栏**：

- 先过低保真信息架构门，再进入高保真。
- 每轮原型围绕同一组任务和数据对比。
- G2 通过后冻结核心视觉语言，后续只做有证据的调整。

---

## 16. 明确非目标

本轮不做：

- 新的账务业务模块。
- 通用待办、提醒、日历或优先级系统。
- 预算、超支评价、消费评分或“安全可花”。
- 无现有事实支持的智能预测和建议。
- 扩大 AI 自动执行权限。
- App 内照片、截图或 OCR 入口。
- 待同步原地编辑。
- 新附件中心、设备管理或新的归档恢复能力。
- iPad、Apple Watch、Web 或 Android。
- 多用户、家庭共享、多账本协作。
- 为追求简洁删除现有高风险确认或异常恢复。
- 为追求跨端一致机械复制布局。
- 以技术术语或后端对象名称作为用户导航。
- 在本规划中安排代码结构、接口、数据库、测试命令、提交或发布。

---

## 17. 首轮需要冻结的产品决定

下列决定应在 D0–D2 通过原型冻结，不能拖到全页面覆盖后再讨论。

| 决定 | 推荐默认 | 验证方式 |
| --- | --- | --- |
| V2 母题 | 一条财务时间主轴，两种平台工作方式 | 双端核心任务原型 |
| iPhone 根入口名称 | 暂保留“今日 / 记一笔 / 账目” | 与“现在 / 时间线”等名称并排测试 |
| iPhone 首页主金额 | 优先展示含义明确的当前可用余额；资产、负债、待回款分开 | 5 秒理解测试 |
| iPhone 报表入口 | 具名次级入口，不进底栏，也不藏在入口卡片墙 | 可发现性任务 |
| macOS 主导航 | 财务时间线、财务分析、设置与治理 | 标准窗口与连续任务走查 |
| macOS 时间方向 | 默认过去在上、未来在下、打开定位今天 | 双向方案滚动与定位比较 |
| 账户位置 | 作为范围与当前摘要，不再是独立事实世界 | 账户切换与返回测试 |
| 卡片使用量 | 仅摘要、决定、异常、预览、回执和危险区 | 新旧截图与视线顺序评审 |
| 品牌变化程度 | 保留米白、深绿、金色，减少大面积使用 | 浅深色高保真对照 |
| “需要处理”范围 | 仅真实事件上的到期、逾期、候选、冲突与核验 | 事件状态矩阵 |
| 未来窗口 | 默认突出最近事件；30/90 天作为可选范围 | 真实数据密度测试 |
| 复杂任务承载 | iPhone 完整页面，macOS 上下文工作区 | 还款、报销到账与导入原型 |

---

## 18. 规划与设计交付物

在进入完整前端升级前，应具备：

1. 双端现状体验地图。
2. 高频与高风险任务清单及 V1 基线。
3. V2 信息架构图。
4. 财务事件词典、状态词典和关系规则。
5. 现有能力到 V2 的 100% 映射表。
6. iPhone 核心低保真与高保真原型。
7. macOS 核心低保真与高保真原型。
8. 双端统一视觉语言与平台差异说明。
9. 按钮、表单、详情、预览、确认和回执的交互合同。
10. 正常、空白、加载、离线、失败、冲突、未知和部分完成状态矩阵。
11. 文案与术语清单。
12. 可访问性、动态字体、窗口与长内容矩阵。
13. V1/V2 核心任务并排比较记录。
14. 每阶段决策门与用户确认记录。
15. 最终 Go / No-Go 评审材料。

---

## 19. V2 产品完成定义

Fiscal V2 只有同时满足以下条件，才算前端升级完成：

- 用户不需要先理解内部模块，就能沿时间找到一件财务事实。
- “今日”能快速说明当前财务位置、最近变化、最近未来与真实待处理事件。
- “账目”形成过去、今天和已知未来的连续阅读，而不是功能入口集合。
- macOS 中央主轴、范围和检查器形成稳定工作关系，且根结构稳定收束为财务时间线 / 财务分析 / 设置与治理。
- 同一事件在两端使用相同金额、方向、日期、确定性、状态和关系。
- 信用、分期、报销、现金流、AI 和导入仍保留完整专业能力。
- 已发生、未来、候选、待同步、冲突和结果未知绝不互相冒充。
- 操作的错误、冲突、未知和成功凭证留在原任务上下文。
- 返回、刷新、切换范围与下钻不会破坏用户位置。
- 页面主要依靠金额、时间、对齐、留白和状态建立层级，不依赖卡片堆叠。
- iPhone 在快速理解和单项任务上明显优于 V1。
- macOS 在连续浏览、比较、核验和分析上明显优于 V1。
- 浅色、深色、大字号、VoiceOver、减少动态效果、长内容和支持窗口范围均成立。
- 所有高风险流程在正常和异常条件下均可安全完成或核验。
- 用户在 V1/V2 并排评审中主动选择 V2。

最终判断标准不是“页面是否全部重画”，而是：

> **Fiscal 是否终于让人先看见自己的钱、时间和关系，再看见工具。**
