# v1.5.0 · F3 已知未来与高风险领域执行记录

> 规划冻结：2026-08-15（Asia/Shanghai）｜权威状态、解锁顺序与变更入口始终以 [`PROJECT_PLAN.md`](../../PROJECT_PLAN.md) 为准。本记录展开 F3 的后端契约、所有权及验收；不是平行控制面，也不授权施工范围以外的改动。

## 1. 顺序、共享边界与证据

严格串行：`F3-A → F3-B1 → F3-B2 → F3-C → F3-D → F3-E → F3-F → F3-G`。每一块均须 Builder → Independent Review → 当前 Builder 修复/复测 → Independent Review；只有最终 **0 findings** 才解锁下一块。F3-C、F3-D 已达到 **Independent implementation review verified**，各自 macOS runtime gate deferred。F3-E 审查链 `1×P1+1×P2+1×P3 → 1×P2 → 0 findings` 已终审，当前为 **Independent implementation review verified；`F3E-MAC-UI-AUTOMATION` deferred**。F3-F 前置 Backend D3 与 frontend 均已终审；frontend 终态为 **Independent implementation review verified；`F3F-MAC-UI-AUTOMATION` deferred**。F3-G 第七次 Independent Review 为 **0 findings**，终态见 §9；现仅解锁 F4 Planner/Build planning，F5 与正式 root 仍锁定。

每块可先扩 `App/Sources/FiscalKit/V15/Foundation/{V15Contracts,V15Services,V15State}.swift` 的 schema-shaped typed DTO/enum/request/response/receipt/409，再修改自己独占的 `Features/<domain>/{iOS,macOS}/**`、`Shared/Fixtures/F3*`、F3 Gallery route/host、`Tests/FiscalKitTests/V15/F3*`、`Tests/V15GalleryUITests/F3*`、`Tests/V15GallerymacOSUITests/F3*`、SnapshotTool scene、`archive/releases/v1.5.0/qa/frontend/f3-results.md` 及 `screenshots/f3/`。Source-manifest 变动须 `cd App && xcodegen generate`。

Feature 绝不可直接用 raw transport / `JSONValue`、旧 DTO/repository/Views/`FiscalDesign` 或任意正式 root；Foundation 请求不可让 Feature 自选 path/body。禁止 Backend、F4/F5、目录迁移、删除，及非本块 Feature。F3 不得修改或关闭 `F2C-MAC-UI-AUTOMATION`。

所有 read/preview/page 都有 generation；旧任务和 `CancellationError` 都不能写回新状态。cursor opaque，不跨 filter/window/revision 复用；读取下一页失败保留已读页并可 retry。离线只显示加密 snapshot 保存时间，禁所有写并说明。金额只经 `CNYAmountParser` 转 minor units；业务日/显示 Shanghai、API timestamp UTC。

任何输入（含非法文本）、selection、dismiss/cancel、expiry、409 或 refresh 都作废本地 preview。含 `preview_token` 的 commit 使用该 preview 绑定的同一 UUID `Idempotency-Key`；response-unknown 只以同 key 的 receipt/readback 定论，绝不换 key 盲重放。没有 token/key 的 direct command 不得假造 receipt；unknown 后 GET 核对意图，否则保留“结果未知，请刷新决定”。后端有 `available_actions` 时严格按 action/enabled/reason；无字段不伪造 capability。

每块 QA 要写 owned paths、fixture/gallery route、逐 endpoint request/response/error、token/key/receipt/readback、执行命令、截图、review findings 与限制。必须有 decode/model/race tests、真实 iOS 和 macOS XCUITest（SnapshotTool/模型不能替代）；收口运行 xcodegen、针对及全 FiscalKit tests、正式 iOS/macOS Release、Gallery iOS/macOS、SnapshotTool 实跑、`git diff --check` 和 clean-room/root 搜索。截图全部离线合成，禁止账户尾号、凭证、PDF 原文、provider text 或真实数据；逐块覆盖浅/深、AX3/AX5、Reduce Motion、长中文/Int64、loading/empty/error/retry/offline/conflict/unknown。

## 2. F3-A — 已知未来时间轴

独占 `Features/Timeline/**`。只读 `GET /api/v1/reports/future-events?window_days={7|30|60|90}&account_id?&cursor?&limit=1..100`。完整 decode `meta{timezone=Asia/Shanghai,currency=CNY,as_of,data_revision,schema_version}`、window/account、`credit_cycle/reimbursement_party/cash_flow_item` source、`inflow/outflow`、`exact_due/confirmed/expected/scheduled`、金额和 locator。不得客户端去重、算预测或混入 `/reports/facts`。

cursor 的 scope 绑定 window/account/revision；`future_events_scope_changed` 409 只显示 reload，重读首屏。iOS 是 7/30/60/90 segmented 时间线+账户筛选；macOS 是日期脊柱、检查器和键盘翻页。只接受 source-type 与 ID 一致的三种 `fiscal://` locator，打开本地只读 event inspector；未知、不匹配、自由 path/query 均零请求且不可执行。

Fixture/model/UI 覆盖四 certainty/三 source success、empty、long/Int64、next-page failure、409 reload、offline、unsafe link 和 refresh race；真实双端 UI 点击 window/filter/page/retry/reload/inspector/close。截图：`f3a-{ios,mac}-{light,dark,ax5}-{timeline,empty,page-error,conflict,offline}`。

## 3. F3-B1 — 信用账期

独占 `Features/Credit/**`。typed read：`GET /credit-accounts`、`/credit-accounts/{id}`、`/credit-accounts/{id}/cycles?cursor&limit=1..100`、`/credit-cycles/{id}`、`/credit-cycles/{id}/transactions?cursor&limit=1..100`。账期 preview：`POST /credit-accounts/{id}/schedule-change-preview` body `{expected_version,cycle_mode,statement_day,due_day}`；完整保留 old/new schedule、affected cycle current/expected version、checkpoint count、counts/conflicts/warnings、`available_actions`、token/expiry/revision。commit 是 `POST /credit-accounts/{id}/schedule-change` 同 body + `preview_token` + stable `Idempotency-Key`。

没有 enabled `commit`、token expiry、409 或任一输入变化均不可提交并显示原因。unknown 只以同 key readback/receipt 定论。iOS 是分步账期 sheet；macOS 是账期脊柱、cycle inspector、keyboard。Fixture/read/model/UI 覆盖 opening/overdue、keyset、preview→expiry/409→reload→success、unknown same-key 与离线。

## 4. F3-B2 — 分期五态与生命周期

独占 `Features/Installments/**`。typed `GET /installment-plans?account_id?&status?&cursor&limit=1..100`、`/{id}`、`GET /transactions/{id}/installment-eligibility`、`GET /installment-cycle-options?purchase_transaction_id&months=1..60`、`GET /installment-liabilities?account_id`。状态完整为 `active/completed/settled_early/partially_cancelled/cancelled`；未知 future state 仅 display-only。

覆盖 purchase `POST /installment-purchases/preview`→`POST /installment-purchases`，plan `POST /installment-plans`、`POST /{id}/preview`→`PUT /{id}`，以及 `settlement-preview`→`settle-early`、`reverse-settlement-preview`→`reverse-settlement`、`cancel-preview`→`cancel-future`。后三个 command 用 stable key并显示 `operation_id/replayed` 和返回系统流水；PUT plan 无 token/key，不能虚构，unknown 只能 GET 比对。完整显示 locked/future period、affected cycles、warnings，绝不自行重算债务。iOS 分步；macOS 脊柱/检查器。Fixtures/UI：eligibility reason、五态/long、preview input/cancel/409、三命令 success/unknown、keyset/offline。

## 5. F3-C — 报销（高风险）

> 当前状态：**Independent implementation review verified；`F3C-MAC-UI-AUTOMATION` deferred。** 四轮审查链为 `2×P1+2×P2 → 2×P2 → 1×P2 → 0 findings`；完整修复、真实 iOS 12/0、双端 Release/Gallery、15 张 macOS 图与残余 runtime blocker 证据见 `archive/releases/v1.5.0/qa/frontend/f3-results.md`。只解锁 F3-D Builder，F3-E 及以后仍锁定。

独占 `Features/Reimbursements/**`。typed claims/receipts list/get；`GET /reimbursement-expense-candidates?query&date_from&date_to&cursor&limit=1..100` 与 `GET /reimbursement-receipt-account-options`。candidate 必保留 `eligible`、`reasons[]`、`reason_details{code,message,field_path}`、nullable `category_id` 和金额分配。

新 claim 是直接原子 `POST /reimbursement-claims` draft + stable key。四种 preview/commit 必分型而不可混用：

- claim replace：`POST /reimbursement-claims/{id}/preview` → `PUT /reimbursement-claims/{id}`；
- cancel outstanding：`POST /reimbursement-claims/{id}/cancel-preview` → `POST /reimbursement-claims/{id}/cancel-outstanding`；
- create receipt：`POST /reimbursement-claims/{id}/receipt-preview` → `POST /reimbursement-claims/{id}/receipts`；
- replace receipt：`POST /reimbursement-receipts/{id}/preview` → `PUT /reimbursement-receipts/{id}`。

每个 commit 带对应 expected version(s)、preview token 与同 key，展示 server current/proposed/effects/warnings/receipt。submit/retract/reopen/void/restore/archive/unarchive、receipt void/restore 是 versioned direct command：readback 核对，绝不假称预览/receipt 或无依据重复。claim 六态固定 `draft/pending/partial_received/received/cancelled/partially_received_cancelled`。

灰按钮是硬门：iOS 与 macOS 都须真实 UI/E2E 测新建 claim 和“登记到账”，各自覆盖 candidates/receipt-account loading、empty、load-error+retry；所有标题/当事人/分摊/金额/日期/收款账户字段的 local/remote reason；invalid→valid；preview→409→重新 preview→success；特别验证 `category_id=null` 且 `eligible=true` 的 expense 可以选择并提交。任一 disabled 主按钮逐项显示缺项、field path 或服务原因，不能只有灰色；iOS sheet 内有本地错误 banner，macOS 在 inspector 显示。

Fixtures/screenshots 至少：`f3c-{ios,mac}-{claim-new,claim-reasons,receipt-loading,receipt-empty,receipt-retry,invalid-valid,preview,conflict,success,partial,offline,ax5}`。

## 6. F3-D — 现金流

> 当前状态：**Independent implementation review verified；`F3D-MAC-UI-AUTOMATION` deferred。** 审查链为 `1×P1+2×P2 → 0 findings`；Builder Backend 定向 PostgreSQL **19/0**、迁移/D5 事实金额、真实 iOS **4/0**、四个 Release、13 个 Swift Testing 声明的 BFT、10 张逐图检查的 macOS 证据与 clean-room 结果见 `archive/releases/v1.5.0/qa/frontend/f3-results.md`。第二 reviewer 未独立重跑 PostgreSQL，不能把终审 0 findings 记作第二次 PG runtime pass。当前只解锁 F3-E Builder，F3-F/G 仍锁定。

独占 `Features/CashFlow/**`。typed `GET /cash-flow-items?account_id?`、`/cash-flow-items/history?month?`、`/cash-flow-items/{id}`。create/settle 是 stable-key atomic；settle body 精确为 `{expected_version,actual_amount_minor,occurred_at,account_id,destination_account_id?,category_id?,title?,note?}`。update/cancel/confirm 与 `PUT /cash-flow-system-items/{system_kind}/{reference_id}` 为 expected-version direct response/readback。

只以 server item/history/status/action 为真，不从 future timeline 反推。system reimbursement/credit projection 仅按服务端允许字段处理；typed system update 发送 title/note/expected date/status，不发送金额。Backend 的兼容金额输入已 deprecated 且忽略，reimbursement active/history 金额始终由当前 outstanding fact 派生，credit projection 只读不变。iOS 分步、macOS 脊柱/检查器。Fixture/UI 覆盖 occurrence/this-and-future、transfer account constraint、confirm/settle/cancel、409/unknown/offline/history。

## 7. F3-E — 对账

> 当前状态：**F3-E Independent implementation review verified；`F3E-MAC-UI-AUTOMATION` deferred。** 审查链为 `1×P1+1×P2+1×P3 → 1×P2 → 0 findings`；稳定 visible-form fingerprint、captured typed request、moving-clock retry 与输入显式失效均已终审。最终真实 iOS **6/0**、14 个 model test 声明与 2 个 macOS UI test 声明的 BFT、四个 Release、SnapshotTool Release 实跑、14 张逐图检查的 macOS 图与 clean-room 证据见 `archive/releases/v1.5.0/qa/frontend/f3-results.md`。其后 F3-F/G 均已终审；现仅解锁 F4 Planner/Build planning，F5 与正式 root 仍锁定。

独占 `Features/Reconciliation/**`。typed `GET /reconciliation/checkpoints?account_id|credit_cycle_id`、`/checkpoints/{id}`、`/diagnosis?target_kind=account|credit_cycle&as_of&account_id?|credit_cycle_id?`、`/attention`。`POST /reconciliation/checkpoints` 是 direct atomic response，body `{target_kind, exactly-one account_id|credit_cycle_id, as_of UTC, actual_balance_minor, note?}`；unknown 只能 GET/list 核对，不重放。

Attention ignore 仅当当前 item 的 `available_actions` 包含 enabled `ignore`，才 `POST /reconciliation/attention/{source_type}/{source_id}/ignore` with real `expires_at`；否则显示 backend reason。iOS 核对分步表单；macOS account/cycle spine+difference inspector。Fixture/UI 覆盖 open/reconciled、missing/overdue/unknown attention、diagnosis/409/error/retry、disabled reason/offline。

## 8. F3-F — AI 提议

> 当前状态：**F3-A/B1/B2/C/D/E/F/G 均 Independent implementation review verified；九个 macOS runtime gates deferred。** F3-G 七审 0 findings 后暴露的跨阶段全 FiscalKit 回归已修复并经独立复审 0 findings；当前全量 **369/369 passed**。现仅解锁 F4 Planner/Build planning，F5 与正式 root 继续锁定。

独占 `Features/AI/**`。D3 是不变量：typed read settings 必显示/测试 `effective_auto_execute=false`；没有自动执行、strategy relaxation、provider secret 或财务聊天 UI。typed `POST /ai/proposals`（source/text + stable key）、list/get/quality-events、`PUT /ai/proposals/{id}` draft+expected version；人必须确认编辑内容后才 `POST /execute`。ignore/retry/undo 都带 precise expected version，undo 另有 expected transaction version。

显示 `processing/pending/executed/failed/ignored/undone`、field confidence/missing fields/reason/error 与人工编辑 diff。无 key unknown 只 GET proposal/结果后决定。iOS confirmation sheet；macOS proposal spine+inspector。Fixture/UI：missing/low confidence、edit→manual execute、failed→retry、ignore/undo/409/offline；绝不出现 automatic execute。

## 9. F3-G — 账单导入

> 当前状态：**F3-G Independent implementation review verified；`F3G-MAC-UI-AUTOMATION` deferred。** 七轮审查链 `4×P1+1×P2 → 1×P1+1×P2 → 2×P2 → 1×P1+1×P3 → 1×P1+1×P2 → 1×P1 → 0 findings` 已终审；完整 model **19/0**、固定 UDID iOS **8/0**、macOS UI BFT、四 Release 与 SnapshotTool/16 图证据保留。其后跨阶段全量回归已从 14 failed tests / 17 issues 修复为 **369/369 passed** 并经独立复审 0 findings。macOS UI runtime 仍为既有三次同根 automation-enable timeout，`F3G-MAC-UI-AUTOMATION` deferred；现仅解锁 F4 Planner/Build planning，F5 与正式 root 继续锁定。

独占 `Features/StatementImport/**`。macOS 全窗导入台，iOS 分步。`POST /statement-imports` 只发送 metadata `{document_sha256,byte_size,page_count,mime_type=application/pdf,display_name}`；PDF/image 绝不进入 API。typed chain：attempt/evidence（masked text+normalized boxes only）；provider-attempt（expected version、evidence SHA、用户确认 authorization）必须呈现 `execution_scope=request_bound`；validation-run/review/workbench/page/filter、row draft-resolution（batch/row/resolution 三 version）、final-create-draft、selected `confirmation-preview`。

confirmation preview 返回 server-derived `StatementImportConfirmRequest`；只以其 rows/versions `POST /confirm` with stable key，然后显示 full receipt（batch status、confirmed rows、per-row applied/skipped、created/matched/skipped counts、replay）。response-unknown 只以相同 key `GET /confirmation-receipt` 判断。完整呈现九 import state、五 resolution、checks/candidates/source-unavailable、partial row receipts；selected rows 原子，未知 source 不进金额相加。

PDFKit/local extraction 只在当前 UI task 保存临时源；离页、进入后台、取消或失败立即 cancel task，清内存、临时 PDF、page render 和尚未上传 evidence，不转后台/断线继续。Fixtures/UI：permission/file invalid、masked evidence、request-bound cancel、review/error/retry、unresolved、preview→409、partial receipt、cleanup。截图：`f3g-{ios,mac}-{intake,workbench,page,review,preview,partial,request-bound-cancel,offline,ax5}`。
