# v1.5.0 · F5 results

> Status: **v1.5.0 (24) source closeout complete; stopped before release-package generation.** Formal iOS/macOS roots use V15 production DI and the legacy visual layer is removed. The final source-only offline/error adjustment was intentionally not followed by another long test/build/review cycle, per the user's 2026-08-22 direction. No release package, signing, notarization, tag/push or production deployment was performed.

## User-directed final source closeout

- Production Today views now read `services.offlineSnapshotAt` dynamically, so a network fallback that occurs after model construction can become visible and read-only.
- QA-only RootSmoke no longer sets an artificial offline flag. Its source flow uses a real connected read to populate the encrypted snapshot, then relaunches the same isolated app/store against an unavailable loopback endpoint; a separate fresh isolated service covers the no-snapshot transport error.
- `App/project.yml` is `1.5.0 (24)`. This version bump is source metadata only and does not imply that an archive, signed artifact or deployed release exists.
- Prior Builder evidence below remains historical evidence. The final small source edits above were not re-run through the long matrix and are not represented as a new pass.

## F5-B builder evidence

- **Rollback/source boundary.** v1.4 released source identity is `ad956cda9e66692733a005d00983c4fd4a6ffc28` (historic `ad956cd`); no reset, checkout, database downgrade or user directory-migration change was made. The rollback path remains selecting that released App revision/build after authorization, with additive API handled forward.
- **Atomic formal roots.** `IOSRootView` and `MacRootView` are now thin `V15IOSLiveAppShell`/`V15MacLiveAppShell` roots. `FiscaliOSApp` and `FiscalmacOSApp` construct `V15Services` with the production `APIConfiguration`, platform `AccessKeyStore` and `DataRevisionStore`; formal roots contain no Gallery/fixture, old View, DTO/repository, `FiscalDesign`, Web, `URLSession` or `URLRequest` reference. Project target exclusions keep `V15Gallery` and `V15RootSmoke` out of formal products.
- **QA-only root proof.** `V15RootSmokeiOS` and `V15RootSmokemacOS` compile those same root files and V15 production-DI code paths, but use `http://127.0.0.1:8000`, bundles `com.linotsai.fiscal.v15-root-smoke.ios` / `.macos`, isolated stores and a unique `com.linotsai.fiscal.v15-root-smoke.*` Keychain service. The app, never the runner, writes its one-shot QA key through real `AccessKeyStore`; cleanup launches the same QA app to delete it and then verifies a fresh Bootstrap. Resolved QA signing is simulator/local `Sign to Run Locally` (`CODE_SIGN_IDENTITY=-`), not Developer/Distribution. The disposable loopback Backend was PID `65323`, database `fiscal_f5b_root_smoke`; no Backend source or production endpoint/keychain was touched.
- **Actual RootSmoke runtime.** Post-switch and post-delete results: macOS `V15RootSmokemacOSUITests` **2/0** (`/private/tmp/f5b-rootsmoke-macos-post-cleanup.xcresult`), iOS `V15RootSmokeUITests` **2/0** (`/private/tmp/f5b-rootsmoke-ios-post-cleanup.xcresult`). Each executes cold Bootstrap, local authorized connection, real Today→Ledger read navigation, error/offline route coverage, app-owned key cleanup and a fresh keyless Bootstrap; the test also asserts no Gallery route.
- **Deletion manifest and retained boundary.** Removed only the old formal visual roots and visual layer: legacy UI suite and SnapshotTool, `FiscalDesign`/`MoneyFormat`, and `Features/{AIProposalScreens,CashFlowScreens,CreditScreens,IOSStatementImportScreen,InstallmentScreens,MacStatementImportIntake,MacStatementImportWorkspaceHost,MacTransactionWorkbench,MasterDataScreens,PlaceholderScreen,ReconciliationScreens,ReimbursementScreens,ReportingScreens,SettingsScreens,SharedOverviewComponents,TransactionScreens}.swift`, plus obsolete project/scheme references and four legacy visual-helper unit assertions. Retained `APITransport`, `AccessKeyStore`, `OfflineSnapshotStore`, `DataRevisionStore`, auth, CNY/Shanghai helpers, typed DTO/repository/model boundaries and user-directory migration. A legacy `CreditDashboardTotals` test lost its deleted screen-only helper and was removed rather than reintroducing old visual code.
- **Focused repair.** Real loopback bootstrap exposed fractional ISO-8601 parsing in `APITransport`; its decoder now accepts fractional/nonfractional UTC `Z` and offset timestamps. Focused `P16InfrastructureTests` passed **7/0**, including the regression. This is transport decoding only, not a Backend change.
- **Post-delete validation.** `xcodegen generate`; `FiscalKitTests` **385 tests / 40 suites**; full Gallery macOS **27/0** (`/private/tmp/f5b-gallery-macos-post-cleanup.xcresult`) and iOS **89/0** (`/private/tmp/f5b-gallery-ios-post-cleanup.xcresult`, 2848.227 s). `git diff --check` passed. Formal iOS and macOS Release build-only both passed with explicit `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY='' DEBUG_INFORMATION_FORMAT=dwarf`; dwarf is an invocation-only disk-space safeguard after the default dSYM step exhausted the local disk, not a production setting change. Neither formal product was installed, launched, archived or signed.
- **Final visual matrix.** `V15GallerySnapshotTool` Release no-sign build passed, then rendered **155** PNGs to `/private/tmp/f5b-final-visual-matrix-r1` with only its matching Release framework via `DYLD_FRAMEWORK_PATH`. Structural checks found 0 zero-byte and 0 sub-1000-byte files, 150 unique SHA-256 values (five expected fixture duplicates); the F4C AX5 image is 2360×1960. Gallery/fixture rendering is visual evidence only and never substitutes for the real RootSmoke runtimes above.
- **Environment note.** Four stale, reproducible pre-F5-B xcresult directories were precisely deleted only after their final-pass QA summaries had replaced them, releasing about 4.5 GiB; no repo path, current F5-B result bundle or user path was removed. Two obsolete watchers for a nonexistent old RootSmoke command were precisely terminated. The remaining loopback process is test-only and must be stopped/its disposable database removed after review if no rerun needs it.

**Stop point: immediately before release-package generation.** A future authorized turn may generate/sign packages and perform release operations; this closeout does not do so.

## Environment and source boundary

- Current source remained dirty/commit-free, as required; no formal root, legacy view, `App/project.yml`, Backend migration, signing configuration or user directory-migration file was edited. F5-A edits are limited to the Gallery-only foreground activation/launcher, Gallery fixture and UI-test determinism repairs, the dated P17 test fixture, the P31 archive dry-run assertion, and mechanical Ruff formatting of three previously shared Backend files.
- macOS `26.6.2 (25G83)`; Xcode `26.6 (17F113)`; destination was `My Mac` (`arm64`, id `00008132-0001242821FA401C`). The fixed iOS simulator `211DD03C-812D-4A42-97EF-F693D7DF924C` was booted but unused by this blocked macOS gate.
- Preflight found no residual `xcodebuild`, `xctest`, V15 Gallery or Gallery test-runner process. A pre-existing `/Applications/Fiscal.app` process was not a Gallery app and was left untouched. `testmanagerd` was a normal running system LaunchAgent; it was inspected only, never killed/restarted. Disk free space was 13 GiB.
- `cd App && xcodegen generate` completed. Fresh BFT used `/private/tmp/fiscal-f5a-dd.QkUhHF`, result `/private/tmp/fiscal-f5a-macos-bft.xcresult`: **TEST BUILD SUCCEEDED**. After safe Gallery-harness recovery, two further fresh BFTs also passed: `/private/tmp/fiscal-f5a-retry1-bft.xcresult` and `/private/tmp/fiscal-f5a-retry2-bft.xcresult`. Every resulting Gallery runner used Xcode's local `Sign to Run Locally` (`CODE_SIGN_IDENTITY = -`); no Developer or Distribution identity was used.
- The console user and current uid were both `linotsai` / `501`. The prior native `ioreg` evidence had shown an on-console but locked Aqua session, explaining the pre-AX `Running Background` failures. No credential/security bypass, daemon kill/restart, host restart or Automation permission change was attempted. After the user physically unlocked the desktop, native evidence no longer reported the locked field and serial Gallery runtime proceeded into business assertions. No Gallery/XCTest process remained after each run.

## macOS runtime recovery and exact gates

Command:

```sh
xcodebuild -project App/Fiscal.xcodeproj -scheme V15GallerymacOS -configuration Debug \
  -destination 'platform=macOS' -parallel-testing-enabled NO \
  -derivedDataPath /private/tmp/fiscal-f5a-dd.QkUhHF \
  -resultBundlePath /private/tmp/fiscal-f5a-f2c.xcresult test-without-building \
  -only-testing:V15GallerymacOSUITests/F2CMacGalleryUITests
```

The first diagnostic F2C attempts and F4-A control had genuinely entered the selected classes but failed before the first AX query with `Running Background`; the locked Aqua console was the corroborating external condition. Safe QA-only recovery retained a regular Gallery activation policy and one shared `launchGalleryMac` sequence (terminate existing test app → launch → wait/activate foreground). It changes neither formal root nor production target, entitlement, signing configuration or Backend.

After the physical unlock, a fresh Debug BFT used `/private/tmp/fiscal-f5a-resume-dd.N9sDKx`. Every exact class below was then executed serially by `test-without-building` on `platform=macOS`, with a nonzero test count and zero failures. The `F3E`/`F3F` initial business failures were real AX defects, not an environment retry: stable root/pane containers now use `children: .contain`; `V15PreviewState` now exposes its actual interactive children rather than combining recovery controls. F3B2/F3C/F3D scenario tests likewise assert the fixture's already-open terminal/recovery state instead of fictional entry controls.

| Gate | exact result bundle | result |
| --- | --- | --- |
| F2C | `/private/tmp/fiscal-f5a-resume-f2c-final.xcresult` | **3/0** |
| F3A | `/private/tmp/fiscal-f5a-resume-f3a-ax-final.xcresult` | **1/0** |
| F3B1 | `/private/tmp/fiscal-f5a-resume-f3b1-final.xcresult` | **2/0** |
| F3B2 | `/private/tmp/fiscal-f5a-resume-f3b2-pass.xcresult` | **2/0** |
| F3C | `/private/tmp/fiscal-f5a-resume-f3c-final.xcresult` | **3/0** |
| F3D | `/private/tmp/fiscal-f5a-resume-f3d-pass2.xcresult` | **2/0** |
| F3E | `/private/tmp/fiscal-f5a-resume-f3e-final-pass.xcresult` | **2/0** |
| F3F | `/private/tmp/fiscal-f5a-resume-f3f-final2.xcresult` | **4/0** |
| F3G | `/private/tmp/fiscal-f5a-resume-f3g-initial.xcresult` | **1/0** |
| F4C | `/private/tmp/fiscal-f5a-resume-f4c-initial.xcresult` | **3/0** |

Fresh BFT evidence for the last source changes is `/private/tmp/fiscal-f5a-resume-f3f-preview-actions-bft.xcresult`; it succeeded before F3F/F3G/F4C. The current-source full macOS Gallery runtime subsequently passed **27/0**: `/private/tmp/fiscal-f5a-resume-mac-gallery-full.xcresult`. The transient `DebuggerLLDB...StoreError` log was nonfatal; every result above records launched business runtime rather than BFT substitution.

## Pre-root Apple verification

- Full `FiscalKitTests` passed: **389 tests / 40 suites, 0 failures** (`/private/tmp/fiscal-f5a-fiscalkit-full.xcresult`).
- The first fixed-simulator iOS Gallery full run reported **89 executed, 12 assertion failures**: F1-A derived a Gallery business date from the host clock; F3-B2 did likewise; F3-C's UI helper could lose selection after a second tap. These are test/fixture harness defects, not production behavior. The smallest repairs derive the visible date from the fixed Gallery fixture, inject that date only into Gallery construction, and conditionally focus/clear fields. Focused regressions passed F1-A **6/0**, F3-B2 **6/0**, and F3-C **12/0**.
- The first fresh iOS rerun reached **88 passed, 1 failed** in F3-A. `F3ATransport` used one `reads` counter for both concurrent account-option and future-event requests, so the account read could consume the intended first future-event conflict. The fixture now counts only `reports/future-events`; F3-A focused race coverage passed **3/0**. The second fully fresh fixed-simulator run passed **89/0** (`/private/tmp/fiscal-f5a-gallery-ios-full-rerun2.xcresult`, 2852.4 s), including `testReduceMotionAndTransparencyFlagsExposeStableRenderingMode`. This is simulator evidence only, not a closure of the real-device Reduce Motion or VoiceOver human gates.
- Four Release build-only commands passed, each with `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=''`: `FiscaliOS` (generic iOS Simulator), `FiscalmacOS` (macOS), `V15GalleryiOS` (generic iOS Simulator), and `V15GallerymacOS` (macOS). The shared V15 accessibility change was recompiled by the current run; its result bundles are `/private/tmp/fiscal-f5a-resume-fiscalios-release-unsigned.xcresult`, `/private/tmp/fiscal-f5a-resume-fiscalmacos-release-unsigned-final.xcresult`, `/private/tmp/fiscal-f5a-resume-gallery-ios-release-unsigned.xcresult`, and `/private/tmp/fiscal-f5a-resume-gallery-macos-release-unsigned.xcresult`. The two formal targets were not launched, installed, tested, archived or signed. Resolved formal Release settings before overrides were: FiscaliOS `com.linotsai.fiscal` / `https://fiscal.linotsai.top` / iPhone Developer; FiscalmacOS `com.linotsai.fiscal.mac` / `https://fiscal.linotsai.top` / Developer ID Application. The command-line override was recorded by xcodebuild as empty identity and signing allowed/required `NO`; no Developer or Distribution identity was used for the actual builds. Gallery bundle IDs are `com.linotsai.fiscal.v15-gallery.ios` and `.macos`; its shell uses in-process synthetic fixtures, not a configured API base URL.
- `V15GallerySnapshotTool` Release unsigned build passed. Its full current matrix rendered **155** synthetic macOS PNGs to `/private/tmp/fiscal-f5a-visual-matrix` after supplying only the matching Release framework through `DYLD_FRAMEWORK_PATH`; direct tool execution first failed because its rpath did not locate `FiscalKit.framework`. This is recorded as a resolved local rpath invocation requirement, not a source or signing change.
- Structural matrix QA found **155 PNGs**, **0** zero-byte/undecodable/undersized/solid-color files, 150 unique SHA-256 values and 5 expected fixture duplicates (shared initial route renderings). Sizes cover 1640×1800 through 2880×2160. Generated and viewed contact sheets by block: F0 **23**, F1 **11**, F2 **12**, F3 **92**, F4 **17**. They cover light/dark, compact/comfortable, AX5, loading/empty/error/offline/conflict/unknown and long-content states without visible clipping. Direct high-risk views: F3-C AX5 reimbursement long content and F3-G response-unknown show readable recovery/read-only affordances; F4-B AX5 export-offline preserves disabled controls and retry, while F4-C error and offline show re-entry/close plus disabled operator-only restore with no payload/password bytes. This synthetic tool is visual evidence only, not macOS UI runtime evidence.
- Fresh fixed-simulator Reduce Motion/Reduce Transparency method passed **1/0** (`/private/tmp/fiscal-f5a-ios-reduce-motion.xcresult`, `testReduceMotionAndTransparencyFlagsExposeStableRenderingMode`); it actually launched the local iOS Gallery and found the stable rendering-mode surface. It does not substitute for real-device or macOS manual Reduce Motion verification.

## Backend, migration and Archive verification

- Created disposable local PostgreSQL `fiscal_f5a_pg_20260821` and `fiscal_f5a_pg_verify_20260821`; each migrated zero-to-head revision `0035`. The first also completed `downgrade -1` then `upgrade head`; the second was a separate fresh full-test database.
- First PostgreSQL full run exposed one stale P17 test: its July 2026 fixture invoked `CreditService` with the host date 2026-08-21, correctly receiving `overdue_credit_cycle_locked`. The product was not changed; the schedule-rebind test now injects 2026-07-16, inside its business fixture cycle. Focused P17 passed, then the second fresh full PostgreSQL run passed **391/391** (84.95 s). No-PostgreSQL run passed **148**, skipped **243** (3.46 s); skips are reported as skips, not passes.
- Backend static closure: `ruff format --check` **243 files already formatted**, `ruff check .` passed, and `pyright` reported **0 errors, 0 warnings**. The initially reported format candidates (`api/routes/archive.py`, `services/cash_flow.py`, `test_p13_cash_flow_postgres.py`) were existing shared-stage edits; only project Ruff's mechanical formatting was applied, then P13 + P22 passed **18/18**. The explicit F5 Archive/revision/report/retirement group P22/P31/P33/P34/P35 passed **25/25**.
- Two independent executions of P31 each created new UUID-named isolated source and empty target PostgreSQL databases, migrated both, encrypted export, opened the artifact, ran `ArchiveService.dry_run_report` with `relationship_errors == 0`, restored only into the fresh empty target, and compared merchant identifiers/mappings/receipts, data revision, account-balance fingerprint and transaction fingerprint. Both passed; each test's `finally` dropped its exact source and target databases. This F5 assertion adds the dry-run check that the pre-existing P31 roundtrip lacked. P33/P34 coverage additionally passed in the explicit 25-test group, including encrypted archive restore and report/financial readback invariants.
- Cleanup: after all Backend/Archive assertions, precisely dropped only `fiscal_f5a_pg_20260821` and `fiscal_f5a_pg_verify_20260821`; queried PostgreSQL for those names and `fiscal_p31_archive_%`, with **0** remaining databases. The visual matrix and five contact sheets remain in `/private/tmp` as reviewable non-secret QA evidence; no archive artifact was retained.

## Remaining human gates and next action

All safely executable pre-root Apple, Backend/fresh-PostgreSQL, Archive/financial-invariant, visual, runtime and unsigned Release audit evidence is complete. SnapshotTool, iOS/model tests, historical evidence and BFT remain explicitly not substitutes for the ten macOS runtime results documented above. VoiceOver with real iOS/macOS accessibility paths, real-device Reduce Motion, and physical-device validation were not performed and remain open human release gates.

## Independent review closure

Review chain: **`1×P3 → 1×P3 → 0 findings`**. The first P3 corrected stale unlock/open-gate wording; the second P3 synchronized historical F2C/F3*/F4C deferred/blocker wording. Both remediations were documentation-only and ran no tests.

| Evidence owner | Verified evidence |
| --- | --- |
| Builder | Ten exact macOS runtime gate classes **23/0**; current-source full macOS Gallery **27/0**; documented pre-root nonmac evidence: FiscalKit, fixed iOS Gallery, Backend/PostgreSQL/Archive invariants, visual matrix, and unsigned Release build-only. |
| Independent reviewer | Independently ran full macOS Gallery **27/0** and verified the recorded pre-root nonmac evidence against this QA record. |

**Next action: F5-B Builder formal root atomic switch only.** F5-C unsigned audit draft, publish, deploy, signing, tag and push remain locked. VoiceOver, physical-device validation and real-device Reduce Motion remain unclosed F5-C/candidate/publish human gates; they do not block F5-B.
