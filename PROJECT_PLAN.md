# Fiscal · PROJECT_PLAN

> 当前目标：**v1.8.0（Build 36）macOS 全页面前端升级** ｜更新：2026-09-02（Asia/Shanghai）｜阶段：**RELEASING · 用户已授权一条龙发布**

## 1. 权威范围与当前事实

- 用户已通过 [macOS 前端升级合同](MACOS_FRONTEND_UPGRADE_CONTRACT.md)。该合同定义全部验收范围；本计划定义唯一的施工顺序、边界与当前状态。任何冲突以本计划的现行结论为准。
- v1.7.1（Build 35）已发布，源码标签 `v1.7.1` 不移动；本轮**不发布、不换包、不部署**，直到全部 P 完成、复审通过并由用户另行授权。
- 当前仅升级 macOS；保留米白、深绿、金色的品牌方向、现有业务能力与数据语义，不新增业务模块。
- 不改 Backend API、数据库或 migration；若实施证明某个既有 macOS 能力确实缺少后端契约，停止该项并另报用户决定，不能顺带扩展后端。
- 共享 SwiftUI 的修改须限于 macOS 分支或证明 iOS 行为、布局未变；每一批都要编译 iOS Simulator。截图记账、快捷指令和所有 iOS 信息架构不改。
- 既有六个 Xcode scheme 本地变更受保护：`FiscaliOS.xcscheme`、`FiscalmacOS.xcscheme`、`V15GalleryiOS.xcscheme`、`V15GallerymacOS.xcscheme`、`V15RootSmokeiOS.xcscheme`、`V15RootSmokemacOS.xcscheme`；不得覆盖、还原或提交。

## 2. 不可跳过的阻断项

以下任一项未通过，不能进入最终复审或发布：

1. **设置可达性**：主数据的账户、分类、商户三个入口都可见、可进入，列表、详情及新建路径同步正确。
2. **报销金额语义**：总额、已到账、未到账在列表、详情、回执和报表关联处始终有标签，不以无说明的 `¥0.00` 代替未到账金额。
3. **表单时机**：新表单初始状态不报错；字段交互或提交后才显示邻近错误；相同禁用原因只显示一次。
4. **AI 分层**：待确认任务与历史记录明确分开，待确认优先，原始 OCR 不淹没处理动作。
5. **报表范围**：月报、年报、上一期、下一期和当前范围清楚可见；最大化的下钻表不再锁在窄列。
6. **现代桌面一致性**：1000×680 最小支持窗口与 1440×900 宽窗口都不出现压缩文字或无意义大片空白；按钮、表单、状态、详情面板和焦点反馈遵循同一规则。

## 3. 固定技术与验收规则

- 先扩展 `V15DesignTokens`、`V15Controls`、`V15StateViews` 与 `V151MacWorkspace` 的 macOS 专用能力，再迁移页面；不得在各功能页复制一套视觉规则。
- 保留现有账务写入边界：金额继续走 `CNYAmountParser`（元、两位小数），业务日期继续用 `Asia/Shanghai`；交易类型、账户类型、预览失效、generation 竞态守卫和错误链路不可因改界面而退化。
- 所有异步页面共用加载、空白、失败、离线、冲突、未知结果、禁用、成功回执和危险确认表达；动画短且可关闭，状态不可只靠颜色理解。
- 每个 P 都补齐对应的 fixture/Gallery、可访问名称与 macOS UI 自动化；视觉门禁为浅色常用窗口 **1000×680**、浅色宽窗口 **1440×900**，并覆盖深色和关键错误/空白/禁用状态。
- 改 SwiftUI View 的每批收口至少执行对应测试、`FiscalKitTests`、`FiscalmacOS` build 与 `FiscaliOS` generic iOS Simulator build；若改 `App/project.yml` 或增删源文件，先按 xcodegen 规则生成且不得触碰受保护 scheme。

## 4. P 阶段计划

### P1 · macOS 工作台底座、响应式空间与统一控件

- **目标**：先把全 App 共用的桌面骨架改成现代财务工作台，使后续页面在同一套空间、文字、颜色、按钮、表单与状态规则上施工。
- **范围**：五个主导航、一级/二级工具栏、内容区与三栏宽度策略；macOS 专用字体与金额层级、分区/卡片、四类按钮、分段筛选、字段、详情空态、危险确认、焦点/工具提示、加载/空白/失败/离线/冲突/未知结果/回执。
- **必须修复**：宽窗口新增空间必须用于多列摘要、主列表或详情，单列安全页可用分区/双列，不能把主体固定在窄列；主要操作每区最多一个，普通导航不得再做整行强调按钮；初始表单的校验组件必须支持“未交互不报错”。
- **不做**：不在本 P 改任何功能页的业务流程、接口或写入语义；不以统一组件之名改变 iOS 外观。
- **交付与门禁**：macOS 设计系统、Shell、Gallery 基线及自动化断言可复用；五个根入口在两种窗口尺寸可读、可操作，Tab 焦点和中文可访问名称完整；通过 P1 Gallery 截图与 macOS/iOS 编译。

### P2 · 流水、账户与快速记账

- **目标**：把最高频的流水工作区做成清晰、快速、适合键盘操作的桌面记账面板。
- **范围**：流水工具栏、账户余额板、流水列表、多选、搜索、账户/账目右侧详情、无选择态、记一笔编辑器及返回路径。
- **必须修复**：账户卡按宽度换列且资产余额、信用欠款语义分开；流水三层信息和各交易方向/作废/多选/焦点状态清楚；搜索自动聚焦、Enter 执行、Esc 收起；新账目金额优先、类型驱动字段、Tab/Enter/Esc 顺畅，空表单不预警，保存只在有效改动后可用。
- **不做**：不重算余额、不改账户/交易 API、不改变现有金额解析、交易校验或批量写入语义。
- **交付与门禁**：账户筛选、账目详情、月份/搜索切换与多选互不串状态；以 fixture 覆盖长账户名、长金额、无账户、加载/失败、空流水、编辑初始/错误/可提交状态；执行真实只读走查及一条受控写入链路验收。

### P3 · 已知未来、现金流计划、信用账期、分期与报销

- **目标**：将未来事项家族收敛为一致的三栏工作台，让时间、资金方向、来源、状态与下一步动作一眼可读。
- **范围**：已知未来二级导航和 7/30/60/90 天筛选；现金流计划；信用账期；分期；报销的列表、摘要、详情、创建/修改/预览/确认/撤销等现有操作界面。
- **必须修复**：四个二级入口统一为清晰导航而非等宽长按钮；系统派生与可编辑项、开放/结清/逾期、锁定/未来/取消期次、普通/危险操作分开；分期预览与表单不重复报错；报销完整显示总额、已到账、未到账和到账历史，候选账目可搜索并不挤满右栏。
- **不做**：不新增提醒、待同步、附件、设备管理或新的现金流业务规则；不改变报销分摊、到账、信用账期、分期和未来事项的后端语义。
- **交付与门禁**：每页在无选择、已选择、编辑中、只读/不可用和宽窗口下均有 Gallery 场景；预览随输入失效、异步请求带 generation 守卫、错误在当前编辑界面可见；完成真实 macOS 只读走查及现有写入流程的金额方向验收。

### P4 · 报表与下钻

- **目标**：把“数字和长列表”升级为可读的财务分析面板，同时不改变报表数据和统计口径。
- **范围**：报表总览、支出、现金流、债务、四页分段导航、期间工具栏、导出菜单与报表下钻。
- **必须修复**：月报/年报成为独立明确的范围选择；上一期、下一期、当前范围、刷新、导出位置稳定；支出分类和日趋势、现金流趋势与账户比较、债务到期与分期安排采用现有数据可支撑的图形/结构表达；下钻完整利用宽窗口且返回时保留期间与口径。
- **不做**：不引入新统计口径、不重写报表服务或 CSV/PDF 数据契约、不将已知未来与本期实际现金流混淆。
- **交付与门禁**：四镜头及下钻覆盖有数据、零值、空数据、加载失败、下一页失败、离线快照；关键分类/指标仍可进入同一明细；金额、内部转账、债务与报销的既有语义测试通过，宽窗口视觉审计通过。

### P5 · AI 待确认与 PDF 账单导入

- **目标**：让待处理任务成为中心，证据、AI 建议、用户修正和处理结果各归其位。
- **范围**：AI 待确认/历史筛选、提案详情、创建/修改/确认/忽略/撤销/重新读取；PDF 导入的文件、证据、识别行、处置、批量选择、预览和确认界面。
- **必须修复**：待确认与历史分层且待确认优先；OCR 原文折叠为证据区，低置信字段重点提示，AI 值和最终入账值可对照；导入空态提供 PDF 选择/拖放和六阶段进度，单行与批量动作不混用，所有可见文案和可访问名为中文。
- **不做**：不增加照片、截图或 OCR 入口，不改变 iOS 快捷指令十分钟截图读取方式，不让 AI 自动记账。
- **交付与门禁**：AI 的待确认为零/非零、历史、低置信、失败与撤销；导入的空白、解析、缺失、冲突、部分成功、未知结果、离线与分页失败都有可恢复场景；确认写入仍保留用户确认与回执链路。

### P6 · 系统与数据、设置与主数据

- **目标**：把安全与配置页面从长表单改为清晰、可靠、可到达的桌面管理界面。
- **范围**：系统运行状态、数据归档、恢复边界、个人口令；主数据的账户、分类、商户；归档区；AI 与识别设置。
- **必须修复**：账户/分类/商户的可见切换入口和新建/编辑/详情完整可达；分类合并/拆分先预览影响，商户搜索使旧分页失效；归档按类型紧凑分组、恢复为次要操作；系统页四区分开，空口令不报错，恢复限制不以巨大禁用按钮占位；AI 设置呈现完整状态与“必须人工确认”边界。
- **不做**：不改变归档/恢复、密码、分类合并拆分、商户分页或 AI 配置的授权和数据规则；不把系统与数据重新塞回 iOS 设置。
- **交付与门禁**：阻断项 1 和 3 必须逐项通过；设置/系统在两种窗口尺寸无窄列空白，账户、分类、商户、归档、安全、AI 设置各有空白/错误/只读/危险确认 Gallery 与 UI 验收；写入操作的确认对象、风险和回执准确。

## 5. Build、Review 与当前状态

- Build 只按 **P1 → P2 → P3 → P4 → P5 → P6** 分段；P 只表示施工阶段，不包含 Review、修复 Review 问题、独立复审或发布工作流。
- 合同施工章节映射：P1 覆盖 §4、§5、§14、§15；P2 覆盖 §6、§7；P3 覆盖 §8；P4 覆盖 §9；P5 覆盖 §10、§11；P6 覆盖 §12、§13。
- Build 完成后进入独立 **Review 工作流**：Reviewer 依据合同 §16 和本计划阻断项执行代码审查、全页面视觉审查、操作路径审查、窗口尺寸与浅/深色矩阵审查，并输出按严重度排序、可定位的 findings。
- Review 发现问题后进入单独的修复回合；修复完成后重新执行独立复审。Review 与复审结果只更新当前状态和验收证据，不新增 P 编号。
- 独立 Review 和修复闭环完成、用户确认视觉后，才允许用户另行决定是否进入发布工作流；Review 不包含提交、打标签、部署、换包或发布授权。
- 本轮明确不做：iOS 视觉重做、Backend/migration、发布链，以及待同步原地编辑、提醒、归档恢复新能力、附件、设备管理等后续 Backlog。
- 当前状态：P1–P6 已完成；工作台底座、流水/账户、未来事项家族、报表、AI/PDF 导入及系统/主数据均已按合同完成 macOS 升级。工作区的六个受保护 scheme 变更与用户创建的合同文档仍保留且未触碰。
- 独立 Review 的 6 项 findings 已全部修复：AI 切换待确认/历史时只允许操作当前可见提案；报表年报可切回月报；Today 刷新不再擅自打开账户范围且详情关闭保留当前范围；macOS Gallery 的窗口、sheet 与安全输入自动化恢复稳定；年报 fixture 改为完整自然年；支出趋势统一使用金色支出语义。
- 修复验收：`FiscalKitTests` 394 tests / 39 suites 通过；`V15GallerymacOSUITests` 30/30 通过；`FiscalmacOS` 与 `FiscaliOS` generic Simulator 构建通过；`git diff --check` 通过。另以真实 macOS 交互核对 Today、报表期间切换及归档确认弹层。
- 独立复审确认上一轮 6 项 findings 均已闭环，但新发现 4 项：正式主壳在 1000×680 时给多个三栏模块的可用宽度不足；主数据空白新建表单仍允许点击保存；账单导入未选行时重复显示同一禁用原因；错误、危险和品牌强调仍共用过近的青色/金色视觉。
- 独立复审证据：`V15GallerySnapshotTool` 生成 146 张 macOS 截图并完成视觉抽检；`V15GallerymacOSUITests` 30/30 通过；针对性 12 项共享测试、`FiscalmacOS` 与 `FiscaliOS` generic Simulator 构建及 `git diff --check` 均通过。复审只读，未修改施工文件或六个受保护 scheme。
- 第二轮 4 项 findings 已修复：1000×680 窄窗口使用 76px 图标导航轨，AI、账单导入、报销与分期的三栏最小宽度改为可验证的共享契约；主数据保存只在草稿完整且发生变化时启用；账单导入的同一禁用原因只显示一次；macOS 错误卡片、字段错误与危险操作改用独立红色语义，不再借用品牌青色或提醒金色，共享组件以平台分支保持 iOS 原外观不变。
- 真实视觉走查额外发现并修复：空白新建主数据时“归档账户/分类”原为可点击但无作用的假操作，现改为禁用并显示“请先选择现有记录”。
- 第二轮修复验收：完整 `FiscalKitTests` 396/396（39 suites）通过；`V15GallerymacOSUITests` 30/30 通过；`FiscalmacOS` Debug/Release 与 `FiscaliOS` generic Simulator 构建通过；`git diff --check` 通过。另以本轮 Release 构建直接连接只读生产数据，在最小窗口目视核对流水、未来时间线、分期、报销及设置主数据，并用 Gallery 错误场景核对红色错误卡片；未替换或关闭 `/Applications/Fiscal.app`。
- 第二次独立复审确认上一轮窗口响应式、保存门禁、错误/危险色和既有 6 项 findings 没有回归，但新发现 5 项 P2：数据安全空密码初始报错且原因重复；报销新建表单初始报错；账单导入未选行原因仍由两个按钮持有；流水作废与主数据归档等危险操作仍有普通按钮样式；主数据字段错误可能跨对象或成功保存后残留。
- 第二次独立复审证据：`git diff --check`、完整 `FiscalKitTests` 396/396、`V15GallerymacOSUITests` 30/30、`FiscalmacOS` Debug/Release 与 `FiscaliOS` generic Simulator 构建均通过；Reviewer 抽查当前代码生成的 117 张 macOS Gallery 截图及 19 张 F4 截图。复审只读，未修改文件、提交、发布或触碰六个受保护 scheme。
- 第二次独立复审的 5 项 P2 已修复：数据安全与个人口令初始空表单保持安静，交互后才显示邻近红色错误且按钮只保留可访问提示；新建报销单按已触碰字段显示错误；账单导入只在右侧操作区显示一份空选择原因；流水作废、账户/分类归档及恢复操作使用明确的危险/次要样式；主数据在切换类型、切换对象、新建及成功保存时收敛字段错误、冲突和回执归属。
- 本轮额外收口：禁用按钮的可访问说明继续保留，但可见原因拥有独立稳定标识，自动化可以准确区分“屏幕上显示的原因”和“辅助功能提示”；归档密码与个人口令的字段错误统一使用红色错误语义和警示图标。
- 本轮修复验收：完整 `FiscalKitTests` 397/397（39 suites）通过；`V15GallerymacOSUITests` 32/32 通过；`FiscalmacOS` Release 与 `FiscaliOS` generic iOS Simulator Release 构建通过；`git diff --check` 通过。另以当前 Debug Gallery 实际操作并目视核对数据安全初始/错误态、新建报销单、账单导入空选择、账户归档和流水作废；仅使用临时 Gallery，未操作 `/Applications/Fiscal.app`。
- 第三次独立复审确认上一轮 5 项中，初始空口令噪声、新建报销单初始错误、账单导入重复禁用原因、危险操作样式及主数据对象生命周期均已基本闭环；新发现 2 项 P2：数据安全失败、报销保存失败及信用账期字段错误仍有部分使用黄色或品牌青色，而非确定性错误应使用的红色语义；主数据服务端字段错误虽不再跨对象残留，但同一错误仍会被原样传给表单内多个不相关字段。
- 第三次独立复审证据：`git diff --check`、完整 `FiscalKitTests` 397/397（39 suites）、`V15GallerymacOSUITests` 32/32、`FiscalmacOS` Release 与 `FiscaliOS` generic iOS Simulator Release 构建均通过；`V15GallerySnapshotTool` 生成 146 张临时截图，Reviewer 抽查浅色/深色、约 1180×820、1240×760 与 1440×900 的关键页面。复审只读，未修改文件、未操作生产环境、未发布、未换包或触碰六个受保护 scheme。
- 第三次独立复审的 2 项 P2 已修复：数据安全、个人口令、报销保存与信用账期的确定性失败统一使用红色错误语义，结果未知继续使用黄色提醒语义，普通禁用说明保持中性；主数据服务端字段错误按账户、分类、商户的准确字段路径分发，聚合错误和未知字段只在表单级显示一次，不再复制到不相关字段。
- 本轮补充主数据字段分发单测，并实际操作 Gallery 核对数据安全“失败/未知”、信用账期字段错误和主数据编辑器。验收结果：针对性测试 89/89（4 suites）、完整 `FiscalKitTests` 398/398（39 suites）、`V15GallerymacOSUITests` 32/32、`FiscalmacOS` Release 与 `FiscaliOS` generic iOS Simulator Release 构建及 `git diff --check` 全部通过。
- 第四次独立复审确认第三次复审的两项问题已闭环，但全页复扫新发现 1 项 P2：现金流入账、报销到账及账单导入行处理等“结果未知/回读未知”状态仍有部分复用红色确定失败卡片，可能误导用户把“需要安全核验”理解为“操作已经失败”。应统一拆分为黄色恢复提醒，并保留安全检查或读取最新状态操作。
- 第四次独立复审证据：`git diff --check`、完整 `FiscalKitTests` 398/398（39 suites）、`V15GallerymacOSUITests` 32/32 与 `FiscalmacOS` Release 构建通过；基于当前代码重新生成 146 张 macOS 截图并抽查全页视觉矩阵。因复审按要求及时收口，本次 Reviewer 未重复执行 iOS build；上一修复回合的同代码 iOS Simulator Release 构建已通过。复审只读，未修改文件、提交、发布、换包或触碰六个受保护 scheme。
- 第四次独立复审的 1 项 P2 已修复：设计系统新增“确定失败”和“结果暂时不明”的统一语义分流；确定失败继续使用红色，结果未知改为浅黄色虚线恢复提醒，并保留安全检查、读取最新状态或停止恢复操作。现金流入账、报销到账、账单导入行处理已全部迁移；横向扫描同步收口了快速记账和账目分类修改中同类的 macOS 状态，iOS 原有视觉保持不变。
- 本轮补充语义映射单测、三组 macOS 未知结果 UI 回归和报销未知结果 Gallery 场景。验收结果：针对性共享测试 67/67（4 suites）、针对性 UI 3/3、完整 `FiscalKitTests` 399/399（39 suites）、`V15GallerymacOSUITests` 35/35、`FiscalmacOS` Release 与 `FiscaliOS` generic iOS Simulator Release 构建及 `git diff --check` 全部通过。
- 真实视觉走查使用当前 Debug Gallery 核对报销到账、现金流入账和账单导入行处理三组未知结果：黄色状态、恢复动作、原操作禁用和中文可访问说明均正确，未再出现红色确定失败表达；未操作 `/Applications/Fiscal.app`。
- 按用户要求先后两次清理本轮与历史验证副产物：移除 Fiscal 的 Xcode 派生缓存、展开的发布校验目录和临时日志/压缩包；项目目录当前约 889 MiB，磁盘可用空间约 26 GiB。11 组既有签名发布 `artifacts/` 均保留，源码、发布记录和六个受保护 scheme 未因清理受损。
- 用户已明确取消再次独立复审并授权“一条龙发布”。v1.8.0（Build 36）按已通过的完整测试、双端 Release 编译和真实视觉走查进入发布链：版本同步、提交推送、不可变标签、干净标签签名打包、生产只读验收、macOS 可恢复换包和 iOS IPA 交付；本轮累计差异无 Backend 或 migration，不部署后端、不创建无意义数据库备份。
