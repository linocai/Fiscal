# Fiscal · PROJECT_PLAN

> 当前目标：**发布 Fiscal v1.9.0（Build 37）** ｜更新：2026-09-03（Asia/Shanghai）｜阶段：**RELEASING · 一条龙发布执行中**

## 1. 当前事实、范围与边界

- v1.8.0（Build 36）macOS 升级已经发布；不可变标签 `v1.8.0` 指向 `da8cecafe8ccef12961ec57d65ed1972f0576fd0`，发布证据见 `archive/releases/v1.8.0/RELEASE_STATE.md`。本轮 iOS 全页面升级锁定为 v1.9.0（Build 37）。
- 本轮只升级 iPhone 客户端的全页面视觉与交互。现有根结构是“今日、账目、中央记一笔”两个主入口加全屏业务工作区；保留其高频骨架，重做层级、导航、页面组织、表单、状态和反馈，不把 macOS 三栏照搬到手机。
- 覆盖启动/解锁、今日、流水与账户余额、记一笔、未来时间线、现金流、信用账期、分期、报销、报表与下钻、AI 待确认、PDF/账单导入、待同步、系统与数据、设置与主数据，以及它们的读取、写入、确认与回执状态。
- 保留米白、深绿、金色的品牌方向和所有现有账务数据语义；金额继续走 `CNYAmountParser`，业务日期继续用 `Asia/Shanghai`，既有预览失效、版本冲突和异步 generation 守卫不得退化。
- 默认不改 Backend/API/数据库/migration。若某项现有 iOS 能力确实被后端契约阻断，停止该项并单列阻断，不能顺带扩展范围。
- 截图记账不变：只允许 iOS 快捷指令读取最近 10 分钟截图，由用户轻敲背面两下触发；不增加 App 内照片、截图或 OCR 入口。也不新增待同步原地编辑、提醒、归档恢复新能力、附件或设备管理。
- 六个既有本地 scheme 修改受保护：`FiscaliOS.xcscheme`、`FiscalmacOS.xcscheme`、`V15GalleryiOS.xcscheme`、`V15GallerymacOS.xcscheme`、`V15RootSmokeiOS.xcscheme`、`V15RootSmokemacOS.xcscheme`；不得覆盖、还原、生成或提交。

## 2. 固定 iPhone 体验合同

- **视觉与信息层级**：沿用 macOS 已验证的现代、克制、重视金额的方向；重要金额、待处理事项和下一步动作先看见。深绿只用于品牌、选中、主操作和正向金额；金色用于支出、欠款和注意事项；确定错误与危险操作用红色；结果未知、离线和待核验用黄色；普通提示保持中性。任何状态不能只靠颜色表达。
- **按钮与表单**：每个操作区最多一个主要操作，其余分为次要、安静、危险；危险操作必须先说明对象和后果再确认。新表单初始安静，字段失焦、修改或提交后才在字段旁显示错误；页面级错误在当前 sheet/全屏页顶部；相同禁用原因只显示一次；保存仅在内容有效且有变化时启用。
- **导航与触控**：保留底部“今日、记一笔、账目”作为唯一高频导航，不把所有业务硬塞进 Tab；其余能力用清楚的任务入口和全屏 `NavigationStack` 工作区承接。详情、筛选和轻量确认用合适 detent 的 sheet；复杂表单、导入和多步操作用全屏页。返回遵循系统返回/关闭语义，回到原来 Tab、筛选和滚动位置，不嵌套失控的 sheet。
- **手机专属门禁**：所有可点目标至少 44pt；主操作避开底部安全区，键盘出现时字段、错误和提交区始终可见且可关闭键盘。项目只面向 iOS 26 及之后版本的现代 iPhone 竖屏，保持竖屏锁定；真实操作与视觉验收固定复用现有 iPhone 17 Pro / iOS 26.5 模拟器，并在同一设备上用长内容和大字号验证布局弹性，不新增旧款 iPhone、横屏或 iPad 范围。
- **可访问性与外观**：支持浅/深色、默认字体、XXXL 与辅助功能大字号；优先换行、纵向堆叠和滚动，不缩小、截断或用颜色代替文案。所有图标、底部入口、危险确认、禁用原因和回执有中文可访问名称；减少动态效果和透明度时仍可读、可操作。
- **共享层防回归**：共享 `V15DesignTokens`、`V15Controls`、`V15StateViews` 与模型只能通过明确平台分支改变 iOS；每个 P 都要重跑 macOS Gallery/编译，已发布 macOS 的布局、红色错误和黄色未知结果不得回退。

## 3. 已知现状与统一验收

- 当前 iOS 已有 `V151IOSWorkspace`、iOS 专属功能页、共享状态组件、fixture Gallery、Root Smoke 与 14 组 iOS Gallery UI 测试；已有 API 与 fixture 能覆盖主要读取、写入、冲突和恢复路径，当前没有已知的 Backend 阻断。
- 每个 P 都要补齐本范围的真实页面和 Gallery 场景：加载、空白、确定失败、离线只读、冲突、结果未知、禁用、危险确认、成功回执、部分完成/分页失败；写入状态必须留在发起操作的 sheet 或全屏页。
- 每个 P 收口至少通过对应单测与 iOS Gallery UI 测试、`FiscalKitTests`、`FiscaliOS` generic iOS Simulator build、`FiscalmacOS` build 与 `git diff --check`；共享 View 再加 macOS Gallery 关键场景。整轮视觉门禁另见 Review 工作流。

## 4. Build 阶段

### P1 · iPhone 外壳、解锁与设计底座（完成）

- **目标**：先让 App 从打开、解锁到进入工作区都像一个现代 iPhone 财务产品，并建立后续页面共用的控件和状态语义。
- **页面/流程**：启动连接、个人口令解锁、错误口令、无效凭据、系统未就绪、离线只读；底部导航、中央记一笔、任务入口、全屏工作区和返回链路。
- **必须解决**：清除旧式描边/整行强调按钮；统一导航标题、工具栏、卡片、金额、列表行、sheet、底部安全区与键盘避让；把 iOS 的确定错误迁移为红色、未知结果为黄色、普通说明为中性，且不改变 macOS 已发布语义。
- **不做**：不新增 Tab、业务入口、登录方式或离线写入能力；不改变快捷指令和通知的业务动作。
- **验收**：从任一解锁结果都能明确知道下一步；任一工作区可系统式返回原上下文；iPhone 17 Pro / iOS 26.5 的大字号与长内容场景下，底栏和中央记一笔不遮挡内容，浅/深色和减弱动效均可读。

### P2 · 今日、流水、账户余额与快速记一笔（完成）

- **目标**：把最高频的“看今天、看余额、查流水、立刻记一笔”做成单手可完成的连续流程。
- **页面/流程**：今日概览、账户价值和待决定事项、账户余额板、流水搜索/筛选/分页、账目与账户详情、待同步只读队列、记一笔及还款/分类/报销等既有快捷去向。
- **必须解决**：金额、流入流出、信用欠款和待收报销的层级与标签一致；搜索和筛选不盖住列表；详情用可关闭 sheet，列表选择、回读与写入回执不串状态；记账按金额、类型、账户、日期、名称的自然顺序，键盘上方仍可见保存和错误。
- **不做**：不重算账户余额，不改交易/账户 API、金额解析、类型校验、待同步策略或新增原地编辑。
- **验收**：现代 iPhone 竖屏上的长账户名、长金额和大字号均不挤压；真实记账、转账/还款、加入报销等既有路径保持方向正确；搜索、筛选、详情关闭和成功回读都回到正确上下文。

### P3 · 已知未来、现金流、信用、分期与报销（完成）

- **目标**：让所有“将来要发生的钱”在手机上按时间、风险和下一步动作组织，而非桌面式多栏挤压。
- **页面/流程**：未来时间线及 7/30/60/90 天和账户筛选；现金流计划；信用账期和还款；分期创建、预览、修改、结清/取消；报销单、到账、历史和候选账目。
- **必须解决**：将二级能力整理为清楚的 iPhone 层级与单列摘要；系统派生与可编辑项、开放/结清/逾期、已锁定/未来/取消期次分明；报销总额、已到账、未到账始终带标签；预览随任何输入失效，错误、冲突和结果未知留在当前流程可恢复。
- **不做**：不新增提醒、现金流规则、报销分摊、信用账期或分期的后端语义。
- **验收**：每个页面都有无选择、空、只读、编辑、危险确认和异常 Gallery；真实还款、分期预览/确认、报销到账路径的金额与回执正确，返回后保留筛选与阅读位置。

### P4 · 报表、期间与下钻（完成）

- **目标**：把报表从“缩在手机上的长表”改成清晰可读的财务分析和明细追踪体验。
- **页面/流程**：月报/年报、总览、支出、现金流、债务、期间切换、刷新、CSV/PDF 导出、分类/指标下钻、下一页和返回。
- **必须解决**：当前期间、口径和镜头始终可见；核心金额、趋势和分类结构先于说明文字；下钻用导航栈展示完整明细，返回后不丢期间、口径或滚动位置；零值、空数据、读取失败和离线快照不伪装成正常报表。
- **不做**：不新增统计口径、图表数据、导出格式或改报表服务契约。
- **验收**：四镜头、年/月切换和下钻在 iPhone 17 Pro / iOS 26.5 的正常及大字号场景可读；导出确认、过期数据、首次/下一页失败与重试有可见且不重复的反馈。

### P5 · AI 待确认与 PDF/账单导入（完成）

- **目标**：让需要人工决定的事项优先、证据可追溯、确认过程安全，不让 OCR 原文或导入工具压垮手机界面。
- **页面/流程**：AI 待确认/历史、提案详情、创建/修改/确认/忽略/撤销/回读；PDF 选择、同意边界、页证据、识别行、单行/批量处置、预览和确认；来自快捷指令的待确认提示入口。
- **必须解决**：待确认和历史分层，低置信字段与最终入账值可对照，原始 OCR 默认收起；PDF 流程按选择、提取、解析、处置、预览、确认清楚分步；单行与批量动作不混用，未知结果只能核验不可重复写入。
- **不做**：不新增 App 内照片、截图或 OCR 入口，不让 AI 自动记账，不改变最近 10 分钟截图快捷指令和用户的轻敲背面触发方式。
- **验收**：待确认零/非零、低置信、删除确认、服务失败、撤销与回读均可走通；导入覆盖空白、脱敏证据、缺失、冲突、部分成功、离线与分页失败，所有动作和隐私边界有中文说明。

### P6 · 系统与数据、设置与主数据（完成）

- **目标**：让安全、配置和主数据在手机上易找、易读、写入安全，不再是长表单和分散操作的集合。
- **页面/流程**：运行状态、数据归档与恢复边界、个人口令；账户、分类、商户；归档区；AI 与识别设置，及各自新建、修改、归档/恢复、合并/拆分、搜索/分页和确认回执。
- **必须解决**：用清楚层级区分系统状态、安全操作和配置；账户/分类/商户入口完整可达，信用字段只在信用账户出现；字段错误严格归属当前对象且在成功、切换、新建后收敛；危险归档/恢复、合并/拆分先明确对象、影响和确认。
- **不做**：不改变密码、归档恢复、分类合并拆分、商户分页或 AI 配置的授权/数据规则，也不新增设备管理或归档恢复能力。
- **验收**：空表单不预警，保存门禁和单一禁用原因正确；每个管理页都有空白、错误、只读、危险确认和成功回执场景；修改后列表、详情与返回层级同步且 macOS 主数据不回归。

## 5. Build、Review、修复、独立复审与发布

- **Build**：只按 P1 → P6 施工；P 是实现批次，不包含审查、修复、提交或发布。每个 P 完成后更新本节当前状态，不追加过程长日志。
- **Review**：Build 完成后单独进行代码、全页面视觉、真实触控路径、键盘/sheet/返回、浅深色和动态字体审查；所有模拟器操作只使用现有 iPhone 17 Pro / iOS 26.5，不创建或使用旧款 iPhone；输出可定位、按严重度排序的问题。
- **修复**：只处理 Review findings，并重跑受影响的单测、iOS/macOS build、Gallery/UI 与真实模拟器走查；不为修复另起 P。
- **独立复审**：修复后由未参与施工者复扫全部 iOS 页面和共享 macOS 回归；复审只读，不修改代码、scheme、版本或发布记录。
- **发布**：用户已于 2026-09-03 授权“一条龙发布”。目标锁定为 v1.9.0（Build 37）；完成最终门禁后提交并推送源码、创建不可变标签、从干净标签签名打包、只读核验宁波生产、可恢复更换 macOS 客户端并交付 iOS IPA。
- **当前状态**：P1–P6、Review、修复和独立复审已闭环，现金流 AX5 长金额裁切等最后问题均已修复。发布前累计核对确认从宁波现役 revision `64cb1aee0190eeba81f1a38cf6b322d4d1ee33e4` 到本次候选没有 `Backend/`、API 或 migration 差异；生产仍为 Alembic `20260831_0038`，服务、定时任务、内外网健康与鉴权读取均通过，因此不会部署、迁移、重启或创建发布专用数据库备份。当前正在生成 1.9.0（37）工程并执行最终 Apple 门禁；六个受保护 scheme 必须保持原有本地修改且不进入发布提交。

## 6. 后续 Backlog 与白话摘要

- 本轮之后再由用户选择：待同步原地编辑、提醒、归档恢复新能力、附件、设备管理。

这次会把 iPhone 的每个页面都整理成“先看懂钱和下一步、再做操作”的界面：底部只留下最高频入口，复杂事情用顺手的全屏层级打开；表单、错误、确认和回执都会留在你正在操作的地方。功能和数据不重做，截图记账也完全按现在的快捷指令方式保留。
