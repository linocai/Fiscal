# Fiscal · PROJECT_PLAN

> 当前目标：**v1.7.0（Build 34）** ｜更新：2026-09-01（Asia/Shanghai）｜阶段：**RELEASE · 一条龙发布已授权并执行中**

## 1. 现在要解决什么

- v1.6.0（Build 33）已经正式发布，生产源码为 `dee89464`、发布记录为 `f484f55`；本轮不重发、不移动 `v1.6.0` 标签。
- 用户认可现有配色、字体和基础视觉语言；**不换肤，不重新发明一套视觉风格**。
- 这次要解决的是 macOS 首页“东西都塞在左边、真正要看的账户和流水反而被挤走”的问题，并删除没有帮助的余额核对链路。
- 版本为 **v1.7.0（Build 34）**：这是功能删除、主导航重排和接口收口，不是补丁级小修。
- 完整施工说明见 [v1.7.0 macOS 工作台重整](archive/plans/v1.7.0-macos-workspace-reset.md)。本文件是唯一入口；发布事实继续以 [v1.6.0 发布记录](archive/releases/v1.6.0/RELEASE_STATE.md) 为准。

## 2. 用户已经定下来的样子

- 左侧只放一级模块：**流水、未来现金流、报表、系统与数据、设置**。不再塞“全部、未分类、需要决定、待同步、本月信用、待收报销”等筛选。
- “流水”仍是主工作台：中央是所选月份的真实账目，点一条后右侧仍看详情和操作；这个三栏工作方式保留。
- 月份和账户保留，但移到“流水”的内容标题栏：月份可明确切换；账户以可见的余额条/账户选择器出现，不会因左栏滚动而消失。账户切换只改变本次流水范围和右侧账户详情，不偷改月份。
- 顶部不再展示没有作用的 `Fiscal + 月份 + 项数 + 密度` 散件和大片空白。把空间留给当前模块、月份、账户、搜索和“记一笔”。
- “记一笔”仍是主操作，固定在当前工作区右上，不再占据左栏第一大块。
- 未来账单、工资、报销回款、信用到期和现金流计划从月流水顶部移走，统一进入“未来现金流”。它只展示已经录入、已排期或已有账期的事实，不做预测。
- 报表的口径、计算和内容继续使用 v1.6.0 的正式报表 v2；只是把入口提升到一级模块。导入、归档、AI 待确认等低频事务收进“系统与数据”。
- 所有“需要决定”总表和“尚无余额核对锚点”之类泛化提醒消失。真正可处理的问题只在所属模块、所属账目或所属导入批次出现，并给出直接操作。

## 3. 明确删除与保留

- **彻底删除余额核对**：双端入口和页面、检查点历史、差额诊断、核对提醒、API、service/repository/schema、数据库表和相关测试都移除。
- 删除 macOS 的“紧凑／舒适”选择；保留已获认可的紧凑、可读密度作为唯一布局。
- 保留账目、账户、分类、信用账期、分期、报销、现金流、导入、归档、报表、服务端预览写入和登录恢复等既有业务事实。
- iOS 不重做信息架构或视觉；但因后端和余额核对被删除，必须移除 iOS 的“核对”入口、页面、提醒、模型和契约，保证它不会请求已删除接口。
- 截图记账玩法不变：只保留 iOS 快捷指令读取最近 10 分钟截图、轻敲背面两下触发；不增加 App 内照片、截图或 OCR 入口。

## 4. 施工批次

### B17-A · 先安全拆掉余额核对

状态：**实现与 PostgreSQL 回归完成**。后端核对 route/service/repository/schema/model 已移除；`0038` 已从 `0037` 线性建立，仅删除核对与通用提醒表；repository 只返回四项真实完整性计数，legacy 报表读取独立序列化写入无状态 `0/null` 占位，公开 OpenAPI／新客户端契约不再暴露核对词或字段。formal mutation receipt 已不再声明已退役的 `attention`／`reconciliation` scope；有数据 `0037 → 0038` 回归证明核心账务事实保留且 downgrade 明确阻断。

- 先把核对从报表完整性、账户详情、信用账期改期、通用提醒和数据修订范围中解开；不能只藏按钮后留下死接口或死字段。
- 新建一个从 `0037` 线性前进的 `0038` migration。上线前备份并验证数据库，记录核对表行数与摘要；migration 才删除核对表及只为“需要决定”服务的静音记录。账目、分录、账户、信用账期和导入数据绝不删除。
- 报表的新客户端契约不再有“对账差异／最近核对”。为不让已安装的 1.6 客户端因缺必填字段而连正常流水也打不开，旧报表读取在一个过渡版本内只返回 `0/null` 占位；不保留任何核对数据、计算或操作接口。`/reconciliation/*` 正式移除。确认两端都已升级后，再删除这两个不可见占位字段。
- 详见执行记录的“删除边界、数据保护与兼容”部分。

### B17-B · 重做 macOS 工作台的摆放，不重做视觉风格

状态：**实现与独立视觉复审完成**。`V151MacWorkspace` 已改为五个常驻模块；右侧按模块切换而不丢失左栏，月份、账户/余额、搜索与记一笔集中在流水上下文栏；账户筛选与右侧详情已分属独立状态，点交易不再隐藏筛选，月份切换保留有效筛选，账户消失才清真 filter 并 reload；搜索草稿必须确认才生效且可清除；旧 lens、密度、未来事项混入和账户核对入口已移除。

- 以 `V151MacWorkspace` 为唯一壳重排导航。左栏改成固定、短小的模块导航；不允许账户、月份或筛选迫使用户滚动左栏。
- 在“流水”中做一条紧凑的上下文栏：月份、账户余额与账户选择、搜索、“记一笔”。中央列表默认显示该月全部正式流水；未来事项绝不混进来。
- 右侧账目详情、批量分类、账户详情和既有写入安全规则继续保留。账户详情删去“进入余额核对”，其余快捷入口只指向仍存在的功能。
- “未来现金流”复用已有现金流、已知未来与对象导航能力，集中展示未来事实及其真实状态；“报表”直接打开现有报表；“系统与数据”承接导入、归档、AI 待确认等低频入口。

### B17-C · 让两端和测试跟上新事实

状态：**实现完成**。共享 Swift、iOS 入口、Today 泛化提醒和 Gallery/fixture 已随已删除的后端契约收口；信用账期预览也不再读取或展示已移除的 checkpoint 字段；截图快捷指令未改。RootSmoke 新增五模块常驻、系统与数据入口与无重复标题的覆盖。

- 移除共享 Swift 的核对 DTO、service、repository、feature、fixture 和深链读取；同步清理 Today/报表里已删除的字段与文案。
- iOS 只做必要的核对删除和契约收口，其他布局、导航和截图快捷指令不借机调整。
- 更新 macOS Gallery/RootSmoke 与共享测试：覆盖模块导航、月份与账户互不串改、月流水不混未来事项、详情仍可操作、未来现金流和报表可达，以及所有删除入口确实不存在。

### B17-D · 先验证，再谈发布

状态：**自动验证与独立视觉复审完成，发布链执行中**。完整 PostgreSQL pytest（394 passed）、Ruff、Pyright、FiscalKitTests（391 passed）、iOS Simulator/macOS App 编译均通过；`0037 → 0038` 有数据迁移已独立回归。2026-09-01 的契约、账户筛选/搜索、分期入口与 mutation scope 修复已定向回归；本机临时 API RootSmoke 实测五模块常驻及低频入口可达后已停止。

- 后端要验证 `0037 → 0038` 的有数据升级、核对路由消失、信用账期改期仍安全、报表与导出不再出现核对词、其余账目/报表/未来现金流不变。
- Apple 要跑共享 `FiscalKitTests`，并编译 iOS Simulator 与 macOS target；macOS 必须实际检查常用窗口宽度、浅深色、长账户名/长金额、空态和错误态。iOS 只回归删除核对后主流程无坏口。
- 独立视觉复审已检查约 1004pt 窗口、长账户、空错态、五模块与主要深色内容；发现的隐藏快捷键无障碍污染已修复为仅在流水模块挂载并从 AX／鼠标命中移除。相关单测、双端 App 编译与修后本机 RootSmoke 实跑均通过；RootSmoke 明确验证四个隐藏命令在流水、未来和系统上下文都不再出现在 AX 树，并在结束后完成测试 App/keychain 清理。
- 用户已于 2026-09-01 明确授权“一条龙发布”；当前按源码提交与不可变标签、干净标签签名构建、宁波备份/迁移/部署、健康验收、macOS 可恢复换包和 iOS IPA 交付的既定顺序执行。

## 5. 不可越过的边界

- 不触碰现有六个本地 Xcode scheme 变更：`FiscaliOS.xcscheme`、`FiscalmacOS.xcscheme`、`V15GalleryiOS.xcscheme`、`V15GallerymacOS.xcscheme`、`V15RootSmokeiOS.xcscheme`、`V15RootSmokemacOS.xcscheme`；不还原、不提交。
- 本轮为保护上述 scheme，禁止再次运行会改写 scheme 的生成器；`project.pbxproj` 已作最小人工同步。
- 金额仍用 `CNYAmountParser`，业务日期和用户可见日期仍用 `Asia/Shanghai`；异步读取继续使用 owner/generation，写入继续使用现有 preview/commit 与幂等规则。
- 宁波是唯一现役 Fiscal 环境。跨 `0037` 或未来 `0038` 的回滚禁止盲目 Alembic downgrade：停写、备份并验证当前库、恢复到新目标、验证后才切换。

## 6. 当前状态与后续 Backlog

- v1.7.0（Build 34）实现、自动验证与独立视觉复审已完成；发布链已获授权并执行中。当前仍未宣称已发布，只有源码、签名包、宁波部署、健康验收、macOS 换包与 iOS 交付全部完成后才改为已发布。
- 本轮之后再决定的候选仍是：待同步原地编辑、提醒、归档恢复、附件、设备管理。它们不自动并入 v1.7.0。
- 发布操作员的下一步：保护六个本地 scheme 不进入提交，创建并推送 v1.7.0 源码提交与不可变标签，然后只从干净标签完成签名、部署、换包和发布记录。
