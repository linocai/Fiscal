# Fiscal · PROJECT_PLAN

> 当前目标：**v1.7.1（Build 35）** ｜更新：2026-09-01（Asia/Shanghai）｜阶段：**RELEASED · 一条龙发布完成**

## 1. 这次为什么要改

- v1.7.0（Build 34）已经完成发布：源码 `64cb1aee`、不可变标签 `v1.7.0`、发布记录 `58e0d25`；宁波运行 `64cb1ae` / Alembic `20260831_0038`。**不重发、不移动既有标签。**
- 用户认可当前配色、字体、留白和基础视觉语言；问题是 v1.7.0 把最该看的账户余额缩成了一个名称下拉，违背了两段原始录屏的要求。
- 本计划重新对照了 2026-08-31 19:12:07 与 19:38:45 两段录屏、已安装 v1.7.0、现有实现和测试。前版完整重整记录见 [v1.7.0 macOS 工作台重整](archive/plans/v1.7.0-macos-workspace-reset.md)，生产事实见 [v1.7.0 发布记录](archive/releases/v1.7.0/RELEASE_STATE.md)。
- v1.7.1（Build 35）只补齐下列 macOS 可见成果；源码、不可变标签、签名包、生产只读验收、macOS 可恢复换包与 iOS IPA 交付均已完成。

## 2. 录屏需求追踪：遗漏了什么

| 录屏中确定的要求 | v1.7.0 实际结果 | v1.7.1 完成标准 |
| --- | --- | --- |
| 账户要放到醒目位置，不能为了看余额而下拉 | 流水顶栏只有“全部账户 / 余额 16,430.42”菜单；展开后也只见名称 | 所有可用账户的名称、类型和余额/欠款直接显示在流水上方的账户余额板中；不是名称菜单的替身 |
| 月度时间保留，但账户和时间都不能再占左侧栏 | 月份已移出左栏；账户也移出，但错误地缩没了 | 左栏继续只放五个模块；月份留在流水工作区，账户余额板紧随工作区顶栏 |
| 重点信息要摆在前面，没意义的功能和空顶栏删掉 | 核对、旧筛选、密度和空顶栏已删除；余额板遗漏 | 保留已完成的五模块、月流水、右侧详情、未来现金流和报表，不把旧筛选或核对带回来 |
| 报表做得好，应从隐藏处拿出来 | 已成为一级模块，但外层和内容各写一次“报表” | 根页面只保留一个用户可见的“报表”标题，不留空白工具栏 |
| 中央流水和点账目后的右侧详情是正确的 | 已保留 | 账户板加入后仍保持：选账户筛流水并打开账户详情；再选账目时账户筛选不丢失 |

另有四项实现收口：

1. “余额 16,430.42”其实只代表现金/借记资产，却没有同时说明信用欠款，语义会误导。
2. “系统与数据”和“设置”也有外层、内容层重复同名标题；设置里还重复放了一整套 macOS“系统与数据”。
3. 从“记一笔”返回时写的是“返回账目”，与用户已定的“流水”不一致。
4. 后端仍保留一个版本的旧报表读取兼容字段 `legacy_report_serialization`；这是给尚未升级的旧 iOS 客户端的保护，不是漏删的功能。

## 3. 用户最终会看到什么

### 流水：真正的账户余额板

- 流水顶部仍是：**流水、月份、搜索、记一笔**。删掉当前“全部账户 / 单一余额”的 `Menu`。
- 紧接其下是账户余额板。第一张“全部账户”总览卡明确分开写：**资产余额**（现金与借记账户合计）和 **信用欠款**（信用账户合计）；不再用一个无说明的“余额”掩盖两种金额。
- 每个未归档账户各有一张可点击卡：账户名称、类型（现金／借记／信用）和金额。现金、借记显示“余额”；信用显示“欠款”，用现有负向金额颜色，不能写成含糊的“当前余额”。
- 点击单个账户：流水只显示该账户，右侧直接打开该账户详情，卡片有选中态。随后点某笔流水时，右侧改为账目详情，但账户筛选和卡片选中态仍保留。点击“全部账户”回到全月全部正式流水并清除账户详情。
- 所有当前可用账户都直接渲染；窗口变窄时改为 2–3 列的多行卡片，余额板自然增高，不能横向藏卡、不能用“更多账户”或名称下拉冒充余额板。归档账户仍只在设置/归档区管理。
- 账户读取中显示骨架；读取失败或账户后来失效时沿用现有错误与清选逻辑，不留下“列表已换、右侧仍是旧账户”的假状态。

### 其余 macOS 收口

- 左侧仍只有 **流水、未来现金流、报表、系统与数据、设置**；月份和账户不回迁左栏。
- 报表、系统与数据、设置的根页面取消重复的外层同名标题和其占用的空白高度；保留原页面自己的标题、功能和必要操作。未来现金流的“已知未来”标题与模块名不同，保留。
- macOS 设置删除“系统与数据”子页；加密归档、运行状态和口令只从左侧一级“系统与数据”进入。iOS 设置里的该卡片、sheet 和全部 iOS 信息架构保持不动。
- 所有从“记一笔”返回流水的文案统一为“返回流水”。

## 4. 技术边界与实施批次

### B171-A · 账户余额板与真实交互（macOS）✅

- 所有权：`App/Sources/FiscalKit/V15/AppShell/V151MacWorkspace.swift`。把现有 `spineSummary` 分成工作区工具栏和余额板；复用 `ledger.accounts`、`facts.cash.currentBalanceMinor`、`facts.credit.currentDebtMinor` 与现有 `V151MacLedgerAccountContext`，不造新接口或新账务计算。
- 用本地宽度驱动的自适应网格保证所有活跃账户可见；保留当前紧凑行高、颜色、圆角和金额排版。为总览卡、每张账户卡和选中态提供稳定 accessibility identifier/label。
- 保留现有账户筛选、账户详情、月份、搜索和 generation/owner 保护；覆盖账户点击、点账目、月份切换、刷新后账户已归档、加载失败五种状态。

### B171-B · 去重与 macOS/iOS 边界 ✅

- 调整 `V151MacWorkspace` 的根模块工具栏结构：没有动作的根页不渲染空标题栏；“系统与数据”只保留 AI 待确认与账单导入的动作栏，不重复其标题。
- 在 `V15SettingsView` 的 **macOS 分支**排除 `security` pane；保留 `V15DataSecurityMacView` 给一级模块使用。iOS 分支（设置卡片、`V15DataSecurityView`、截图记账）不得改动。
- 将 ledger 父目的地的显示名从“账目”统一为“流水”，只影响返回文案，不改账目数据、深链或业务类型。

### B171-C · 测试、版本与发布前置 ✅

- 扩展 `P10MacWorkbenchTests`：资产与信用金额的展示语义、账户筛选与右侧详情的独立所有权、点击“全部账户”复位、账户消失后的安全收敛。
- 扩展 `V15RootSmokemacOSUITests`：余额板直接出现全部 fixture 账户（名称、类型、金额），信用卡显示“欠款”；点卡筛流水并进入账户详情；点账目后筛选仍在；三处根页不存在重复外层标题/空栏；macOS 设置没有“系统与数据”子页；“返回流水”可见。不能只断言一个菜单按钮存在。
- 手工视觉验收必须覆盖浅/深色、约 1000pt 和常用宽度、长账户名、长金额、无账户、加载和错误态。以“所有余额直接看见且读得懂”为通过条件，不以截图相似或编译通过替代。
- 用户明确开始施工后才将 `App/project.yml` 的版本更新为 `1.7.1 / 35`。由于六个 scheme 是用户本地变更，版本同步不得覆盖、还原或提交它们；先在干净临时副本运行 xcodegen 得到精确项目版本差异，再只把该差异同步到工作区的项目文件。

## 5. 这次明确不做

- 不重做 iOS 页面、导航或截图快捷指令；iOS 只会随同一版本号重新构建。
- 不改后端、OpenAPI、数据库、Alembic、宁波部署或任何账务计算；本轮账户板只读现有数据。
- 不删除 `Backend/src/fiscal_api/api/legacy_report_serialization.py`，也不删它的兼容测试。用户尚未确认旧 iOS 已安装更新，兼容层继续返回无状态 `0/null` 占位，直到确认所有旧客户端不再使用后才另立小范围计划移除。
- 不恢复余额核对、需要决定总表、旧左侧筛选、紧凑／舒适切换或空顶栏；不把“待同步、提醒、归档恢复、附件、设备管理”等 Backlog 自动并入本版。
- 不触碰、还原或提交六个既存 scheme 变更：`FiscaliOS.xcscheme`、`FiscalmacOS.xcscheme`、`V15GalleryiOS.xcscheme`、`V15GallerymacOS.xcscheme`、`V15RootSmokeiOS.xcscheme`、`V15RootSmokemacOS.xcscheme`。

## 6. 完成与发布门槛

1. 先完成 B171-A → B171-B → B171-C；每批先跑针对性测试，再跑 `FiscalKitTests`，并编译 iOS Simulator 与 macOS App target。
2. 独立复审必须实际看 macOS 余额板和三处去重页面，复审重点是可见信息、窄窗口、浅深色和账户/账目状态，而不是只读代码。
3. 只有用户再次说“一条龙发布”才进入发布：先核对宁波仍是 `64cb1ae` / `0038` 且目标累计差异没有后端内容；若仍纯客户端，则不做数据库迁移或后端部署，按完整客户端签名、验签、macOS 可恢复换包与 iOS IPA 交付执行。用户本人安装 iOS，不能声称已安装。
4. 发布时创建新的不可变 `v1.7.1` 标签；不移动 `v1.7.0`。若后续同营销版本需要另一构建号，另建不可变 build 标签。

## 7. 当前状态与下一步

- B171-A/B/C 已完成：流水顶部现在直接显示“全部账户”资产/信用汇总和全部活跃账户卡；账户点击、账目详情和筛选状态已按计划收敛；报表、系统与数据、设置的重复外层标题已移除；macOS 设置不再重复系统与数据入口；返回文案已统一为“返回流水”。
- 版本已更新并从实际 App 验证为 `1.7.1 (35)`。`FiscalKitTests` **392 passed / 0 failed**；macOS App 与 iOS Simulator 构建通过；隔离本机数据库上的 macOS RootSmoke **1 passed / 0 failed**。临时 API 已停止，临时数据库已删除。
- 已用生产数据做只读视觉复看：7 个账户及汇总全部直接可见，资产与信用欠款分开，零欠款显示 `¥0.00`，账户详情和三处去重页面正常。没有执行任何记账或设置写入。
- 独立 Reviewer 已重新构建并实际进入 `1.7.1 (35)` 只读生产数据工作台，在接近 1000pt 的窗口检查余额板、7 张账户卡、筛选/详情联动和三处去重页面，结论为 **无发现**。暗色模式和写入类动作未在本次独立复审中执行，作为已知未覆盖项保留。
- 六个既存 scheme 的施工前后 SHA-256 完全一致；它们仍是用户原有本地变更，没有被覆盖、还原或纳入本轮实现。除这六项外，工作区只有本计划列出的 v1.7.1 源码、测试、版本与计划改动。
- 一条龙发布已完成：发布源码 `c1e53f9f259abb0307fafd0e78487c6d52235d05` 已推送，不可变标签 `v1.7.1` 已推送并固定指向该提交；宁波因累计差异不含 Backend 而继续保持 `64cb1ae` / `0038`，生产健康与鉴权只读验收通过。
- macOS 已可恢复换装为 `/Applications/Fiscal.app` `1.7.1 (35)`，旧 `1.7.0 (34)` 保存在 `/Applications/Fiscal-v1.7.0-build34-backup-20260901-165027.app`；iOS IPA 已交付到 `~/Downloads/Fiscal-iOS-v1.7.1-build35-development.ipa`，等待用户本人安装。完整证据见 `archive/releases/v1.7.1/RELEASE_STATE.md`。
