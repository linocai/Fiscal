# Fiscal · PROJECT_PLAN

> 当前目标：**Fiscal V2 前端升级** ｜更新：2026-09-04（Asia/Shanghai）｜状态：**RELEASED · v2.0.0 (38)**

## 1. 目标与完成定义

V2 不是继续增加财务模块，而是让用户打开 Fiscal 后能立刻回答：我现在有多少钱、欠多少钱；最近什么改变了它；接下来确定会发生什么；现在是否有一件真实财务事项需要处理。

产品母题固定为：**一条财务时间主轴，两种平台工作方式。** iPhone 是快速确认与单项处理的“现在界面”；macOS 是浏览、比较、搜索、核对与连续处理的工作台。两端共享金额、日期、状态和对象关系，绝不机械复刻布局。

本轮完成后，日常入口只服务看钱、找钱、记钱和处理明确事项；低频专项能力仍可发现，但不再占据首屏或一级导航。完成不以截图漂亮为准，必须同时通过双端真实任务、异常状态和构建门禁，并关闭 P4 所列全部 Review P1/P2。

## 2. 已确认事实、痛点与决定

- 用户已确认 V2 必须从使用者痛点出发，主动删除、合并或下沉无用功能；不是保留全部功能后重新换皮。
- 用户的核心痛点是：当前资产/负债不够一眼可信；一笔钱的过去、当前与未来关系分散；首页和导航充满重复入口；高风险结果、冲突、离线与待同步脱离发生对象。
- 现有账务、信用、分期、报销、现金流、报表、AI 复核与账单导入能力已经有客户端数据与安全合同。本轮**不改 Backend、API、数据库或 migration**，也不为视觉统一伪造新事实。
- `FISCAL_V2_FRONTEND_UPGRADE_PLAN.md` 是已确认的体验依据；本文件是唯一施工控制面。若两者冲突，以本文件的当前决定和范围为准。
- 所有业务日继续使用 `Asia/Shanghai`；金额继续由 `CNYAmountParser` 解析为元、两位小数。写入、预览、幂等、冲突、结果未知、离线只读和待同步保护不得退化。

## 3. V2 产品合同

### 3.1 财务事件与时间边界

- 正式流水、信用应还、分期期次、报销回款与有来源的现金流计划，以同一“财务事件”阅读语言出现：**何时 → 什么 → 多少 → 影响什么 → 现在状态 → 下一步/结果**。
- 已发生、准确应付、已确认未来、已排期、预计、候选、待同步、冲突和结果未知必须有文字与结构差异；候选、预计和未确认写入绝不混入正式余额、流水或报表。
- 时间主轴是阅读与定位方式，不是万能页：长表单、导入、预览、危险确认和批量处置必须进入上下文工作区；不建立脱离真实对象的待办中心。
- 每次从事件进入专项任务、详情或报表下钻，返回时恢复原范围、搜索/筛选、选择和阅读位置。

### 3.2 功能去留与入口决策

| 当前能力/入口 | V2 决定 | 目标位置与理由 |
| --- | --- | --- |
| 今日 | 保留并重构 | 当前数据时间、主要资产/负债、最近变化、最近未来与真实待处理事项；删除功能大厅式入口卡。 |
| 记一笔 | 保留为最高频动作 | iPhone 固定中央动作；macOS 工具栏与键盘入口。金额优先，特殊类型只展开所需字段。 |
| 流水与已知未来 | 合并为同一时间语义 | 历史正式账目与有来源的未来事项在“账目/财务时间线”中连续阅读；现有读取模型可分段复用，不能臆造统一服务端事件。 |
| 账户余额板 | 保留但降级 | 是范围、资金承载对象和摘要下钻，不再成为独立入口墙。信用账户继续使用欠款/应还语义。 |
| 信用、分期、报销、现金流 | 保留并下沉到对象层 | 从相关金额、事件详情或具名专项入口进入；不争抢主导航。关系、预览与结果保留。 |
| 报表 | 保留为分析镜头 | 从当前期间/范围进入并能下钻回账目；不在首页与当前余额争夺视觉第一层。 |
| AI 待确认、账单导入 | 合并为复核工具层 | 候选仍需人工确认；导入仍是完整多步流程。只保留一个具名、低频入口，删除跨页重复快捷入口。 |
| 待同步 | 取消独立产品身份 | 作为相关事件的系统状态；保留安全核验页，禁止待同步原地编辑或盲目重放未知结果。 |
| 主数据、数据与安全、设置 | 合并为治理层 | 账户/分类/商户、识别与导入、归档/安全按组组织；正常状态紧凑，异常才提升。 |
| 现有重复“更多”、卡片网格、溢出菜单捷径 | 删除 | 不能再以第三个 iPhone 根 Tab、macOS 模块大厅或今日/账目重复菜单存在。低频能力只从一个具名二级路径发现。 |

“删除”在本轮首先指删除重复的可见产品身份、入口和无效容器；没有用户明确确认前，不物理删除任何仍承载既有财务事实、写入、恢复或安全责任的领域能力。

## 4. 双端成果合同

### iPhone / iOS

- 根结构严格为 **今日 / 记一笔 / 账目**：两个切换入口与居中的创建动作；不得新增“更多”第三 Tab，也不得用底部导航承载系统工具。
- 今日顺序固定为：上海业务日与数据新鲜度 → 当前财务位置 → 最近变化与最近未来 → 仅在真实对象存在时显示的待处理事项 → 安静结束态。首屏最多一个主金额区和一个待处理区。
- 账目默认锚定今天，提供过去、回到今天、未来的明确移动方式；搜索、账户、日期、类型、来源、状态与作废筛选清楚可见。事件行采用平面列表，不用卡片墙。
- 路由固定：未来是账目内的时间范围；“财务分析”是今日中的具名次级动作；“设置与治理”是今日顶部唯一的低频工具入口。AI/导入/待同步只在后者发现，信用/分期/报销/现金流优先由事件关系进入。
- 详情先呈现身份、金额、日期、状态、账户影响和关系；编辑、预览、冲突与危险操作升级为完整页面，sheet 内必须就地展示错误与恢复路径。
- 报表、信用、分期、报销、现金流、AI/导入和治理层只从关联对象或唯一具名二级入口进入；不得同时在今日、账目和溢出菜单重复出现。
- 保留现有截图快捷指令边界：仅用户轻敲背面触发、只读最近 10 分钟截图；不增加 App 内相册、截图选择或 OCR 入口。

### macOS

- 主窗口收敛为 **财务时间线 / 财务分析 / 设置与治理** 三个清楚空间；信用、分期、报销、现金流、AI 和导入由事件检查器、分析下钻或设置中的具名工具入口进入，不再形成平级模块森林。
- 时间线维持范围栏、中央主轴、右侧检查器的优势：范围栏管理日期/账户/筛选，中央区负责历史与未来阅读、搜索与选择，检查器解释对象关系、可用动作和结果；无选择时保持紧凑状态。
- 常用宽度保持高密度可读；窄窗口收缩范围与检查器，不能压缩金额和标题。创建账目始终可从工具栏/键盘进入，搜索与多选不丢上下文。
- 分析只观察正式事实，持续显示期间、账户范围、口径和数据时间；下钻回到同一范围的时间线。设置与治理收纳主数据、AI/导入、待同步与安全；系统与数据不再是独立常驻导航项。

## 5. 文件责任边界

- 根路由、来源恢复与根事实刷新只在 `V151IOSWorkspace.swift`、`V151MacWorkspace.swift` 收敛；不得新增根 Tab、模块大厅或第二套事实状态。
- 未来核验/展示在 `Features/Timeline/*`；账目范围、筛选与详情在 `Features/Ledger/*`；不可把预计/候选伪装为正式流水。
- 信用、分期、报销、现金流与记一笔只改各自 `Features/{Credit,Installments,Reimbursements,CashFlow,Record}/*` 的既有链路；不得改变金额方向、账户类型校验、预览失效或幂等写入。
- 待同步安全合同在 `Shared/State/*` 与 macOS 工作台；只能补可安全重放入口，不扩大编辑、冲突或结果未知处理权。
- 证据归 `App/Tests/V15RootSmoke*`、`V15Gallery*UITests`、`FiscalKitTests/V15/*`。`Backend/`、API、schema、数据库、migration、版本、发布记录与签名不在范围；增删源文件或改 `App/project.yml` 才运行 xcodegen。

## 6. 分阶段施工与完成状态

### P0-P3 · V2 结构收敛（已完成；P4 前基线）

- 已冻结 iPhone “今日 / 记一笔 / 账目”、macOS “财务时间线 / 财务分析 / 设置与治理”、时间真值与治理层边界；未重启模块大厅或重复入口。
- 已知未来按日期、确定程度、金额方向和来源稳定阅读；预计、准确到期、已确认未来和正式流水不混算。AI、导入、待同步与主数据已下沉至治理层。
- P3 前已通过当时的单元、双端构建、root smoke 与时间线回归，但该证据未覆盖 P4 的五项真实场景，不能作为本阶段验收。

### P4 · Review P1/P2 修复（已完成）

本阶段仅修复 2026-09-04 Review 已确认的五项问题；未恢复模块大厅、未新增根导航、未改 Backend/API/schema/migration，亦未扩大离线写入权限。独立 closure Review 已确认没有 P0/P1/P2；另发现的两个 P2（信用 contextual load 的错误提示、iOS future-open 的 close/onDisappear/行替换竞态）均已修复。

1. **分期正式可达（双端，P1）**：在已选信用账户的对象详情中提供唯一、具名的“分期计划”正式入口，列出现有计划并进入既有的分期管理能力；“改为分期”仍只用于没有关联的新建计划。路由必须携带账户和计划标识，不能回退为“取第一张信用账户”或重新引入全局分期大厅。
2. **macOS queued replay（P1）**：待同步页为可安全重放的 `.queued` 写入提供明确“同步全部/同步”动作；`.failed` 可重试后重放；结果未知、冲突或不可重放项保持隔离，不能借修复绕开既有幂等、联网、错误展示或删除确认保护。
3. **专项写入后的根事实刷新（双端，P2）**：信用、分期、报销、现金流及记一笔在“已确认写入”后，由根工作区使当前范围内的账目、今日/摘要和已知未来一起失效并重载；失败、取消、冲突或结果未知不得伪造成功刷新。所有异步回写沿用 generation/请求归属保护。
4. **对象上下文与返回（双端，P2）**：从账户、交易或未来事项进入信用、分期、报销、现金流时，传递精确对象与来源阅读状态（账户范围、筛选、搜索、选择、位置）；专项页打开正确对象，返回恢复来源，而不是固定跳未来或默认选择第一个对象。
5. **iOS future-open 生命周期（P2）**：未来事项核验任务在刷新、范围变更、离开账目/时间线或开始新导航时失效或取消；异步返回前校验当前生命周期与选择，既不能以旧结果劫持新导航，也不能保留已过期错误或触发与 inspector 关闭竞争的展示。

已完成的实现收敛为：双端路由传递精确账户/计划/交易/报销对象与来源；分期与报销模型按精确对象加载且不回退首项；macOS 只重放安全的 queued/failed 写入；已确认写入或专项返回时刷新根事实；future-open 以 generation、选择与生命周期共同守卫。

## 7. 验收门禁

| 门禁 | 必须通过的证据 |
| --- | --- |
| 产品路径 | 两端均能在 5 秒内看出数据时间、主要金额和下一明确事项；没有重复首页入口、孤立功能页或隐藏的关键能力。 |
| 账务正确性 | 金额、方向、账户、上海业务日、正式/未来/候选区别一致；任何改动不得引入重复写入、重复计算或静默状态升级。 |
| 异常与恢复 | 空、加载、筛选无结果、离线、服务失败、冲突、结果未知、待同步、作废/归档和长内容均有就地、可理解且可恢复的表现。 |
| iOS | 390×844、浅/深色、最大动态字体下根导航、主金额、事件行、键盘与底部创建按钮可见可达；无 sheet 内错误被背景页遮挡。 |
| macOS | 最小/标准/宽窗口下金额、标题、范围、列表与检查器可读；键盘搜索、创建、多选和返回上下文稳定。 |
| 自动验证 | `xcodebuild -project App/Fiscal.xcodeproj -scheme FiscalmacOS -destination 'platform=macOS' test -only-testing:FiscalKitTests`；再分别构建 `FiscaliOS`（generic iOS Simulator）和 `FiscalmacOS`（platform=macOS）。涉及 root 导航时更新并运行对应 smoke/UI test；需要本地服务的测试只在服务已按既有方式启动后执行。 |

### 7.1 P4 定向验收（已完成）

| 风险 | 双端验收证据 |
| --- | --- |
| 已有分期 | 已有分期的信用账户从正式对象路径可进入计划管理；已关联交易不依赖“改为分期”按钮；打开的账户/计划精确匹配选择。 |
| macOS 待同步 | 离线产生 `.queued` 写入后，恢复联网可由待同步页重放并从队列消失；失败重试可继续重放；冲突/结果未知不被批量重放。 |
| 根事实 | 分别完成信用、分期、报销、现金流写入后，返回当前根范围立即反映账目、金额和已知未来；失败/取消不改变显示事实。 |
| 上下文/返回 | 非首张信用账户、带筛选的交易和未来事项均打开正确领域对象；返回后恢复原范围、筛选、搜索、选择与阅读位置。 |
| iOS 生命周期 | 用可控慢响应覆盖点击未来事项后切换根入口、刷新和离开页面；旧响应不能打开专项页或遗留错误，新选择仍可正常打开。 |

已通过：F3B1 26/26、F3A 7/7、`FiscalKitTests` 405 tests / 39 suites、`FiscaliOS` generic iOS Simulator build、`FiscalmacOS` build、iOS Root Smoke 7/7、macOS Root Smoke 4/4，以及 `git diff --check`。六个受保护 scheme 哈希保持不变。Root smoke 使用隔离的临时 QA 数据库；服务、数据库与本轮 `fiscal-v2-p4` 临时 DerivedData 均已清理。

上述 smoke 覆盖已实现的真实内容路径与 P4 回归；它本身不等同于人工视觉验收。v2.0.0 发布候选已另行完成人工复核：iPhone 390×844 浅色、深色、AX5 与长金额，macOS 1000×760/1400×850，以及完整离线只读状态；证据与发布事实见 `archive/releases/v2.0.0/RELEASE_STATE.md`。

### 7.2 P3 基线（仅供参照）

P3 曾通过 399 个 FiscalKit 测试、iOS root smoke 7/7、macOS root smoke 4/4、双端 App 构建及时间线 gallery；这些是 P4 施工前结果，不能替代 7.1 的真实路径与本轮重跑证据。

## 8. 非目标与 Backlog

- 非目标：后端/API/数据库/migration；新的预算/提醒/待办中心；附件中心、设备管理；App 内照片/截图/OCR；iPad、Watch、Web、多用户/多账本；发布、提交或版本升级。
- 若现有事实无法支撑“完整历史+未来同屏”或跨领域关系，保留连续阅读和上下文跳转，不在前端制造第二套事件/余额模型；该问题登记为后端产品议题，不阻塞本轮前端收缩。
- 任何“物理删除某个仍可用的领域能力”的提案，必须先给出使用痛点、替代路径、历史数据影响和双端回归清单，再由用户确认；本轮不自行删除财务能力代码。

## 9. 完成状态与后续边界

V2 与 P4 Review closure 均已完成并于 2026-09-04 作为 `v2.0.0 (38)` 发布。源码提交 `bf5b9c99443a5184a83319b605dd691577d5987f` 已推送到 `origin/main`，不可变标签 `v2.0.0` 已推送；macOS 签名包已替换并启动，iOS development IPA 已交付到 Downloads 等待用户安装。累计差异不含 `Backend/`，因此线上后端、数据库与 Alembic `20260831_0038` 保持不变。完整发布、签名、生产核验、制品与回滚记录见 `archive/releases/v2.0.0/RELEASE_STATE.md`。

六个受保护 Xcode scheme 的既有本地修改保持原样，未被生成、覆盖或纳入发布提交。后续工作从已发布 V2 基线开始，不得重新引入重复根入口、模块大厅、无来源未来或脱离对象上下文的专项跳转。
