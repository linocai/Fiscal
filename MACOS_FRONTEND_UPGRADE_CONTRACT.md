# Fiscal macOS 前端升级合同

状态：待用户确认后进入正式工作流
范围：Fiscal macOS 客户端

## 一、升级目标

- 将 macOS 客户端统一为现代、清晰、克制的个人财务工作台。
- 保留现有米白底色、深绿色主色、金色支出色和当前整体品牌方向。
- 保留现有业务能力、数据含义、账户余额板和桌面端多栏工作方式。
- 让常用窗口、最大化窗口和支持的最小窗口都能合理使用空间，不出现内容被压缩、截断或大片无意义空白。
- 统一所有页面的导航、按钮、表单、卡片、列表、状态、详情面板和操作反馈。
- 完成 macOS 全页面的视觉升级、交互整理、可访问性补齐和真实操作验收。

## 二、升级范围

- 全局应用框架和左侧主导航。
- 流水、账户余额板、流水详情、账户详情、搜索和批量选择。
- 新建账目编辑器。
- 已知未来、现金流计划、信用账期、分期和报销。
- 报表总览、支出、现金流、债务和报表下钻。
- AI 待确认和历史识别内容。
- PDF 账单导入。
- 系统与数据。
- 设置中的主数据、归档区、AI 与识别。
- 页面内的新建、修改、预览、确认、取消、归档、恢复和只读状态。
- 加载、空白、失败、离线、冲突、未知结果、禁用、成功回执和部分完成状态。

## 三、保持不变的边界

- 不增加新的业务模块。
- 不改变现有账目、账户、报销、分期、信用账期和报表的数据语义。
- 不改变截图记账和快捷指令的现有使用方式。
- 不增加 App 内照片、截图或 OCR 新入口。
- 不修改 Backend API、数据库结构或 migration，除非正式施工中发现现有 macOS 功能无法到达是由后端契约缺失造成，并重新提交用户确认。
- 不主动改造 iOS 视觉；共享组件调整不得改变 iOS 既有行为和布局。
- 不发布版本，不换包，不部署生产，直到本合同施工完成、独立复审通过并由用户另行授权。

## 四、全局布局合同

### 4.1 主导航

- 保留左侧五个主入口：流水、未来现金流、报表、系统与数据、设置。
- 统一图标尺寸、文字尺寸、行高、左右留白和选中状态。
- 当前页面必须有明确但不过重的选中反馈。
- 主导航不得因为窗口最大化而显得过小，也不得在常用窗口中过度占宽。
- 主导航中的图标和文字必须同时可识别，不使用无说明的装饰图标代替页面名称。

### 4.2 顶部工具栏

- 每个一级页面使用同一套顶部工具栏结构。
- 工具栏只放页面标题、范围切换、搜索、刷新、导出和页面级主要操作。
- 页面级主要操作不得继续无条件横跨整个内容区。
- 二级页面统一使用返回入口、父级名称和当前页面名称，不再混用不同的返回样式。
- 月份、年份、筛选条件和导出入口必须让用户一眼看出当前状态和可执行动作。

### 4.3 桌面空间

- 常用窗口下优先保证内容可读、操作不拥挤和详情面板可用。
- 最大化窗口下使用新增宽度展示更多有效信息，不把主体锁在一条窄列中。
- 内容不得为了塞进单行而缩小字体或压缩卡片。
- 多栏页面为左侧范围、中间主内容、右侧详情建立统一宽度规则。
- 右侧详情在没有选择时使用紧凑、明确的空状态，不用大面积空白填充页面。
- 单列表单和安全页面设置合理的内容宽度，并通过分区或双列布局使用宽屏空间。

## 五、视觉系统合同

### 5.1 字体与数字

- 建立页面标题、区域标题、卡片标题、正文、辅助说明、字段标签和状态文字的固定层级。
- 核心金额使用统一的等宽数字和对齐方式。
- 重要金额、当前余额、当前欠款、净额和待处理金额必须形成第一视觉层级。
- 辅助说明保持可读，不使用过小、过淡的文字承载必要信息。
- 金额正负、流入流出、余额欠款的文案和符号在所有页面保持一致。

### 5.2 颜色

- 深绿色用于品牌、当前选中、主要操作和正向金额。
- 金色用于支出、欠款、到期和需要关注的金额。
- 红色只用于错误、危险和不可逆风险，不用于普通提示。
- 黄色只用于确实需要用户关注的警告，不在未操作的空表单中提前出现。
- 禁用、只读、归档、离线和未知结果各自使用可区分的视觉状态。
- 所有文字、图标和控件满足浅色模式下的对比度要求。

### 5.3 卡片与分区

- 减少无意义的浅色描边卡片。
- 只有独立摘要、可选择对象或需要明确边界的内容使用卡片。
- 同一页面不使用大量外观相同的盒子承载不同等级的信息。
- 列表主要依靠间距、对齐、背景变化和选中状态建立层级。
- 统计摘要、主内容、说明文字和操作区必须有清楚的视觉区别。

### 5.4 按钮

- 只保留四类按钮：主要操作、次要操作、安静操作、危险操作。
- 主要操作每个操作区最多一个。
- 普通导航、恢复、刷新、筛选和查看详情不使用整栏宽的强调按钮。
- 危险操作不得与保存、创建、查看等普通操作使用相同外观。
- 图标按钮必须有工具提示和可访问名称。
- 按钮必须具备正常、悬停、按下、键盘聚焦、加载、禁用和完成状态。
- 禁用原因只显示一次，不在按钮上下重复同一段说明。

### 5.5 表单

- 字段按照用户实际填写顺序分组，不再平均堆叠所有输入项。
- 金额、类型、账户、日期和名称按照任务重要性排序。
- 未操作的新表单不显示错误。
- 字段失焦、用户修改或尝试提交后再显示对应错误。
- 错误显示在相关字段附近，页面级错误显示在表单顶部。
- 保存按钮仅在内容有效且发生可保存变化时进入可提交状态。
- 表单关闭后不保留会误导下一次操作的错误、预览或临时状态。

## 六、流水与账户升级合同

### 6.1 流水工具栏

- 将页面标题、月份、搜索、刷新状态和“记一笔”整理到统一工具栏。
- “记一笔”作为清晰的主要操作保留，但不得继续表现为横跨整个页面的长条。
- 搜索改为工具栏搜索或明确的快捷搜索面板。
- 搜索打开后自动聚焦输入框，回车执行，Esc 关闭并恢复原选择。

### 6.2 账户余额板

- 保留“全部账户”摘要和每个账户余额卡片。
- 根据可用宽度自动换行或调整列数，始终保持卡片可读宽度。
- 不为了在最大化窗口中单行展示全部账户而缩小文字。
- 借记余额、信用欠款、账户类型和选中状态使用统一格式。
- “全部账户”明确展示资产余额和信用欠款，不能把两者混成一个含义不清的净数。
- 账户卡片选中后，中间流水和右侧详情同步更新并保持明确选中反馈。

### 6.3 流水列表

- 统一日期、标题、分类、账户、来源、方向和金额的列对齐。
- 标题作为第一层，分类与账户作为第二层，来源和补充状态作为第三层。
- 流入、流出、转账、还款、平账和已作废记录使用稳定且可区分的呈现方式。
- 选中行、键盘焦点行、多选行和已作废行必须视觉区分。
- 保留键盘移动、预览、多选和提交能力，并让快捷键提示保持清楚但不抢占内容。
- 长列表滚动时保留必要的顶部范围信息和底部选择状态。

### 6.4 右侧详情

- 账户详情和账目详情使用同一套信息分组。
- 当前金额、标题和状态置于顶部。
- 基本信息、资金影响、来源、历史和可用操作分区显示。
- 普通操作、修改操作和危险操作分开摆放。
- 没有选择时显示简洁说明，不在详情栏中央放置过大的空状态。

## 七、新建账目升级合同

- 将新建账目从窄列后台表单改为适合快速录入的 macOS 编辑界面。
- 金额作为第一输入，进入页面后可直接键盘录入。
- 类型使用清楚的分段选择，并在切换类型时只显示当前类型需要的字段。
- 账户、目标账户、分类和日期使用统一的选择控件。
- 名称与备注降低视觉等级，但保持快速输入能力。
- 转账、还款和其他特殊类型清楚展示资金方向和来源/目标账户。
- 保存区固定在容易到达的位置，不随空白区域漂移。
- 支持 Tab 顺序、回车提交和 Esc 返回。
- 禁用保存时只展示一个明确原因。
- 不改变现有金额解析、业务日期、账户校验和写入语义。

## 八、未来现金流升级合同

### 8.1 已知未来

- 保留左侧时间范围、中间未来事项和右侧详情的三栏结构。
- 将现金流计划、信用账期、分期、报销整理为统一的二级导航，不再使用四个等宽描边长按钮。
- 未来 7 天、30 天、60 天、90 天和账户筛选使用一致的筛选组件。
- 每个事项明确展示日期、标题、来源、确认程度、资金方向和金额。
- 到期、需要决定、已排期和预计状态使用稳定的标签体系。
- 选中事项后，右侧说明来源、金额、状态和下一步可用动作。

### 8.2 现金流计划

- 顶部固定显示流入、流出和净额摘要。
- 中间列表按时间顺序组织，并区分系统派生与手工计划。
- 系统派生事项和可编辑事项使用不同的操作权限表达。
- 左侧未来事项、历史、账户筛选、新建和刷新使用明确的层级。
- 新建与修改表单在右侧提供足够宽度，不把字段压成窄栏。
- 重复计划、单次事项和系统来源显示清楚。
- 只读事项不展示看似可以点击但实际不可用的主要按钮。

### 8.3 信用账期

- 左侧信用账户显示账户名称、账单日、还款日和必要风险状态。
- 中间顶部突出信用额度、当前欠款、可用额度和超额情况。
- 账期列表明确区分开放、已结清、逾期和未知状态。
- 账期详情统一展示本期应还、已还、仍需还、本期消费、分期本金和手续费。
- “调整账期”保持为次要操作，不与主要还款或确认操作争夺视觉重点。
- 账期、分期和相关账目之间的跳转保持一致。

### 8.4 分期

- 左侧计划列表展示标题、状态、未来金额和下一期。
- 中间顶部突出计划总额、未来未出账金额、期数和当前状态。
- 分期期次改为清晰的时间序列，区分已锁定、未来、取消和已结账期。
- 未来负债按月汇总并与期次列表建立清晰对应。
- 右侧详情、修改、操作使用单一分段导航，不重复显示两个“详情”。
- 修改计划、计划操作、提前结清、取消未来等动作按普通与危险级别分组。
- 新分期表单不重复展示相同错误。
- 预览与确认创建必须有清楚的前后关系。

### 8.5 报销

- 报销单顶部同时标明已申报、已到账和未到账金额。
- 不再使用没有标签的 `¥0.00` 代表未到账金额。
- 左侧报销单列表显示标题、当事人、状态、未到账金额和是否需要处理。
- 中间详情统一展示金额矩阵、当事人与分摊、到账历史和关联账目。
- 右侧只显示当前状态允许执行的操作；不可用操作降级显示说明。
- 新建报销单先填写标题、当事人和预计日期，再选择垫付账目和分摊金额。
- 候选账目提供搜索、范围提示和明确选中状态，不在狭窄栏中一次铺满大量候选。
- 登记到账、修改、提交、撤回、作废、归档和恢复按照风险等级分组。
- 到账金额、未到账金额和报销总额在所有位置保持同一语义。

## 九、报表升级合同

### 9.1 报表工具栏

- 总览、支出、现金流和债务使用统一的报表分段导航。
- 月报与年报使用明确的范围选择，不再通过同一个月份文字按钮隐式切换。
- 上一期间、下一期间、当前期间、刷新和导出保持固定位置。
- 导出 CSV 和 PDF 保持现有能力，入口使用标准菜单。

### 9.2 总览

- 保留收入、个人实际承担、净收支和期末信用欠款四个核心指标。
- 增加现有数据能够支持的期间趋势和结构表达。
- 数据完整性改为紧凑状态区，未处理导入、失败导入和未分类只有在非零时提升警示级别。
- 期末账户余额根据窗口宽度使用合理的多列布局。
- 借记余额和信用欠款清楚分组，不混用余额含义。

### 9.3 支出

- 保留现有消费总额、商户退款、净消费、预计可报销、已收报销、个人预计承担和个人实际承担口径。
- 口径切换改为明确选择组件，并始终显示当前主口径。
- 分类支出使用可读的排序条形图或等价现代图表。
- 每日数据使用趋势图表达，零值日期不再以长文本列表占据主体。
- 分类名称、笔数、金额和占比保持同时可读。
- 点击分类或统计口径继续进入明细。

### 9.4 现金流

- 保留流入、流出、净现金流和内部转账的现有语义。
- 顶部用核心指标与期间趋势表达资金实际进出。
- 按账户部分使用对齐清楚的比较视图，不继续作为大块文本表格。
- 内部转入和内部转出独立说明，不与收入支出混合。
- 已知未来事项作为辅助区，不与本期实际现金流争夺第一层级。

### 9.5 债务

- 顶部突出当前信用欠款和待收报销。
- 信用账户欠款、近期还款日和分期安排按紧急程度组织。
- 债务到期使用时间轴或月份分布展示。
- 分期本金、手续费、期数和到期月份保持清楚对应。
- 零欠款、已结清和当前开放账期使用不同状态表达。

### 9.6 报表下钻

- 明细表使用可用宽度，不再缩在大屏幕中央。
- 表头、列宽、金额对齐和滚动行为统一。
- 返回报表、当前范围、当前口径和下钻对象始终可见。
- 空明细、加载失败、下一页失败和离线快照使用统一状态组件。
- 下钻不得改变用户原来的报表期间和统计口径。

## 十、AI 待确认升级合同

- 将待确认和历史记录明确分区或提供清楚的状态筛选。
- 有待确认内容时优先展示待确认任务；没有待确认内容时显示完成状态，再允许查看历史。
- 左侧列表统一展示标题、状态、来源、金额和是否需要复核。
- 中间内容按照“原始证据、AI 识别结果、用户修改、处理记录”分区。
- 原始 OCR 长文本默认折叠或放入可独立滚动的证据区，不直接占满主内容。
- 识别把握只突出低置信度和需要复核的字段，不让所有高置信度字段使用同等重量。
- AI 提取值和最终入账值提供清晰对照。
- 右侧只放当前记录可执行的确认、修改、忽略、撤销和重新读取动作。
- 撤销和其他危险操作与普通操作明确分开。
- 新建待确认内容使用完整表单，不挤在狭窄操作栏中。
- 所有可见按钮、工具提示和可访问名称使用中文，不保留 `Add`、`Refresh` 等英文名称。

## 十一、账单导入升级合同

- 在空状态中央提供明确的 PDF 选择和拖放区域。
- 显示选择文件、提取、解析、逐行处置、预览和确认六个阶段。
- 明确说明原始 PDF、本机提取、脱敏内容和长期保存边界。
- 左侧显示文件与页面证据，中间显示识别行，右侧显示当前行处置。
- 全部、可用、缺失等筛选使用统一分段控件。
- 每一行清楚展示原始证据、识别内容、匹配候选、缺失字段和处理状态。
- 批量选择、单行处置和确认预览使用不同的操作层级。
- 没有选择行时不重复显示多条相同的禁用原因。
- 导入失败、部分成功、冲突、未知结果、分页失败和离线状态保持可恢复。

## 十二、系统与数据升级合同

- 将页面拆分为运行状态、数据归档、恢复边界和个人口令四个清楚区域。
- 备份、恢复演练和磁盘状态使用统一状态卡，突出正常、警告和失败。
- 归档密码字段和创建操作组成独立任务区。
- 恢复限制使用说明区表达，不把永久禁用按钮作为主要内容。
- 修改个人口令使用独立安全表单。
- 未填写任何口令时不显示字段错误。
- 错误只在相关字段修改或提交后出现。
- 安全说明保持可见，但不重复同一句不可用原因。
- AI 待确认和账单导入改为标准工具栏入口，不继续使用两个占满整行的描边按钮。
- 最大化窗口下合理使用多列，不把所有内容锁在左侧窄列。

## 十三、设置升级合同

### 13.1 主数据入口

- macOS 主数据必须提供账户、分类、商户三个可见且可操作的切换入口。
- 分类和商户不得继续处于存在实现但界面不可达的状态。
- 切换主数据类型后，列表、详情和新建动作同步更新。
- 当前主数据类型、当前选中项和新建状态必须明确。

### 13.2 账户

- 账户列表显示名称、类型和必要状态。
- 账户编辑按基本信息、余额、信用规则和归档操作分区。
- 新建账户空表单不允许直接保存。
- 信用账户才显示额度、账单日、还款日和账期方式。
- 排序操作使用明确的顺序控制，不依赖难发现的小文字按钮。
- 归档和恢复与保存分开。

### 13.3 分类

- 分类列表显示名称、方向、图标、颜色和层级。
- 新建、修改、排序、合并、拆分、归档和恢复入口全部可达。
- 合并与拆分必须先显示预览，再允许确认。
- 涉及历史账目重新归类时清楚展示影响数量和目标分类。
- 收入分类和支出分类使用明确分组。

### 13.4 商户

- 商户列表提供搜索、分页、名称和别名摘要。
- 新建与编辑商户使用统一表单。
- 搜索条件改变后，旧分页状态失效并显示当前已提交条件。
- 空结果、读取失败和下一页失败提供明确恢复操作。

### 13.5 归档区

- 账户、分类、报销单和已作废账目按类型分组。
- 每个归档项使用紧凑行布局，不再为每项放置整行恢复按钮。
- 归档类型、日期、金额、关联数量和只读状态保持可见。
- 恢复作为次要操作，并在执行前明确对象。
- 超过显示上限时提供清楚的范围说明和对应页面入口。

### 13.6 AI 与识别

- 将配置状态、自动记账边界、OCR 来源和快捷指令来源组织为完整状态面板。
- 每项显示名称、当前状态和必要说明。
- 保持“AI 不自动记账，每笔由用户确认”的现有安全边界。
- 页面不得只剩几行文字和大片空白。

## 十四、状态与反馈合同

- 所有异步读取显示稳定的骨架或进度状态，不让页面结构跳动。
- 空状态说明当前没有什么，并提供唯一合理的下一步。
- 服务失败显示具体失败位置和重试入口。
- 离线状态显示快照时间并明确只读范围。
- 冲突状态显示发生变化的字段，并提供重新读取入口。
- 未知结果不得宣称操作失败或成功，必须提供核验与恢复路径。
- 成功操作显示简短回执，并在数据重新读取后自动收敛。
- 禁用状态保留原因，但同一原因只出现一次。
- 危险操作使用确认界面，明确对象、影响和可恢复性。
- 页面切换、选中、展开和关闭使用短而克制的过渡动画，不添加纯装饰动画。

## 十五、键盘与可访问性合同

- 所有主要页面可以使用 Tab 和 Shift-Tab 按顺序访问控件。
- 列表支持方向键或既有快捷键移动选择。
- Enter 用于确认当前主要操作，Esc 用于关闭搜索、弹窗或未保存编辑器。
- 可点击控件具有足够命中区域，不依赖精确点击小图标。
- 图标按钮提供中文工具提示、中文可访问名称和正确角色。
- 当前选中、禁用、只读、错误和危险状态可被辅助功能识别。
- 不使用仅靠颜色才能理解的状态。
- 金额、日期、账户和交易方向的朗读顺序保持自然。
- 键盘焦点始终可见，并与鼠标选中状态区分。

## 十六、验收合同

### 16.1 页面验收

- 完整检查流水、记一笔、已知未来、现金流计划、信用账期、分期、报销、四类报表、报表下钻、AI 待确认、账单导入、系统与数据、主数据、归档区、AI 与识别。
- 每个页面检查常用窗口和最大化窗口。
- 每个多栏页面检查无选择、已选择、编辑中和操作不可用状态。
- 每个表单检查初始、部分填写、字段错误、可提交、提交中、成功和失败状态。
- 每个列表检查空数据、少量数据、长数据、加载和分页失败状态。

### 16.2 必须通过的操作验收

- 设置中账户、分类、商户三个入口全部可达。
- 账户、分类和商户的新建与编辑路径完整。
- 报销总额、已到账和未到账金额不会再产生语义误解。
- 新表单打开时不提前显示错误。
- 同一禁用原因不会重复显示。
- AI 待确认与历史记录有清楚分层。
- 报表月报与年报切换方式明确。
- 报表下钻在最大化窗口下使用有效宽度。
- 所有主要操作、危险操作和只读状态能够一眼区分。
- 搜索、返回、取消、刷新、导出和关闭行为在所有页面保持一致。

### 16.3 视觉验收

- 为每个 macOS 页面保留常用窗口和最大化窗口截图。
- 为关键编辑器、弹窗、错误、空状态、离线、冲突和成功回执保留截图。
- 页面不得存在被锁在窄列的大面积无意义空白。
- 页面不得存在为了单行塞满而缩小文字的情况。
- 页面不得存在普通按钮、危险按钮和只读状态外观混淆。
- 页面不得存在中英文混用的可见文案或可访问名称。
- 全部页面使用同一套间距、圆角、颜色、字体和控件状态。

### 16.4 工程验收

- macOS 相关单元测试通过。
- FiscalKit 共享测试通过。
- FiscalmacOS target 编译通过。
- FiscaliOS generic iOS Simulator target 编译通过，确认共享组件未破坏 iOS。
- 所有新增视觉状态进入可重复验证的 Gallery、fixture 或等价测试入口。
- 真实 macOS App 完成全页面只读走查和写入流程验收。
- 独立代码复审通过。
- 独立视觉复审通过。
- 用户完成最终视觉确认后，才允许进入发布流程。
